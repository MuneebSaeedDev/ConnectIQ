# Figma Screen Inventory

Generated and maintained by `figma-project-audit`.

## Summary

- Figma source: https://www.figma.com/design/FU593OPeUscEvpu3hbOZrT/Ezitech-Project (file key `FU593OPeUscEvpu3hbOZrT`, page `0:1` "Page 1" — single-page file)
- Last audit: 2026-08-18
- Traversal method: Figma MCP `get_metadata` (full-document XML traversal, 76 top-level frames, ~48.3k frame nodes / 21.8k text nodes / 471 instances / 209 vectors), `get_design_context` and `get_screenshot` spot-checks, `get_variable_defs` and `get_libraries` for design-system tokens.
- Total discovered screens: 71
- Total discovered shared/reusable component frames (not counted as screens): 5 (`Header`, `Footer`, `Global UI Components` / `AppShell`, `Sidebar` expanded, `Sidebar` collapsed) — all 5 now `DONE` (2026-08-22), see the Shared/reusable component frames table below.
- Verified implemented screens: 29 (SCR-033 Add User Screen `DONE`, built 2026-08-26 by `frontend-screen-builder` — second MOD-005 screen; SCR-032 User List Screen `DONE`, built 2026-08-26 — first MOD-005 screen; SCR-001 through SCR-031 `DONE` — SCR-027 Edit Organization Screen `DONE`, built 2026-08-24 by `frontend-screen-builder`; SCR-026 Create Organization Screen `DONE`, built 2026-08-24; SCR-025 Organization Details Screen `DONE`, built 2026-08-24; SCR-024 Organization List Screen `DONE`, built 2026-08-24; SCR-023 Real-Time Monitoring Dashboard Screen `DONE`, built 2026-08-24; SCR-022 Performance Analytics Dashboard Screen `DONE`, built 2026-08-24; SCR-021 Error Analytics Dashboard Screen `DONE`, built 2026-08-24; SCR-020 Execution Statistics Dashboard Screen `DONE`, built 2026-08-23; SCR-001–SCR-019 `DONE`, built 2026-08-22/23. SCR-010 resolved by existing shared Sidebar; SCR-011 resolved by ConnectIQ brand rename — see build notes)
- Remaining screens: 43
- Blocked screens: 0
- A frontend codebase now exists at `frontend/` (Vite + React + TypeScript, Redux Toolkit, React Query, React Router — scaffolded 2026-08-22 as part of building SCR-001, per agent.md §5.1 stack). All other screens below remain `DISCOVERED` only — this audit note is preserved historically; see the Screen Matrix status columns for current per-screen state.

Module names in the table below are **candidate groupings inferred from screen names and `agent.md`'s module list**, not finalized module IDs. Formal `MOD-XXX` identifiers and the authoritative screen-to-module mapping are now assigned in `docs/modules/module-plan.md` (generated 2026-08-22 by `module-planner`). Do not treat the Module column below as authoritative — cross-reference `docs/modules/module-plan.md`'s Module Matrix for the current mapping:

- Shell/Bootstrap → MOD-001 · Auth → MOD-002 · RBAC → MOD-003 · Organization Management → MOD-004 · User Management → MOD-005 · Data Sources → MOD-006 · Destinations → MOD-007 · Pipelines → MOD-008 · Analytics/Monitoring Dashboards → MOD-009 · Notifications → MOD-010.

## Screen Matrix

| ID | Figma Page/Frame | Figma Node ID | Screen | Route (proposed, unverified) | States/Variants | Module (candidate) | Frontend Status | Overall Status | Dependencies |
|---|---|---|---|---|---|---|---|---|---|
| SCR-001 | Page 1 | `13:3982` | Splash Screen | `/` (boot) | System-initialization progress, version tag; error/retry state added (not in Figma, extrapolated per design-system-notes.md gap) | Shell/Bootstrap | DONE | DONE | none |
| SCR-002 | Page 1 | `14:4055` | Login Screen | `/login` | Default; SSO button (disabled — no SSO provider integration exists yet, see notes); password visibility toggle; "Keep me signed in" | Auth | DONE | DONE | none |
| SCR-003 | Page 1 | `14:4162` | Reset Password Screen | `/reset-password` | Default form state; sent-confirmation state added (not in Figma, extrapolated per agent-rules.md §5 success-feedback requirement) | Auth | DONE | DONE | Login |
| SCR-004 | Page 1 | `16:4495` | Forget Password Scren *(Figma name has a typo: "Scren")* | `/forgot-password` | Token-based new-password form (real content, see SCR-003/SCR-004 conflict note below); missing/invalid-token state and done-confirmation state added beyond Figma per agent-rules.md §5 | Auth | DONE | DONE | Login |
| SCR-005 | Page 1 | `18:5023` | Email_Verification Screen | `/verify-email` | Pending (default, masked email + resend + cooldown); verified state and invalid/expired-link error state added (not in Figma, extrapolated per agent-rules.md §5) | Auth | DONE | DONE | Login/Registration |
| SCR-006 | Page 1 | `19:5127` | Two Factor Authentication Screen | `/2fa` | Default code-entry; error state added (not in Figma, extrapolated per agent-rules.md §5) | Auth | DONE | DONE | Login |
| SCR-007 | Page 1 | `19:5237` | Change Password Screen | `/account/change-password` | Default form state; current-password-incorrect error; confirm-mismatch error; done-confirmation state | Auth / User Profile | DONE | DONE | Auth session |
| SCR-008 | Page 1 | `20:5458` | Logout Confirmation Screen | `/logout` | Confirm dialog (default); signed-out confirmation state added (not in Figma, extrapolated per agent-rules.md §5) | Auth | DONE | DONE | Auth session |
| SCR-009 | Page 1 | `27:2238` | Main Dashboard Layout Screen | `/dashboard` | Default (loading skeleton, error+retry, empty pipeline list, populated); real-time "Updated Xm ago" via 30s refetch | Dashboard Shell | DONE | DONE | Auth |
| SCR-010 | Page 1 | `33:3288` | Sidebar Navigation Screen | shell component | Default | Dashboard Shell | DONE (resolved by existing Sidebar — see SCR-010 build notes) | DONE | Auth |
| SCR-011 | Page 1 | `34:4157` | Header / Top Navigation Screen | shell component | Default | Dashboard Shell | DONE (resolved by rename, not rebuilt — see SCR-011 build notes) | DONE | Auth |
| SCR-012 | Page 1 | `36:5210` | Notification Panel Screen | shell overlay | Default; loading skeleton, error+retry, per-tab empty states added (not in Figma, extrapolated per agent-rules.md §5); tab filters (All/Unread/Alerts/System) | Dashboard Shell / Notifications | DONE | DONE | Auth |
| SCR-013 | Page 1 | `39:6447` | User Profile Menu Screen | shell overlay | Default dropdown | Dashboard Shell | DONE | DONE | Auth |
| SCR-014 | Page 1 | `45:7` | Executive Dashboard Screen | `/dashboard/executive` | Default with KPI cards, success-rate widgets | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell |
| SCR-015 | Page 1 | `49:1793` | Pipeline Overview Dashboard Screen | `/dashboard/pipelines` | Default, success-rate trend chart | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell |
| SCR-016 | Page 1 | `50:4167` | Data Quality Dashboard Screen | `/dashboard/data-quality` | Default, 8 KPI cards, quality score trend + alerts, validation results + dataset health, rule violations + freshness SLA, schema monitoring, failed-validations table (search/filter/pagination) | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell |
| SCR-017 | Page 1 | `51:6763` | System Health Dashboard Screen | `/dashboard/system-health` | Default, error-rate/SLA banner | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell |
| SCR-018 | Page 1 | `56:11333` | Source Health Dashboard Screen | `/dashboard/source-health` | Default, auth-errors widget | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell, Data Sources |
| SCR-019 | Page 1 | `65:17501` | Destination Health Dashboard Screen | `/dashboard/destination-health` | Default, delivery/sync success-rate widgets | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell, Destinations |
| SCR-020 | Page 1 | `84:5098` | Execution Statistics Dashboard Screen | `/dashboard/executions` | Default, success/failure analysis chart | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell, Pipelines |
| SCR-021 | Page 1 | `69:37077` | Error Analytics Dashboard Screen | `/dashboard/errors` | Default; error table with search/filter; "Showing 1–8 of 248 errors" pagination | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell |
| SCR-022 | Page 1 | `70:43606` | Performance Analytics Dashboard Screeb *(Figma name has a typo: "Screeb")* | `/dashboard/performance` | Default; component table with search/filter; "Showing 1–8 of 142 components" pagination | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell |
| SCR-023 | Page 1 | `72:45894` | Real-Time Monitoring Dashboard Screen | `/dashboard/realtime` | Default; Live/Paused status pill + Pause-updates toggle (15s polling); status banner; 12 KPI cards; live execution activity table (status chips); active pipelines + worker/queue utilization; infrastructure metrics + throughput sparkline + connectivity; live event stream + active alerts; live monitoring table with search/type/status/env filters + "Showing 1–10 of 148 components" pagination | Analytics/Monitoring Dashboards | DONE | DONE | Dashboard Shell |
| SCR-024 | Page 1 | `90:8293` | Organization List Screen | `/organizations` | Default; platform-admin org list; 5 KPI cards (Total 1,284 / Active 1,108 / Trial 124 / Suspended 38 / Expiring 14); search + Status/Plan/Region filters + active-filter chips + Clear all + Columns; bulk-selection bar (Activate/Suspend/Assign Plan/Export/Delete); 12-col table (checkbox, org avatar+name, ID, primary admin, plan badge, status pill, users, pipelines, last activity, created, region, actions); "Showing 1–12 of 1,284" pager; client-side search/filter/pagination; MOD-004 mock boundary + Sample-data badge | Organization Management | DONE | DONE | Auth (Super Admin) |
| SCR-025 | Page 1 | `91:9827` | Organization Details Screen | `/organizations/:id` | Default; page header (avatar, name, Active status pill, ID/Plan/Region/Created/Admin metadata, Edit/Manage Subscription/More Actions); 6 KPI cards; Organization Overview + Organization Info; Subscription & Licensing (license + storage meter bars); Pipeline Activity (stats + table); Platform Health; Users table; Quick Actions; Connectors (summary + table); Security Status; 7-item Recent Audit Activity timeline; React Query 30s refetch keyed on route id; MOD-004 mock boundary + Sample-data badge | Organization Management | DONE | DONE | Organization List |
| SCR-026 | Page 1 | `92:2` | Create Organization Screen | `/organizations/new` | Default form; nested `ConfirmationDialog` (`92:1477`) | Organization Management | DONE | DONE | Organization List |
| SCR-027 | Page 1 | `93:1562` | Edit Organization Screen — **Figma frame is misnamed "Static webpage design"; its inner content frame is named "EditOrganizationScreen" (`93:1566`)** | `/organizations/:id/edit` | Default form; live dirty-tracking (Modified badges, unsaved-changes counter), read-only fields (Organization Code, Renewal Date, Trial Expiration) + ID/Created/Last-Updated metadata block, live Change Summary diff card, 6-item Validation Status, Recent Activity, collapsible Advanced Configuration; loading skeleton, error+retry, mock-boundary Sample-data badge + simulated-save alert; unsaved-changes navigation guard (Cancel/Back/Audit-log + beforeunload); nested `SaveConfirmationDialog` (`93:3115`) — focus-trapped, Escape-to-close, focus restore | Organization Management | DONE | DONE | Organization Details |
| SCR-028 | Page 1 | `93:3192` | Organization Setting Screen | `/organizations/:id/settings` | Default; 7 setting cards (General, Security & Authentication, Notification, Branding, Integration, Operational Defaults, Compliance & Audit) with selects/toggles/free-text; live dirty-tracking (Modified badges, unsaved-changes pill, live Change Summary diff); inline validation (display name required, IPv4 CIDR list, https webhook URLs, portal-header length, `#RRGGBB` brand color); read-only right rail (Organization Summary, Validation Status checklist, Security Health, Recent Configuration Activity, Quick Actions) + read-only API-key table; loading skeleton, error+retry; React Query keyed on route id with `staleTime: Infinity` (no refetch — won't clobber edits); MOD-004 mock boundary + Sample-data badge + simulated-save alert; unsaved-changes navigation guard (Cancel/Back/Quick-Actions/Recent-Activity + beforeunload); backend-dependent actions (logo upload, key rotate/revoke/generate) disabled with explanatory titles citing MOD-002/004/005; focus-trapped Save `ConfirmDialog` (Escape-to-close, focus restore) | Organization Management | DONE | DONE | Organization Details |
| SCR-029 | Page 1 | `94:5039` | Department Management Screen | `/organizations/:id/departments` | Default; nested `DetailDrawer` (`94:6298`) | Organization Management | DONE | DONE | Organization Details | Built `DepartmentManagementScreen.jsx` + `useDepartmentManagement.js` + `departmentManagement.api.js` (MOD-004 real-fetch-first + content-type-guard + `mocked:true` fallback, org-scoped). AppShell shell, 6 KPI cards (24/21/18/247/183/3), searchable/status+business-unit-filterable/sortable/paginated 12-row departments table with filter chips, page-scoped select-all (indeterminate) + disabled+titled bulk actions, per-row focus-trapped `DetailDrawer` (operational summary, department info, DE member/pipeline previews with labeled MOD-004 boundary for other rows), loading/error/empty states, `sr-only` live region, "Sample data" badge. Route registered before `/organizations/:id` catch-all; "Departments" header action wired from Organization Details. ui-reviewer FAIL→PASS (fixed the "Last Activity" sort mis-wired to `attention`; added a sortable `activityMinutes` field) + tester PASS (lint + build clean). |
| SCR-030 | Page 1 | `95:6572` | Team Management Screen | `/organizations/:id/teams` | Default; nested `DetailDrawer` (`95:8111`) | Organization Management | DONE | DONE | Organization Details, Department Management | Built `TeamManagementScreen.jsx` + `useTeamManagement.js` + `teamManagement.api.js` (MOD-004 real-fetch-first + content-type-guard + `mocked:true` fallback, org-scoped). AppShell shell, 6 KPI cards (41/37/34/247/183/5), searchable/department+team-lead+status-filterable/sortable/paginated 14-row teams table with filter chips, department dot+code, team-lead avatar/Unassigned, favorite star, requires-attention flag, page-scoped select-all (indeterminate) + disabled+titled bulk actions (Activate/Archive/Assign Department/Assign Team Lead/Export/Delete), per-row focus-trapped `DetailDrawer` (operational summary incl. executions-7d/success-rate, team info, PC member/pipeline previews with labeled MOD-004 boundary for other rows, ownership), loading/error/empty states, `sr-only` live region, "Sample data" badge. Routes `/organizations/:id/teams` (before `/organizations/:id` catch-all) and `/organizations/current/teams` (Administration→Teams nav) both render it; `orgId` falls back to `'current'`. Default status filter `Active`. ui-reviewer PASS (no blockers; Figma pixel-fidelity not-verified as MCP unreachable in reviewer's tool set — inspected by builder via get_metadata/get_screenshot; applied fixes: `text-white`→`text-on-primary`, added Created column, reconciled favorite-star + name-column verification notes) + tester PASS (lint + build clean, data-contract consistent, no SCR-022/SCR-029-class filter/sort bug). |
| SCR-031 | Page 1 | `99:2052` | Organization Activity Screen | `/organizations/:id/activity` | Default; nested `DetailDrawer` (`99:3801`); shows an integration error entry ("OAuth2 token expired", "Transformation error — schema mismatch") | Organization Management | DONE | DONE | Organization Details |
| SCR-032 | Page 1 | `101:4147` | User List Screen | `/users` | Default; nested `UserDrawer` (`101:5921`) | User Management | DONE | DONE | Auth |
| SCR-033 | Page 1 | `103:6282` | Add User Screen | `/users/new` | Default form (IP allowlist field with empty-value hint); nested `ConfirmDialog` (`103:7455`) | User Management | DONE | VERIFIED | User List |
| SCR-034 | Page 1 | `105:7565` | Edit User Screen | `/users/:id/edit` | Default form; nested `ConfirmDialog` (`105:9112`) | User Management | DONE | VERIFIED | User List | Built `EditUserScreen.jsx` + `useEditableUser.js` + `editUser.api.js` (MOD-005 real-GET/PATCH-first + content-type-guard + `mocked:true` fallback; select sets re-exported from `addUser.api.js`; edit-specific `EDIT_USER_OPTIONS` businessUnit/concurrentSessions). Combines SCR-033 form primitives/visual language with SCR-027 edit-form mechanics: React Query baseline load (`staleTime:Infinity`, no `refetchInterval`, `refetchOnWindowFocus:false` so edits aren't clobbered), `EditForm` keyed on `data.id` for fresh mount, live dirty-tracking + Pending Changes diff vs saved baseline, `beforeunload` guard, `window.confirm` guarded in-app nav, Reset Changes. Design's "3 unsaved changes" modeled as SAVED baseline (John Smith / USR-004812 / EMP-00089) + live user edits (not seeded dirty). 6 numbered sections (Personal Information w/ read-only Employee ID + Account Metadata dl; Organization Assignment w/ dept-transfer-impact callout; Roles & Access w/ role-elevation notice + Access Scope + Effective Permissions grid; License Management w/ read-only status/assigned + org utilization meter 273/300 + Feature Access grid; Authentication & Security w/ read-only status strip + 4 toggles + IP-range CIDR validation + concurrent sessions + disabled Reset Password/Unlock Account/Force Logout admin actions; Notification Preferences w/ forced-on System & Security Alerts). Right rail: User Summary, Account Health, License Summary, Validation Status (5/5), Pending Changes diff, Recent Activity. Focus-trapped `ConfirmDialog` (Escape/Tab-cycle/focus-restore) showing change diff + "What happens next". Route `/users/:id/edit` (after `/users/new`). Header status pill + unsaved-changes badge + "Sample data" badge; simulated-save warning banner. ui-reviewer FAIL→fixed (Department required-field wiring now mirrors AddUser: `required`/`error`/`invalid`/`onBlur`; Save disabled after mocked/successful save until further edit; status pill tone maps Active→success/other→warning) then re-verified; tester PASS (oxlint + `vite build` clean, no findings on new files). Figma pixel-fidelity not independently re-verified (MCP unreachable in reviewer's tool set); layout/content transcribed from frame text nodes by builder (see `editUser.api.js` FIGMA VERIFICATION header). |
| SCR-035 | Page 1 | `105:9208` | User Details Screen | `/users/:id` | Default; "Last Successful Login" field | User Management | DONE | VERIFIED | User List | Built `UserDetailsScreen.jsx` + `useUserDetails.js` + `userDetails.api.js` (MOD-005 real-GET-first + content-type-guard + `mocked:true` design-sourced baseline for John Smith / USR-004812; shared `ROLE_ACCESS_SCOPE`/`FEATURE_ACCESS_BY_LICENSE` re-exported from `addUser.api.js`). Read-only detail view mirroring the SCR-034 pattern-of-record. Route `/users/:id` in `router.jsx` after `/users/:id/edit` (literal-before-param). Reached from the User List `UserDrawer`'s new "View Full Profile" button. Hero header (JS avatar, name, Active status pill, "Senior Data Engineer · Data Engineering · Pipeline Core" subtitle, Engineer/MFA/SSO/Full License chips; disabled Reset Password/Suspend/More + enabled Edit User→`/users/:id/edit`). Left rail: User Summary, Account Status, Security Health (6-item checklist + password age/sessions/failed metrics), License Summary, Quick Actions. 8 numbered sections: (1) User Profile, (2) Organization Assignment (linkable dept/team/manager), (3) Roles & Permissions (+ Engineer Effective Access Scope + View Permission Details), (4) License Information (+ 9-feature Enabled Features grid), (5) Authentication & Security (13 read-only fields incl. "Last Successful Login" + disabled Administrative Actions Reset Password/Unlock/Force Logout), (6) Assigned Resources (Pipelines 18/Connectors 6/Data Sources 3/Scheduled Jobs 12/Dashboards 0/Reports 2, each linked), (7) Recent Activity (8 tagged items), (8) Audit History (6 actor-attributed items). "Sample data" disclosure banner gated on `mocked`; backend-dependent actions disabled with explanatory `title`. sr-only `role=status` region (now announces loaded state), `role=alert` error+Retry, loading skeleton. ui-reviewer PASS (1 medium + 3 low, no blockers); tester PASS (oxlint + `vite build` clean, no new findings, no secrets, mock-fallback correct). In-scope fixes applied: (medium) success state now announced in the live region; (low) live-record shape guard hardened (`data.profile` in addition to `data.userId`). Deferred/documented: disabled-action a11y relies on `title` + adjacent visible copy (kept — disabled buttons aren't focusable); literal glyphs (More ▾/←/arrows) coexist with the inline SVG icon set. Figma pixel-fidelity transcribed from frame text nodes by builder (see `userDetails.api.js` FIGMA VERIFICATION header); reviewer's MCP tools were not exposed so not independently re-verified. |
| SCR-036 | Page 1 | `109:12114` | User Profile Screen | `/profile` | Default; account-recovery fallback verification section | User Management | DONE | VERIFIED | Auth | Built `UserProfileScreen.jsx` + `useUserProfile.js` (`useMyProfile`, queryKey `['users','me','profile']`, `staleTime:30s`, no window-focus refetch) + `userProfile.api.js` (self-service SELF profile, distinct from admin SCR-035; real GET `/me`-first + content-type guard + `data.form`/`data.userId` shape check → `mocked:true` design-sourced baseline for Marcus Chen / usr_8f3h2p9x / EMP-00442 / Acme Corp; `saveMyProfile` real-PUT-first → simulated success; `PROFILE_OPTIONS` design-sourced select sets). Route `/profile` now renders the screen (was RoutePlaceholder). AppShell breadcrumb ConnectIQ › Settings › My Profile. Header (My Profile + subtitle, live unsaved-change count badge, Sample-data badge, Cancel/Reset Changes/Save Changes gated on `!isValid||!dirty||saved`). Editable: Personal Information (avatar meta + First/Last/Display required, managed read-only Work Email, Phone/Job Title/Office Location/Pronouns, read-only User/Employee/Org identifiers), Personal Preferences (7 localization selects), Notification Preferences (5 email + 4 in-app toggles). Read-only: Security (MFA/Password/Recovery Codes/Security Questions rows + password-expiry warning banner), Active Sessions table (3), Personal API Tokens table (3), Recent Account Activity (6 tagged). Right rail: Profile Summary, Account Status, Security Health (score 85 + 5-item checklist), Quick Actions, Help & Support. Live dirty-tracking + Modified badges, `validate()` (first/last/display required), `beforeunload` + `window.confirm` guarded in-app nav (guardedNavigate wraps Cancel AND Quick Actions), simulated-save disclosure (role=alert), sr-only live region, loading skeleton, error+Retry. Backend-dependent actions (Upload Photo, MFA/Password/token/session controls, Sign Out All Devices, Quick Action shortcuts) `disabled` + explanatory `title={MOD005_TITLE}`. ui-reviewer FAIL→fixed→re-PASS: (HIGH) WCAG-AA contrast — added `--color-warning-strong` #b45309 (~4.9:1) token + `warning-strong` utility, swapped all meaningful warning TEXT off `text-warning` (kept for icons/dots/borders only); (MED) Quick Actions "View Activity Log" now routes through the dirty guard; (MED) "Sign Out All Devices" now disabled+title (was raw navigate('/logout')); (LOW) fixed dangling `aria-describedby` on Work Email; (LOW) removed dead email validation path. tester PASS (oxlint + `vite build` clean on new files, no test runner exists). Figma layout/content transcribed from node 109:12114 text nodes (get_design_context + get_screenshot this session; see `userProfile.api.js` FIGMA VERIFICATION header). |
| SCR-037 | Page 1 | `109:13321` | User Activity History Screen | `/users/:id/activity` | Default list; right-hand `ActivityDetails` inspector | User Management | DONE | VERIFIED | User Profile / User Details | Built `UserActivityHistoryScreen.jsx` + `useUserActivityHistory.js` (queryKey `['organizations',org,'users','activity',id]`, `staleTime:30s`, no window-focus refetch) + `userActivityHistory.api.js` (MOD-005 real-GET-first `/organizations/:org/users/:id/activity` + content-type guard + `data.activities[]`/`data.user` shape check → `mocked:true` design-sourced baseline for John Smith / USR-004812). Route `/users/:id/activity` registered before `/users/:id` (literal-before-param); reached from SCR-035 "View Full Activity History" + Quick Action "View Activity" (both re-pointed here from the org activity screen). Chosen `/users/:id/activity` over `/profile/activity` since the frame shows an admin-scoped user context strip + "View User Details", not a self-service view. Header (title + subtitle, Refresh (live refetch, spins), disabled Save Filter + Export Activity History). User context strip (JS avatar, John Smith, Senior Data Engineer, john.smith@acmecorp.com, Department/Team/Role/Account Status/Last Login/Member Since, View User Details link). Six summary stat cards (Total Activities 2,847 / Login Events 186 / Pipeline Actions 1,204 / Administrative 38 / Security Events 12 / Failed Operations 29, warning/danger tones). Seven saved-view tabs (All Activity/Login History/Failed Logins/Pipeline Activity/Administrative Changes/Security Events/Connector Activity) each applying a category (+ status) filter. Filter row: search + Activity Types/Resources/Statuses/Severities selects + Newest/Oldest sort + Clear (real client-side filtering/sorting over the 12 seeded rows, all wired). Eight-column table (Timestamp/Activity/Category/Resource/Action/Status/Severity/Source) with status+severity pills, keyboard-selectable rows (Enter/Space, aria-selected within `role=grid`), empty-state with Clear affordance, pagination footer (10/page → 2 pages over the seeded rows). Right `ActivityDetails` inspector driven by row selection: Event Summary/User Context/Resource Information/Activity Timeline (checkmarked steps)/Technical Metadata (redacted-tail IPs) for the fully-detailed first row, graceful summary for the rest; live Copy Event ID (client-only clipboard), disabled View Audit Record/Export Event. "Sample data" disclosure gated on `mocked`; sr-only `role=status` live region; `role=alert` error+Retry; loading skeleton. ui-reviewer FAIL→fixed→re-clean: (MED) inspector Close now reaches its empty state (`selectedId===null` sentinel vs initial row) instead of re-selecting row 1; (MED) empty filter set no longer leaks an out-of-filter activity into the inspector (dropped `data.activities[0]` fallback); (MED/HIGH) "Sample data" banner body text moved to `text-warning-strong` (WCAG-AA, aligns to the SCR-036 standard-of-record); (MED) Warning/Medium status+severity pills moved to `text-warning-strong`; (LOW) sort select no longer renders a duplicate "Newest First" placeholder (`noPlaceholder`); (LOW) `PAGE_SIZE` 25→10 so pagination is demonstrably exercised, `totalCount` documented as the server-paginated full-set figure; (LOW) table given `role=grid`/`row`/`columnheader`/`gridcell` so `aria-selected` is semantic; (LOW) Copy Event ID made live (pure client clipboard action, graceful fallback in insecure context); (LOW) account-status dot color now mapped from status value. tester PASS (oxlint + `vite build` clean on new files; no test runner/type-check/Playwright in repo — documented tooling gaps). Figma layout/content transcribed from node 109:13321 text nodes (get_design_context + get_screenshot this session; see `userActivityHistory.api.js` FIGMA VERIFICATION header); pixel fidelity not independently re-pulled by the reviewer (Figma MCP not in its tool set). |
| SCR-038 | Page 1 | `109:14710` | User Login History Screen | `/users/:id/login-history` | Default list; right-hand `LoginDetails` inspector; Active Sessions panel | User Management | DONE | VERIFIED | User Profile / User Details | Built `UserLoginHistoryScreen.jsx` + `useUserLoginHistory.js` (queryKey `['organizations',org,'users','login-history',id]`, `staleTime:30s`, no window-focus refetch) + `userLoginHistory.api.js` (MOD-005 real-GET-first `/organizations/:org/users/:id/login-history` + content-type guard + `data.events[]`/`data.user` shape check → `mocked:true` design-sourced baseline for John Smith / USR-004812). Route `/users/:id/login-history` registered before `/users/:id` (literal-before-param). Modeled on the SCR-037 pattern-of-record. Header (title + subtitle, Refresh (live refetch, spins), disabled Save Filter + Export Login History). User context strip (JS avatar, John Smith, Senior Data Engineer, john.smith@acmecorp.com, Department/Team/Role/Account Status/Last Successful Login/Last Failed Login, View User Details link + disabled Force Reset). Six summary stat cards (Total Login Events 863 / Successful Logins 847 / Failed Attempts 16 / Active Sessions 2 / MFA Challenges 831 / Suspicious Events 3, success/warning/danger tones). Seven saved-view chips (All Logins/Failed Logins/Suspicious Activity/Active Sessions/MFA Failures/New Device Logins/High Risk Events) each applying an authResult/mfa/device/risk/session filter. Filter row: search + Auth Result/Method/Device/Risk selects + Newest/Oldest sort + Clear (real client-side filtering/sorting over 12 seeded rows). Eleven-column table (Login Time sortable/Auth Result/Method/MFA/Device/Browser/OS/Location/IP Address (masked octets `•••`)/Session/Risk) with auth+risk pills, MFA glyph cell (✓/✕/—, success/danger/muted tone), keyboard-selectable rows (Enter/Space, aria-selected within `role=grid`), empty-state with Clear affordance, pagination footer (12/page → design's single "1–12 of 863 total" page). Active Sessions panel (2 devices, Current badge, per-row Terminate + disabled Force Password Reset/Terminate All Sessions). Right `LoginDetails` inspector driven by row selection: Authentication Summary/User Context/Device Information/Network Information/Authentication Timeline (checkmarked steps)/Technical Metadata for the fully-detailed first row, graceful field summary for the rest; live Copy Event ID (client-only clipboard, now with insecure-context `title`), disabled View Session/View Security Event/Export Event. "Sample data" disclosure gated on `mocked`; sr-only `role=status` live region; `role=alert` error+Retry; loading skeleton. ui-reviewer PASS (no blockers; 1 medium + 3 low). In-scope fixes applied: (MED) `PAGE_SIZE` 10→12 so page 1 matches the design's "Showing 1–12 of 863" single-page footer; (LOW) invalid `py-token-10`→`py-token-9` (token scale stops at token-9); (LOW) disabled Copy Event ID now carries an explanatory `title`; (LOW) corrected "twelve-column"→"eleven-column" doc drift in the API header. Deferred: `py-token-10` + disabled-Copy-title gaps also exist in SCR-037 (flagged to that owner, out-of-unit); `ORG_ID='current'` shared placeholder across SCR-035/036/037/038 until org context ships. tester PASS (oxlint EXIT=0 + `vite build` 314 modules clean on new files; no test runner/type-check/Playwright in repo — documented tooling gaps). Figma layout/content transcribed from node 109:14710 text nodes (get_design_context + get_screenshot this session; see `userLoginHistory.api.js` FIGMA VERIFICATION header); the 11 filter dropdowns rendered as empty sublayer frames so option sets are `derived` from visible table content; pixel fidelity not independently re-pulled by the reviewer (Figma MCP not in its tool set). |
| SCR-039 | Page 1 | `109:16386` | User Permission Management Screen | `/users/:id/permissions` | Default | User Management / RBAC | VERIFIED | DONE | User Details, Roles |
| SCR-040 | Page 1 | `113:17935` | Role List Screen | `/roles` | Default | RBAC | DONE | VERIFIED | Auth | Built `RoleListScreen.jsx` + `useRoleList.js` (queryKey `['organizations',orgId,'roles',filters]`, `enabled:!!orgId`, `refetchInterval:30s`) + `roleList.api.js` (MOD-003 real-GET-first `/organizations/:org/roles` + content-type guard defending against Vite SPA HTML fallback → `mocked:true` design-sourced baseline of 14 roles; `MOCK_ROLE_LIST` + `ROLE_TYPE_OPTIONS`/`ROLE_STATUS_OPTIONS`/`PRIVILEGE_LEVEL_OPTIONS`/`PERMISSION_GROUP_OPTIONS`/`ROLE_SORT_OPTIONS`). MOCK BOUNDARY: MOD-003 backend still PLANNED (no Role/Permission/UserRole endpoint). Route `/roles` now renders the screen (was RoutePlaceholder shellRoute). AppShell breadcrumb ConnectIQ › Administration › Roles. Header (Role Management + subtitle, disabled Export Roles/Compare Roles/+ Create Role with mock-boundary titles, live Refresh). Six KPI cards (Total 14/System 9/Custom 5/Active Assignments 1,284/Administrative 3/Unused 2). Toolbar: search + Role Type/Status/Privilege Level/Permission Group selects + sort (Role Name/Assigned Users/Privilege Level/Last Updated) + disabled Columns + Clear Filters, with active-filter chips (all real client-side filtering/sorting over the 14 seeded rows). Disabled bulk-action bar (Export/Compare/Activate/Archive Selected) with page-scoped select-all (indeterminate ref). Eight-column table (Role Name w/ IconLock on System roles + Unused badge, Role Type, Permission Groups, Assigned Users, Privilege Level, Status, Last Updated, actions) with type/status/privilege pills, row → RoleDrawer (focus-trapped, Escape/Tab-cycle, Description/Role Details/Permission Groups sections, disabled actions). Pagination footer (25/page, "25 per page" indicator). Loading skeleton; "Sample data" disclosure gated on `mocked`. ui-reviewer FAIL→fixed: (HIGH) PRIVILEGE_TONE corrected (Administrative→`bg-warning-bg text-warning`, Elevated→`bg-shell-accent-wash text-primary`); (HIGH) permission-group filter added (`PERMISSION_GROUP_OPTIONS` export + state/filter/chip/dropdown); (MED) IconLock on System roles; (MED) disabled Columns control; (MED) PAGE_SIZE 16→25 + "25 per page" footer; (LOW) header "Type"→"Role Type"; (LOW) removed no-op RQ-v5 `keepPreviousData`. tester FAIL→fixed→PASS (was missing `PERMISSION_GROUP_OPTIONS` export + undefined `IconColumns`/`IconLock`; now `npm run build` clean 320 modules + oxlint EXIT=0, no roles findings). Re-review BLOCKED by environmental API-credit exhaustion (403 on subagent model), not a code issue; prior FAIL findings all verified addressed on disk + build/lint green. Figma layout/content transcribed from node 113:17935 text nodes (get_metadata + get_screenshot this session; see `roleList.api.js` FIGMA VERIFICATION header). |
| SCR-041 | Page 1 | `113:19063` | Create Role Screen | `/roles/new` | Default form | RBAC | DONE | VERIFIED | Role List | Built `CreateRoleScreen.jsx` + `createRole.api.js` (MOD-003 real-POST-first `/organizations/:org/roles` + `res.ok`+content-type JSON guard defending against Vite SPA HTML fallback + `data.id` shape check → `mocked:true` simulated `role_…` result). MOCK BOUNDARY: MOD-003 backend still PLANNED (no Role/Permission/UserRole endpoint). Route `/roles/new` now renders the screen; the SCR-040 Role List "+ Create Role" was upgraded from a disabled button to a real `<Link to="/roles/new">`. AppShell breadcrumb ConnectIQ › Administration › Roles › Create Role. Controlled multi-section form: Role Information (name, auto-generated snake_case role key w/ leading-letter guard, live 300-char description counter, category/type/privilege selects), Permission Groups (assigned/available split w/ scope tags + search + Add/Remove), Direct Permissions (grouped enable/deny w/ live enabled/denied counts + Grant-all/Deny-all), Administrative Privileges (toggle grid + elevated badge), Resource Access Scope (editable table w/ scope/access-level selects + conditions + add/remove rows), Permission Inheritance (inherit-from + template + inherited-count callout), Role Assignment Rules (assignableBy/maxScope/dept+team restrictions + requiresApproval/restrictedRole toggles). Right rail: Role Summary, Effective Access Overview (Total/Direct/Inherited/Resource Rules), Validation Status (progressbar + 6-item checklist, 5/6 — Security Review + No-Policy-Conflicts pending, no backend engine), Security Impact (Low/Med/High derived from admin+enabled counts). Focus-trapped Confirm Role Creation dialog (Escape/Tab-cycle/focus-restore). A mocked create shows a `role="alert"` disclosure banner and does NOT navigate; a real success routes to `/roles`. Save Draft disabled w/ MOD-003 `title`. ui-reviewer FAIL→fixed: (HIGH) false "36/17 split" doc-comment reworded to state counts are live-computed; (MED) `slugifyKey` leading-digit fix so generated keys satisfy `KEY_PATTERN`; (MED) "No Policy Conflicts" changed to honest `pending:true`; (LOW) `text-success`→`text-success-strong` on small text (WCAG AA); (LOW) `text-white`→`text-text-on-primary`; (LOW) dropped redundant char-counter `aria-live`. tester PASS (oxlint EXIT=0 on SCR-041 files, vite build clean 325 modules, `/roles/new` HTTP 200, all 16 imports resolve). Figma layout/content transcribed from node 113:19063 text nodes (get_screenshot + get_metadata this session; see `createRole.api.js` FIGMA VERIFICATION header). |
| SCR-042 | Page 1 | `113:20627` | Edit Role Screen | `/roles/:id/edit` | Default form | RBAC | DONE | VERIFIED | Role List | Built `EditRoleScreen.jsx` + `useEditableRole.js` (queryKey `['organizations',org,'roles','edit',roleId]`, `enabled:!!org&&!!roleId`, `staleTime:Infinity`, no refetchInterval/window-focus refetch — an edit form must not clobber in-progress edits) + `editRole.api.js` (MOD-003 real-GET-first `/organizations/:org/roles/:id/edit` + `res.ok`+content-type JSON guard defending against Vite SPA HTML fallback + `data.form` shape check → `mocked:true` design-sourced SAVED baseline for Data Engineer / role_7a3f2c91 / key `data_engineer`; `updateRole` real-PUT-first → simulated `{id,savedAt,mocked:true}`). MOCK BOUNDARY: MOD-003 backend still PLANNED (no Role/Permission/RolePermission/UserRole endpoint). Route `/roles/:id/edit` now renders the screen; SCR-040 RoleDrawer "Edit Role" upgraded from disabled button to `<Link to="/roles/:id/edit">`. AppShell breadcrumb ConnectIQ › Administration › Roles › {name} › Edit Role. Edit-form pattern-of-record from SCR-034 EditUser: loads the SAVED baseline (NOT the design's mid-edit dirty state — the design's "→"/"Modified from" transitions become the saved-from values and dirty diff is computed LIVE against them). Header (Edit Role + StatusPill + Sample-data badge + live "Unsaved changes · N" badge, Cancel/Reset Changes/disabled Save Draft (MOD-003 title)/Save Changes gated on `!isValid||!dirty||saved`). Editable sections: Role Information (name required + Modified badge, read-only immutable role key, 300-char description counter, category/status selects, role metadata grid), Permission Groups (assigned/available split w/ scope tags + Added/Removed markers), Direct Permissions (9 domains w/ live on/total counts + Modified badges + per-item toggles), Administrative Privileges (6 toggles + elevated security-review warning), Resource Access Scope (5 resource rows w/ access-level selects + live None→View transition text), Permission Inheritance (inherit-from/template/org+dept policy + inherited-count callout), Assignment Rules (assignableBy/maxScope/dept+team restrictions + requiresApproval/restrictedRole toggles). Right rail: Role Summary, Effective Access Overview (Total/Direct/Inherited/Resource Rules w/ deltas), Assigned Users Impact (187, guarded nav), Validation Status (5-item checklist), Security Impact (Low/Med/High derived from admin+scheduling changes), Pending Changes (live diff). Live dirty-tracking, `validate()`, `beforeunload` + `window.confirm` guarded in-app nav (guardedNavigate wraps Cancel + Assigned-Users link), focus-trapped Confirm dialog (Escape/Tab-cycle/focus-restore), `<EditForm key={data.id}>` remount on data load, Toggle (role=switch), simulated-save `role=alert` disclosure (does NOT navigate on mocked save), loading skeleton, error+Retry. ui-reviewer FAIL→fixed→re-PASS (2 passes): (HIGH) all meaningful 9px `text-token-xs` error/hint copy (name/category/status/description errors, char counter, "Modified from:", "Immutable after creation.") → `text-token-meta` (11px); (HIGH) validation errors now programmatically associated — each error node has `id`+`role="alert"`, field carries `aria-invalid`+conditional `aria-describedby`, empty description-error span replaced w/ real alert node + aria-hidden placeholder; (HIGH) all meaningful `text-warning` (~3:1) → `text-warning-strong` (~4.9:1, WCAG-AA) — simulated-save heading, Unsaved-changes badge, Sample-data badge, ChangeBadge, admin warning, ValidationStatus badge/labels/icons, SecurityImpact pill/icons, resource-scope transition, StatusPill Draft label, FieldLabel + domain "Modified" badges (bare `bg-warning` dot / `border-warning` / `bg-warning-bg` fills kept as icon/border/fill-only per tokens.css). Deferred (out-of-unit, non-blocking): pre-existing `text-token-xs` meaningful copy on admin-privilege/toggle-row descriptions + Pending Changes + ConfirmDialog labels (L865/999/1190-1194/1275-1279) flagged as a follow-up. tester PASS (oxlint EXIT=0 on SCR-042 files, `vite build` clean; no test runner/type-check/Playwright in repo — documented tooling gaps). Figma layout/content transcribed from node 113:20627 text nodes (get_screenshot + get_metadata over 1131 text nodes this session; see `editRole.api.js` FIGMA VERIFICATION header). |
| SCR-043 | Page 1 | `113:22359` | Permission Matrix Screen | `/roles/permissions` | Default matrix | RBAC | DONE | VERIFIED | Role List | Built `PermissionMatrixScreen.jsx` + `usePermissionMatrix.js` (queryKey `['organizations',orgId,'permission-matrix']`, `enabled:!!orgId`, `refetchInterval:30s`) + `permissionMatrix.api.js` (MOD-003 real-GET-first `/organizations/:org/permission-matrix` + content-type guard defending against Vite SPA HTML fallback + `Array.isArray(data.groups/roles)` shape check → `mocked:true` design-sourced baseline of 10 roles × 9 permission groups / 39 permission rows / 390 cells; `MOCK_KPIS` 6 cards, `MATRIX_ROLES`, `PERMISSION_GROUPS`, `PERMISSION_STATES`/`STATE_ORDER`, `ROLE_TYPE_OPTIONS`/`STATE_OPTIONS`/`LEVEL_OPTIONS`/`SOURCE_OPTIONS`, `SAVED_VIEWS`, `buildCellDetail`, `buildMockMatrix`). MOCK BOUNDARY: MOD-003 backend still PLANNED (no Role/Permission/UserRole endpoint); per-cell states DERIVED sample data (deterministic), disclosed honestly. Reconciled the 53-vs-39 count: KPI keeps the design's 53 org-wide permission-node figure, live "Showing N" is computed from the 39 rendered rows (documented in the API FIGMA VERIFICATION header). Route `/roles/permissions` now renders the screen (was RoutePlaceholder shellRoute), registered before `/roles/:id/edit` (literal-before-param); SCR-040 Role List header gained a `<Link to="/roles/permissions">` Permission Matrix action (IconGrid). AppShell breadcrumb ConnectIQ › Administration › Roles › Permission Matrix. Header (title + subtitle, Sample-data badge, live Refresh). Six KPI cards. Matrix panel: saved-view chips (applyView sets the filter set), toolbar (permission search + role search + 5 selects: category/role-type/state/level/source + Differences-only checkbox + Clear Filters), 6-state Legend, sticky-first-column collapsible grouped MatrixTable with per-group coverage bar (coverage recomputed over the visible/filtered role+permission set so % never contradicts the row count), aria-labelled cells (aria-pressed), EmptyState. Detail panel: desktop inline non-modal aside vs mobile focus-trapped modal drawer (role=dialog/aria-modal, Escape/Tab-trap/focus-restore gated on `isModal`, unique title ids per instance). Permission Coverage Insights panel with honest "Across all roles — not affected by column filters" disclosure. Restricted (`!` outline chip) vs Denied (`✕` solid chip) made visually distinct. Loading skeleton; "Sample data" disclosure gated on `mocked`; sr-only `role=status` live region; `role=alert` error+Retry. ui-reviewer PASS (medium/low findings, all fixed): duplicate DOM id (unique mobile/desktop title ids); coverage-vs-filter mismatch (coverage now recomputed against visible set); Administrative view dropping Super Admin (removed level pin, rank>=4 clause governs); desktop focus no-op (focus/trap gated on `isModal`); dead ternary in `buildMockMatrix` removed; Restricted/Denied tones separated; unused `CATEGORY_OPTIONS` export removed; role-column-filter-vs-insights addressed by on-screen disclosure. tester PASS (oxlint clean + `vite build` "✓ built" on new files; no test runner/type-check/Playwright in repo — documented tooling gaps). Figma layout/content transcribed from node 113:22359 text nodes (get_design_context + get_screenshot this session; see `permissionMatrix.api.js` FIGMA VERIFICATION header). |
| SCR-044 | Page 1 | `114:25163` | Access Control Settings Screen | `/settings/access-control` | Default | RBAC | DONE | VERIFIED | Permission Matrix | Built `AccessControlSettingsScreen.jsx` + `useAccessControlSettings.js` (queryKey `['roles','access-control',orgId]`, `enabled:!!orgId`, `staleTime:Infinity`, no refetchInterval/window-focus refetch — a settings form must not clobber in-progress security-policy edits) + `accessControlSettings.api.js` (MOD-003 real-GET-first `/organizations/:org/access-control` + `res.ok`+content-type JSON guard defending against Vite SPA HTML fallback + `data.form` shape check → `mocked:true` design-sourced SAVED baseline for Acme Corporation / ORG-00142; `updateAccessControlSettings` real-PATCH-first → simulated `{id,savedAt,mocked:true}`; exports `ACCESS_CONTROL_OPTIONS`/`AUTH_PROVIDERS`(6)/`MFA_FACTORS`(5)/`COMPLEXITY_REQUIREMENTS`(6)/`COUNTRY_OPTIONS`(12)). MOCK BOUNDARY: MOD-003 backend still PLANNED (no org-level security-policy endpoint). Route `/settings/access-control` now renders the screen (was RoutePlaceholder shellRoute). AppShell breadcrumb ConnectIQ › Administration › Security › Access Control. Settings-form pattern-of-record from SCR-028 OrganizationSettings/SCR-042 EditRole: loads the SAVED baseline (NOT the design's mid-edit dirty state — the frame's amber "Modified"/Pending-Changes rendering is reproduced LIVE; four fields hold pre-edit saved values: `mfaAllUsers` off, `idleTimeout` 60m, `apiTokenExpiration` 180d, Allowed Countries omit Singapore, so nothing is seeded dirty). Header (title + org/last-saved metadata + Reset/Cancel/Save Changes(N) gated on `!dirty||!isValid`). 8 policy sections (Authentication Policies w/ 6 provider toggles + Recommended tags + login-lockout numbers; Multi-Factor Authentication w/ 5 enforcement toggles + 5 factor toggles w/ strength tags + grace/remember selects; Password Policy w/ 6 numeric fields + derived-strength readout + 6-item complexity checklist; Session Management; Network & IP Restrictions w/ read-only IP-range table + allowed-country chips; Access Restrictions; Privileged Access Policies; API & Token Policies). Right rail: Security Overview (live score derived from baseline + high-impact toggles, progressbar), Active Policy Summary, Validation Status (6-item checklist, 5/6 baseline), Compliance Requirements (SOC2/ISO27001/GDPR/PCI/NIST), Security Impact (Low/Med/High derived from pending changes), Pending Changes (live before→after diff). Sticky ActionBar (mirrors header actions). Live dirty-tracking (order-insensitive country set diff + nested provider/factor diff), `validate()` (numeric bounds + ≥1 provider + ≥1 factor + geo/country), `beforeunload` + `window.confirm` guarded in-app nav, focus-trapped Confirm dialog (Escape/Tab-cycle/focus-restore), `<SettingsForm key={data.id}>` remount on load, Toggle (role=switch), simulated-save `role=alert` disclosure, loading skeleton, error+Retry, sr-only live region. ui-reviewer FAIL→fixed→re-PASS: (BLOCKER) invisible primary-button text `text-on-primary`→`text-text-on-primary` (3 buttons); (BLOCKER) invisible toggle knob `bg-on-primary`→`bg-text-on-primary`; (HIGH) dead hover `hover:bg-primary-strong`→`hover:opacity-90`; (HIGH) undefined font-size tokens `text-token-2xs`→`text-token-xs`/`text-token-3xl`→`text-token-xl`; (MED) hardcoded scrim `bg-black/40`→`bg-overlay-scrim`; (MED) ConfirmDialog now captures+restores `document.activeElement`; (LOW) removed inert `shared.isModified &&` guard, unused `baseline` prop, inert `showError('providers')` term. tester PASS (oxlint EXIT=0 on SCR-044 files, `vite build` clean 331 modules; no test runner/type-check/Playwright in repo — documented tooling gaps). Built standalone against the MOD-003 mock boundary despite SCR-043 Permission Matrix now DONE. Figma layout/content transcribed from node 114:25163 text nodes (304 text nodes via get_metadata earlier this session; get_screenshot unavailable — edit-access loss — so pixel fidelity not independently re-pulled by the reviewer; see `accessControlSettings.api.js` FIGMA VERIFICATION header). |
| SCR-045 | Page 1 | `115:26755` | Data Source List Screen | `/data-sources` | Default; nested `SourceDrawer` | Data Sources | DONE | VERIFIED | Auth | Built `DataSourceListScreen.jsx` + `useDataSourceList.js` (queryKey `['organizations',orgId,'data-sources',filters]`, `enabled:!!orgId`, `refetchInterval:30s`, `keepPreviousData`) + `dataSourceList.api.js` (MOD-006 real-GET-first `/organizations/:org/data-sources` + `res.ok`+content-type JSON guard defending against Vite SPA HTML fallback + `Array.isArray(data.rows)` shape check → `mocked:true` design-sourced baseline of 6 KPIs + 14 rows of the 47-total catalogue across DB/warehouse/storage/streaming/API/file connectors; exports `SOURCE_TYPE_OPTIONS`/`STATUS_OPTIONS`/`ENVIRONMENT_OPTIONS`/`AUTH_TYPE_OPTIONS`/`SORT_OPTIONS`/`SAVED_VIEWS`/`MOCK_TITLE`). MOCK BOUNDARY: MOD-006 backend still PLANNED (no DataSource/ConnectionTestResult/SourceHealthRecord endpoint) — disclosed via "Sample data" badge (header + footer + drawer). Route `/data-sources` now renders the screen (was RoutePlaceholder shellRoute); nav item already existed. AppShell breadcrumb ConnectIQ › Data Management › Data Sources. Screen pattern-of-record from SCR-040 RoleList: Header (title + subtitle + Sample-data badge + disabled Export Data Sources / Import Configuration (MOD-006 titles) + Add Data Source Link + live Refresh icon-btn); 6 KPI cards w/ hints + tone bar (grid-cols-2 sm:3 xl:6); Saved Views tablist row (All Sources/Databases/Cloud Warehouses/APIs/Production/Failed Connections(3)/Unused Sources + disabled Save Current View) driving client-side category/env/status/unused predicates; DataSourcesTable (search over name/desc/type/owner/tags + 4 FilterSelects + Sort + disabled Columns + Clear Filters + active-filter chips + page-scoped bulk-select w/ indeterminate ref + bulk-action bar Test Connections/Archive/Export/Assign Tags all disabled w/ MOD-006 titles + semantic table w/ connection-status pills + pagination showing "N of 47" + empty state); focus-trapped `SourceDrawer` (role=dialog/aria-modal, Escape/Tab-cycle/focus-restore via panelRef/closeRef/triggerRef, []-deps focus effect isolated from refetch, sections General Information/Connection Information/Credential Status w/ expiry warning/Operational Status/Usage Summary w/ tags/Technical Metadata, disabled Test Connection/Clone/Archive + Edit Source Link). sr-only `role=status` live region; loading skeleton; `role=alert` error+Retry. Status tones token-only (Connected→success, Warning→warning, Failed→danger, Maintenance→primary wash, Disconnected→muted). VERIFICATION: `vite build` clean (334 modules) + `oxlint` clean on all 3 SCR-045 files (no findings). BLOCKER (documented, non-code): the automatic `ui-reviewer` + `tester` subagent gate could not execute — both terminated on API quota exhaustion (HTTP 403, insufficient account credit), an environmental failure unrelated to the code; build+lint were therefore run directly instead. No test runner/type-check/Playwright in repo (documented tooling gap). Figma layout/content transcribed from node 115:26755 text nodes (get_screenshot + get_metadata over 214 text nodes this session; see `dataSourceList.api.js` FIGMA VERIFICATION header). |
| SCR-046 | Page 1 | `115:28040` | Add Data Source Screen | `/data-sources/new` | Default connector-picker | Data Sources | DONE | VERIFIED | Data Source List | Built `AddDataSourceScreen.jsx` + `addDataSource.api.js` (MOD-006 real-request-first: `testConnection` POST `/organizations/:org/data-sources/test-connection` + `createDataSource` POST `/organizations/:org/data-sources`, each with `res.ok`+content-type JSON guard defending against the Vite SPA HTML fallback → `mocked:true` simulated result; `DataSourceError` class). MOCK BOUNDARY: MOD-006 backend still PLANNED (no DataSource/ConnectionTestResult/SourceHealthRecord endpoint) — disclosed via `role=alert` banner + "Sample data"/"Sample" badges. Route `/data-sources/new` now renders the screen (was absent), registered before any `/data-sources/:id` param routes; SCR-045 Data Source List "Add Data Source" already `<Link to="/data-sources/new">`. AppShell breadcrumb ConnectIQ › Data › Data Sources › Add Data Source. Form pattern-of-record from SCR-033 AddUser (shared Section/Field/TextInput/SelectInput/TextArea/Toggle/Header/ActionBar/ConfirmDialog primitives, focus-trap, sr-only `role=status` live region, mocked banner). 8 sections: (1) General Information (name required, environment/category selects, description textarea, connector-picker grid grouped Relational Databases/Cloud Warehouses/Cloud Storage/APIs·SaaS·Streaming — 17 connectors, `aria-pressed`); (2) Connection Configuration — connector-KIND-aware field set (database/warehouse→host+port+database, storage→bucket+region, api→endpointUrl, streaming→bootstrapServers, file→host+port) w/ SSL toggle + live "Test Connection" action rendering a result panel (Network/Auth/SSL checks + server/latency/permissions, "Sample data" badge when mocked); (3) Authentication — method select drives which credential fields render (`AUTH_FIELD_MAP`: Username&Password/OAuth2/API Key/Service Account/IAM Role/SSL Certificate), password-type inputs; (4) Security Configuration (encrypt-in-transit/at-rest/verify-cert toggles + min-TLS/CA selects + IP allowlist); (5) Schema Discovery — auto-discover toggle + collapsible schema tree (analytics_db public/analytics/reporting/staging, per-table col·row meta, indeterminate schema checkboxes) w/ live selected-tables/columns/estimated-volume computed (not hardcoded), gated "test the connection" hint; (6) Operational Configuration (pool size/query timeout numerics, sync frequency/retry policy selects, read-only toggle); (7) Metadata & Governance (owner/department/classification/sensitivity/lifecycle/business-domain selects + comma tags); (8) Monitoring & Alerts (health-check/failure/latency toggles + threshold + notification-channel checkboxes, unconfigured channels disabled). Right rail: Data Source Summary, Connection Status (idle/testing/connected/failed), Validation Status (6-item checklist w/ progressbar — "Schema Discovery Complete" satisfied once a connection tests OK), Security Review, Pending Configuration (live). Connector-aware `validate()` (name/host/port 1–65535/database/bucket/endpoint-URL/bootstrap-servers/auth-credentials/latency-threshold), focus-trapped Confirm Data Source dialog (Escape/Tab-cycle/focus-restore); a mocked create shows the disclosure banner and does NOT navigate, a real success routes to `/data-sources`; Save Draft disabled w/ MOD-006 title. VERIFICATION: `vite build` clean (336 modules). ui-reviewer + tester gate running. Figma layout/content transcribed from node 115:28040 (get_screenshot + get_metadata over 664 named nodes this session; see `addDataSource.api.js` FIGMA VERIFICATION header). |
| SCR-047 | Page 1 | `115:29580` | Source Connection Setup Screen | `/data-sources/new/connection` | Default | Data Sources | DONE | DONE | Add Data Source |
| SCR-048 | Page 1 | `116:31212` | Database Connector Setup Scren *(Figma name has a typo: "Scren")* | `/data-sources/new/database` | Default (MySQL/PostgreSQL/MongoDB connector form) | Data Sources | DONE | VERIFIED | Source Connection Setup | Built `DatabaseConnectorSetupScreen.jsx` + `databaseConnector.api.js` (MOD-006 real-request-first: `testDatabaseConnection` POST `/organizations/:org/data-sources/database/test-connection` + `createDatabaseSource` POST `/organizations/:org/data-sources/database`, each with `res.ok`+content-type JSON guard defending against the Vite SPA HTML fallback → `mocked:true` simulated result; `DatabaseConnectorError` class). MOCK BOUNDARY: MOD-006 backend still PLANNED (no DataSource/DB-connector-config/test-connection/SourceHealthRecord endpoint) — disclosed via `role=alert` banner + "Sample data"/"Sample" badges. Route `/data-sources/new/database` now renders the screen (was absent), registered between `/data-sources/new/connection` and `/data-sources/new/api` (literal-before-param). Upstream wiring: SCR-047 `sourceConnectionSetup.api.js` gained `LIVE_METHOD_ROUTES` ({database,api}) and `resolveContinueTarget` now routes the Database/API methods straight to their dedicated screens (`live:true`); SourceConnectionSetupScreen "Next step" copy branches on `target.live`. AppShell breadcrumb ConnectIQ › Data › Data Sources › Database Connector. Form pattern-of-record from SCR-046 AddDataSource (shared Section/Field/TextInput/SelectInput/Toggle/Header/ActionBar/ConfirmDialog primitives, focus-trap, sr-only `role=status` live region, mocked banner). Engine-`kind`-aware (relational vs document): (1) General Information (name required + `<fieldset>`/`<legend>` engine radiogroup — postgresql/mysql/mongodb — WAI-ARIA roving-tabindex w/ Arrow-key move-and-focus + `aria-required`, environment select); (2) Connection Configuration — document engine offers a connection-string/URI toggle (`MONGO_URI_PATTERN`) vs discrete host(`HOSTNAME_PATTERN`)+port+database, plus mongo replicaSet/authSource/readPreference; live "Test Connection" action rendering a result panel (checks + server version, "Sample data" badge when mocked); (3) Authentication — method select drives credential fields (`DB_AUTH_FIELD_MAP`: Username&Password/IAM Role/Client Certificate), password-type inputs; (4) Security & Options (SSL/verify-cert/read-only toggles + SSL-Mode select disabled when SSL off + connection-timeout + max-pool-size numerics). Right rail: Connection Summary, Connection Status (idle/testing/connected/failed), Validation Status (5-item checklist w/ progressbar — "Connection Verified" gated on a successful test), Pending Configuration (live). Any connection-shape change invalidates a prior test. Engine-aware `validate()` (name/host/port 1–65535/database or URI/timeout+pool positivity), focus-trapped Confirm dialog (Escape/Tab-cycle/focus-restore + unverified-connection warning); a mocked create shows the disclosure banner and does NOT navigate, a real success routes to `/data-sources`. VERIFICATION: `vite build` clean + `oxlint` EXIT=0 on changed files. ui-reviewer PASS (2 medium + low findings, all fixed in-scope): (MED) engine radiogroup now moves focus to the newly-selected radio on Arrow-key nav (mirrors SCR-047 via `radiogroupRef` + `data-engine-id`); (MED) engine radiogroup converted to `<fieldset>`/`<legend>` + `aria-required` (mirrors SCR-046); (LOW) SSL Mode select now `disabled`+hint when SSL toggled off. tester PASS (oxlint EXIT=0 + `vite build` clean on changed files; no test runner/type-check/Playwright in repo — documented tooling gap). Figma node 116:31212 could NOT be inspected (Figma MCP server "Connected" but its read tools not exposed in the session tool set) — content transcribed from the sibling SCR-046/SCR-049 connector flows + MOD-006 module spec + the SCR-047 method catalogue; pixel fidelity `unverified` pending Figma MCP availability (SCR-044/SCR-049 precedent; see `databaseConnector.api.js` FIGMA VERIFICATION header). |
| SCR-049 | Page 1 | `116:33009` | API Connector Setup Screen | `/data-sources/new/api` | Default | Data Sources | DONE | VERIFIED | Source Connection Setup | Built `ApiConnectorSetupScreen.jsx` + `apiConnectorSetup.api.js` (MOD-006 real-request-first: `sendTestRequest` POST `/organizations/:org/data-sources/api/test-request` + `createApiConnector` POST `/organizations/:org/data-sources/api`, each with `res.ok`+content-type JSON guard defending against the Vite SPA HTML fallback → `mocked:true` simulated result; `ApiConnectorError` class). MOCK BOUNDARY: MOD-006 backend still PLANNED (no DataSource/REST-connector-config/test-request/SourceHealthRecord endpoint) — disclosed via `role=alert` banner + "Sample data"/"Sample" badges. Route `/data-sources/new/api` now renders the screen (was absent), registered after `/data-sources/new/connection`. AppShell breadcrumb ConnectIQ › Data › Data Sources › API Connector Setup. Form pattern-of-record from SCR-046 AddDataSource (shared Section/Field/TextInput/SelectInput/TextArea/Toggle/KeyValueEditor/Header/ActionBar/ConfirmDialog primitives, focus-trap, sr-only `role=status` live region, mocked banner). 7 sections: (1) General Information (name required, environment/category selects, description); (2) Request Configuration — base URL (http(s) validated) + method + response format + resource path + editable Headers/Query-Params `KeyValueEditor`s + live "Send Test Request" action rendering a result panel (DNS/TLS/Auth/Response/Rate-limit checks + latency/records/total/rate-left + pretty-printed JSON body, "Sample data" badge when mocked); (3) Authentication — method select drives which credential fields render (`API_AUTH_FIELD_MAP`: None/API Key(name+value+location)/Bearer Token/Basic Auth/OAuth 2.0), password-type inputs, API-key location select; (4) Pagination — strategy radiogroup (none/offset/page/cursor/link) w/ roving-focus keyboard nav (Arrow/Home/End, one Tab stop) driving strategy-aware param fields + page-size (1–10000); (5) Rate Limiting & Reliability (requests/min + concurrency + timeout 1–600 + retry policy + Respect-Retry-After toggle); (6) Response Mapping — record path w/ test-detected datalist candidates + primary-key field + incremental-sync toggle w/ cursor field; (7) Metadata & Governance (owner/classification selects + tags + verify-TLS toggle). Right rail: Connector Summary (host parsed via `new URL`), Request Status, Validation Status (7-item checklist w/ progressbar — "Test Request Succeeded"/"Response Mapped" gated on a successful test), Pending Configuration (live). Any request-shape change (URL/auth/pagination/headers/query-params) invalidates a prior test result. Connector `validate()` (name/base-URL/auth-credentials/page-size/requests-per-minute/timeout/record-path/incremental-field), focus-trapped Confirm API Connector dialog (Escape/Tab-cycle/focus-restore); a mocked create shows the disclosure banner and does NOT navigate, a real success routes to `/data-sources`; Save Draft disabled w/ MOD-006 title. VERIFICATION: `vite build` clean (342 modules) + `oxlint` EXIT=0 on both new files. ui-reviewer PASS (no blockers; 1 major + 3 minor) — in-scope fixes applied: (MAJOR) Pagination radiogroup now WAI-ARIA-conformant (roving `tabIndex` + Arrow/Home/End key nav + move-and-select, matching the SCR-046 connector radiogroup); (MINOR) header/query-param mutations now route through the same test-invalidation as other request-shape edits (stale "Test Request Succeeded" no longer possible); (MINOR) `KeyValueEditor` accessible names cleaned up via a `fieldLabel` prop ("Header 1 name"/"Header 1 value"/"Remove header 1" instead of "Add header name 1"). Deferred (non-blocking): Cancel routes to `/data-sources/new` (Add Data Source) while the breadcrumb parent is the list — intentional back-to-picker behavior, matches SCR-046. tester PASS (oxlint EXIT=0 on both files + full project, `vite build` EXIT=0; no test runner/type-check/Playwright in repo — documented tooling gap). Figma node 116:33009 could NOT be inspected (Figma MCP server "Connected" but its tools not exposed in the session tool set) — content transcribed from the sibling SCR-046 REST/API connector flow + MOD-006 module spec; pixel fidelity `unverified` pending Figma MCP availability (SCR-044 precedent; see `apiConnectorSetup.api.js` FIGMA VERIFICATION header). |
| SCR-050 | Page 1 | `117:35049` | CSV Upload Screen | `/data-sources/new/csv` | Default; "Skip Empty Rows" / "Treat Empty as Null" options | Data Sources | DONE | VERIFIED | Source Connection Setup | Built `CsvUploadScreen.jsx` + `csvUpload.api.js` (MOD-006 real-request-first: `uploadCsvFile` POST `/organizations/:org/data-sources/csv/upload` + `startCsvImport` POST `/organizations/:org/data-sources/csv/import`, each with `res.ok`+content-type JSON guard defending against the Vite SPA HTML fallback → `mocked:true` simulated result; `CsvUploadError` class). MOCK BOUNDARY: MOD-006 backend still PLANNED (no file-upload/CSV-parse/schema-detection/data-quality/import endpoint) — DESIGN-SOURCED auto-detected file profile, column schema, preview rows, and data-quality figures transcribed from the frame's text nodes; disclosed via top `role=note` banner + per-section "Sample data" badges. Route `/data-sources/new/csv` now renders the screen (was absent), registered between `/api` and `/csv`→`/excel` (literal ordering). Upstream wiring: SCR-047 `sourceConnectionSetup.api.js` `LIVE_METHOD_ROUTES` gained `csv` so `resolveContinueTarget` routes the CSV method straight here (`live:true`). AppShell breadcrumb ConnectIQ › Data › Data Sources › CSV Upload. Form pattern-of-record from SCR-048 DatabaseConnector (shared Section w/ numbered `index` badge/FieldGrid/Field/TextInput/SelectInput/Toggle/RadioCards/ConfirmDialog primitives, focus-trap, sr-only `role=status` live region, mocked banner). Boots pre-populated with the design's already-uploaded `q4_sales_report_2024.csv` (12.4 MB, 284,192 rows, 22 cols, `fileMocked:true`); a real `uploadCsvFile` replaces it + derives the table name. 9 sections: (1) File Upload (drag/drop + file card w/ remove, `UPLOAD_LIMITS` accept `.csv,.csv.gz`); (2) File Information (12 auto-detected rows — encoding/delimiter/quote/escape/line-ending/header/rows/cols/null-rep); (3) Parsing Configuration (delimiter/encoding/header-row/quote/escape/date-format/decimal/thousands selects + skipEmptyRows/trimWhitespace/skipInvalidRows/treatEmptyAsNull toggles); (4) Schema Detection (12 columns, per-column client-side type-override select + nullable toggle, Detected/Review status; "+10 more" note to TOTAL_DETECTED_COLUMNS=22); (5) Data Preview (11 cols × 5 sample rows, "Showing N of 284,192"); (6) Data Quality Validation (6 metric tiles + 9-check table w/ Passed/Warning status + Configure/Review actions carrying `aria-label`); (7) Destination Configuration (target data source/schema/table-name identifier-validated/dataset + write-mode RadioCards + batch/commit/error-threshold numerics + duplicate-strategy RadioCards + priority/scheduling); (8) Metadata & Governance (owner/domain/department/classification/sensitivity/retention selects + tags + compliance-framework checkboxes + notes); (9) — review/import action bar. Right rail: Upload Summary, Data Quality Overview, Import Readiness (live checklist — "validated" done only when warnings===0), Validation Status, Import Estimate. `validate()` (file+targetDataSource+targetSchema+tableName identifier `/^[A-Za-z_][A-Za-z0-9_]*$/`+owner required; positive batch/commit, non-negative error-threshold); focus-trapped Confirm Import dialog (Escape/Tab-cycle/focus-restore + derived warning-count copy from `QUALITY_CHECKS.filter(status==='Warning')`); a mocked import shows the disclosure banner and does NOT navigate, a real success routes to `/data-sources`. VERIFICATION: `vite build` clean + `oxlint` EXIT=0 on both new files. ui-reviewer FAIL→fixed (1 MAJOR + minors, all in-scope): (MAJOR) ConfirmDialog warning-count contradiction (sidebar said 3, dialog hardcoded "2 duplicate+missing") → now derives `warningChecks`/`warningLabels` from `QUALITY_CHECKS`; (MINOR) preview count "Showing 100" → `rows.length`, removed unused `PREVIEW_SHOWN_ROWS` import; (MINOR) quality action buttons `aria-label`; (MINOR) schema name cell `<td>`→`<th scope="row">`; (MINOR) removed leftover `/* SECTIONS_PLACEHOLDER */`. Deferred (non-blocking): initial Import Readiness 50%/In Progress vs design's "Mostly Ready" — intentional (targetDataSource requires selection; reviewer called it defensible UX). tester PASS (`npx oxlint` EXIT=0 on new files + full project 0 errors, `vite build` EXIT=0; no test runner/type-check/Playwright in repo — documented tooling gap). Figma node 117:35049 inspected via get_screenshot + get_metadata (~470 text nodes read in order); pixel fidelity `verified` for structure/section-order/content (see `csvUpload.api.js` FIGMA VERIFICATION header). |
| SCR-051 | Page 1 | `119:37214` | Excel Upload Screen | `/data-sources/new/excel` | Default; "Skip Empty Rows", "Treat Empty Cells as Null", "Empty cell", "Empty Worksheets" options | Data Sources | DONE | VERIFIED | Source Connection Setup | Built `ExcelUploadScreen.jsx` + `excelUpload.api.js` (MOD-006 real-request-first: `importWorkbook` POST `/organizations/:org/data-sources/excel/import` with `res.ok`+content-type JSON guard defending against the Vite SPA HTML fallback → `mocked:true` simulated result; `ExcelUploadError` class). MOCK BOUNDARY: MOD-006 backend still PLANNED (no workbook-upload/worksheet-introspection/schema-detection/quality-validation/import endpoint) — DESIGN-SOURCED workbook profile (`FY2024_Financial_Report.xlsx`), worksheet catalogue, detected schema, preview rows, and quality figures transcribed from the frame's text nodes; disclosed via top `role=note` "Sample workbook / No backend" banner + `role=alert` simulated-import banner. Route `/data-sources/new/excel` now renders the screen (was absent), registered between `/csv` and `/ftp` (literal ordering). Upstream wiring: SCR-047 `sourceConnectionSetup.api.js` `LIVE_METHOD_ROUTES` gained `excel` so `resolveContinueTarget` routes the Excel method straight here (`live:true`). AppShell breadcrumb ConnectIQ › Data › Data Sources › Excel Upload. Form pattern-of-record from SCR-048/050 (shared Section w/ numbered badge/Field/TextInput/SelectInput/Toggle/RadioCardGroup/StatusPill/Header/ActionBar/ConfirmDialog primitives, focus-trap, sr-only `role=status` live region, mocked banner). 10 sections: (1) Workbook Upload (sample notice + uploaded-file card w/ progressbar + dropzone + `WORKBOOK_LIMITS` strip); (2) Workbook Information (12 auto-detected rows w/ ok/warn icons); (3) Worksheet Selection (searchable checkbox table, 7 sheets w/ selectable/empty/hidden StatusPills, Select-All/Clear, live totals); (4) Parsing Configuration (header-row/starting-row/encoding/date/decimal/thousands selects + formula-handling RadioCardGroup + 5 parse toggles); (5) Schema Detection (worksheet selector + 12/18-column table w/ confidence bars + Show-all toggle); (6) Workbook Preview (worksheet selector + 9-col × 5-row sample, cell legend); (7) Data Quality Validation (per-worksheet rollup + 10-check grid + acknowledge-warnings toggle gating readiness); (8) Destination Configuration (target data source/schema selects + import-strategy RadioCardGroup + live worksheet→table mapping `staging.<sheet>_fy2024`); (9) Import Configuration (batch-size/commit-interval/error-threshold numerics + duplicate-strategy/priority/scheduling selects); (10) Metadata & Governance (owner/domain/department/classification/sensitivity/retention selects + compliance-framework toggles + tag editor + notes). Right rail: Workbook Summary, Validation Status (6-item checklist w/ progressbar), Worksheet Information, Data Quality Overview, Import Readiness. Computed helpers `buildWorksheetMapping`/`computeImportEstimate`/`computeChecklist` derive mapping/estimate/readiness from live selection. `validate()` (≥1 worksheet + targetDataSource + targetSchema + positive batch/commit + non-negative error-threshold + owner); focus-trapped Confirm Import dialog (Escape/Tab-cycle/focus-restore, defaults focus to Cancel); a mocked import shows the disclosure banner and does NOT navigate, a real success routes to `/data-sources`. VERIFICATION: `vite build` clean + `oxlint` EXIT=0 on changed files. ui-reviewer FAIL→fixed (1 HIGH + mediums/lows, all in-scope): (HIGH) initial `targetDataSource:''` made Validation Status render 4/6 contradicting the frame's 5/6 → seeded `'Analytics Warehouse (PostgreSQL)'`; (MED) modal scrim `bg-black/40` → `bg-overlay-scrim` token (matches SCR-048/050); (MED) `SheetTabs` had `role=tablist`/`tab` without arrow-key nav/`aria-controls`/`tabpanel` → converted to labeled `role=group` toggle buttons w/ `aria-pressed` (one strip drives two panels off one shared `activeSheet`); (MED) submit-focus for `errors.worksheets` had no target → routes to `worksheet-search`; (LOW) submitted-state "Back to Data Sources" now navigates `/data-sources`; (LOW) mock Replace/Remove/browse controls now `disabled`+title; (LOW) ConfirmDialog now defaults focus to Cancel (non-committing) per sibling convention. tester PASS (`npx oxlint` EXIT=0 on changed files + full project, `vite build` EXIT=0; no test runner/type-check in repo — documented tooling gap). Figma node 119:37214 inspected via get_screenshot + get_metadata (5407px frame, all 10 sections + 5 right-rail panels read from text nodes); pixel fidelity `verified` for structure/section-order/content (see `excelUpload.api.js` FIGMA VERIFICATION header). |
| SCR-052 | Page 1 | `121:39854` | FTP Connection Screen | `/data-sources/new/ftp` | Default | Data Sources | DONE | DONE | Source Connection Setup |
| SCR-053 | Page 1 | `122:41785` | SFTP Connection Screen | `/data-sources/new/sftp` | Default | Data Sources | DONE | DONE | Source Connection Setup | Built `SftpConnectionScreen.jsx` + `sftpConnection.api.js` against the MOD-006 mock boundary (backend still PLANNED). SFTP-only screen (no protocol picker — transport is a fixed always-encrypted SSH-2.0 channel via `SFTP_TRANSPORT`); surfaces SSH-specific concerns the generic FTP screen does not: host-key verification / known-hosts policy, a negotiated cipher-suite table (`NEGOTIATED_SUITE`, 7 rows KEX/HostKey/Cipher C→S+S→C/MAC C→S+S→C/Compression w/ policy+grade), key-source (Enterprise Vault), and SSH session diagnostics. `testSftpConnection` / `browseSftpDirectories` / `createSftpSource` are each real-request-first (POST `/organizations/:org/data-sources/sftp[/test-connection|/browse]`) with the standard `res.ok`+content-type JSON guard (Vite SPA HTML-fallback defense) → `mocked:true` simulated result; `SftpConnectionError` class. MOCK BOUNDARY disclosed via `role=alert` simulated-source banner + "Sample data" badges (SSH Security suite, Directory Discovery, Pattern Match Preview) + `sr-only role=status` live region. Auth-method-driven credential fields (`SFTP_AUTH_METHODS` / `SFTP_AUTH_FIELD_MAP` — Username & Password / SSH Private Key / SSH Key + Passphrase / Public-Private Pair) in a `role=radiogroup` (roving-tabindex + Arrow/Home/End); key-using methods show Key Algorithm + Key Format + Key Source selects (ordered together above credentials). 9 sections: (1) Connection Configuration; (2) Server Configuration (host/port 22 + SSH options + live Test Connection panel w/ ConnectionHealth failed-state); (3) Authentication; (4) SSH Security (negotiated-suite table w/ conditional status pill + "Preferred/Negotiated" heading pre-test); (5) Directory Discovery (simulated remote tree browse); (6) File Discovery Rules (glob + Pattern Match Preview w/ "Sample data" badge); (7) Transfer Configuration (transfer-direction radiogroup + overwrite/encoding); (8) Scheduling & Automation (trigger-type radiogroup + cron hint); (9) Monitoring & Audit (10 toggles). Right rail: Connection Summary / Connection Status / Validation checklist (`role=progressbar`; write-permission "not required" for Download Only; `computeChecklist(form, testState)` consumes reported test checks as source of truth) / SSH Security Review (conditional grade/cipher labels) / Pending Configuration. Upstream wiring: SCR-047 `sourceConnectionSetup.api.js` `LIVE_METHOD_ROUTES` gained `sftp: '/data-sources/new/sftp'` so `resolveContinueTarget('sftp')` routes the SFTP method straight here (`live:true`); route registered in `router.jsx`. AppShell breadcrumb ConnectIQ › Data › Data Sources › SFTP Connection. Cancel branches on submitted-state (`/data-sources` when submitted, else `/data-sources/new/connection`); focus-trapped Confirm Create dialog (Escape/Tab-cycle/focus-restore/backdrop-close). VERIFICATION: `npx oxlint` EXIT=0 on both new files + `npx vite build` EXIT=0. **Figma node `122:41785` pixel fidelity `verified`** — `get_metadata`/`get_design_context`/`get_screenshot` all succeeded this build session; content transcribed from that inspection (default GlobalBank example etl-sftp-globalbank → sftp.globalbank-exchange.com:22). Note: the ui-reviewer subagent could NOT call Figma MCP in its own pass so its fidelity verdict was reviewer-unverified, but the main session inspected the node directly. See `sftpConnection.api.js` FIGMA VERIFICATION header + review-log. |
| SCR-054 | Page 1 | `123:43915` | Webhook Configuration Screen | `/data-sources/new/webhook` | Default | Data Sources | DONE | DONE | Source Connection Setup | Built `WebhookConfigurationScreen.jsx` + `webhookConfiguration.api.js` (MOD-006 real-request-first: `generateEndpoint` / `testWebhook` / `createWebhookSource` each POST `/organizations/:org/data-sources/webhook[/action]` with `res.ok`+content-type JSON guard defending against the Vite SPA HTML fallback → `mocked:true` simulated result; `WebhookError` class). MOCK BOUNDARY: MOD-006 backend still PLANNED (no endpoint-provisioning/test-delivery/source-create endpoint) — DESIGN-SOURCED auth taxonomy, event catalogue, retry schedule, sample payload, and health metrics transcribed from the frame's text nodes; disclosed via top-level `role=alert` simulated-source banner + "Sample"/"Sample data" badges on the endpoint panel and validation result. Route `/data-sources/new/webhook` now renders the screen (was absent), registered after `/sftp` (literal ordering). Upstream wiring: SCR-047 `sourceConnectionSetup.api.js` `LIVE_METHOD_ROUTES` gained `webhook` so `resolveContinueTarget` routes the Webhook method straight here (`live:true`). AppShell breadcrumb ConnectIQ › Data › Data Sources › Webhook Configuration. Form pattern-of-record from SCR-052 FTP (shared Section w/ numbered `index` badge/Field/TextInput/TextArea/SelectInput/Toggle/SegmentedControl/Header/ActionBar/ConfirmDialog primitives, focus-trap, sr-only `role=status` live region, mocked banner). SECURITY: Vault-backed signing secret is ALWAYS masked (`whsec_••••••••••••••••`) — `INITIAL_FORM.secretProvisioned:false`, `buildPayload` transmits only the `secretProvisioned` boolean (never the secret), `generateEndpoint` returns a masked `secretRef` only, `testWebhook`/`createWebhookSource` return no secret, and even Reveal shows a masked reference note (never clear text). 9 sections: (1) Webhook Configuration (name identifier-validated/environment/version/status/organization); (2) Endpoint Configuration (Generate/Regenerate + Copy, endpoint panel w/ Sample badge, HTTP method/content-type/max-payload/timeout/enforce-HTTPS); (3) Authentication & Security (8-method `role=radiogroup` w/ roving-tabindex + Arrow/Home/End, HMAC algo/signature-header/prefix + MASKED secret field w/ Reveal/Rotate, IP-allowlist CRUD, security toggles); (4) Event Subscription (search + accessible category filter, Select-All/Clear, `<fieldset>`/`<legend>` checkbox rows w/ `data-event-id`, live subscribed/est-volume/categories tiles derived from subscribed categories' `volume`); (5) Payload Validation (validation-mode + JSON Schema editor w/ JSON-parse check); (6) Event Processing (queue-mode/priority/DLQ metrics tiles); (7) Retry Policy (strategy/jitter + backoff schedule table); (8) Monitoring & Audit (payload-logging toggle w/ PII `warning` copy, alert email/Slack); (9) Sample Payload (`role=tablist` Headers/Body/Validation, "Generate Example & Test" gated on `endpointReady`). Right rail: Webhook Summary, Validation Status (`role=progressbar`), Security Review, Event Overview, Webhook Health (submit). `validate()` (name `/^[a-z0-9][a-z0-9-]*$/`+environment+organization required; HMAC requires signatureHeader+secretProvisioned; integer minimums for payload/timeout/rate-limit/tolerance/expiration/depth/retries/interval; ≥1 event; JSON-parse for Schema mode); focus-trapped Confirm Create dialog (Escape/Tab-cycle/focus-restore); submit-error focus routing incl. `webhookSecret`→Generate-Endpoint fallback; a mocked create shows the disclosure banner and does NOT navigate, a real success routes to `/data-sources`. VERIFICATION: `npm run build` clean + `oxlint` EXIT=0 on both new files. ui-reviewer PASS (with fixes, all in-scope): (MED) category-filter `<select>` had no accessible name + exposed raw ids → added `ariaLabel="Filter by category"` + `{value,label}` options rendering human category names; (MED) Section 9 test button fired with no endpoint → gated on `endpointReady` (matches header "Send Test Event"); (LOW) checkbox `accent-[var(--color-primary,#4f46e5)]` mismatched brand → `accent-primary` utility; (LOW) masked-secret error associated to non-focusable `<span>` via label `htmlFor` → `Field labelAsText` renders a plain `<span>` label (no dead label click target; submit already routes focus to Generate); (LOW) hardcoded "~2,900/hr" est-volume → derived from subscribed categories' seeded `volume` in both the section tiles and sidebar Event Overview so it moves as events toggle. tester PASS (`npx oxlint` EXIT=0 on new files + full project, `npm run build` EXIT=0; only pre-existing unrelated warnings in SplashScreen/AppShell; no test runner/type-check/Playwright in repo — documented tooling gap). Figma node 123:43915: content transcribed from the frame's text nodes and built against the SCR-052 FTP pattern-of-record; pixel fidelity `not-verified` this pass (Figma MCP read tools unavailable in-session — see `webhookConfiguration.api.js` FIGMA VERIFICATION header; re-run fidelity check with Figma MCP access before final sign-off). |
| SCR-055 | Page 1 | `124:45976` | Connection Test Result Screen | `/data-sources/new/test-result` | Default success/fail result | Data Sources | DONE | VERIFIED | any Source Connection Setup screen | Built `ConnectionTestResultScreen.jsx` + `connectionTestResult.api.js` + `useConnectionTestResult.js` against the MOD-006 mock boundary (backend still PLANNED: no `ConnectionTestResult` telemetry endpoint). `fetchConnectionTestResult(orgId, connectionId)` is real-GET-first (`/organizations/{orgId}/data-sources/{connectionId}/test-result`) with standard `res.ok`+content-type JSON guard (Vite SPA HTML-fallback defense) + shape check (`Array.isArray(data.checklist)`) → `{ ...DEFAULT_RESULT, mocked:true }` design-sourced baseline for Snowflake DW (Prod-Analytics-Warehouse / conn_sf_9a8b7c6d / xy12345.us-east-1.snowflakecomputing.com:443); exports `STATUS_TONES`, `CHECKLIST_STEPS`, `CONNECTION_INFO_METADATA`, `PERFORMANCE_METRICS`, `LATENCY_BREAKDOWN`, `SECURITY_VALIDATION_ITEMS`, `PERMISSION_CHECKS`, `DIAGNOSTIC_LOGS`, `HISTORICAL_TESTS`, `RECOMMENDATIONS`, `ConnectionTestResultError`. MOCK BOUNDARY disclosed via top `role=note` "Sample data" banner + `role=status aria-live=polite` region. Data via `useConnectionTestResult` (React Query v5, `queryKey ['organizations',orgId,'data-sources',connectionId,'test-result']`, `staleTime: 60_000`). Layout: ResultBanner (overall status Successful, score 94/100, 13/13 checks, 842ms latency) + Header (Copy Diagnostics / Export Report / View Configuration / Retry Test / Save Connection) → 2-col `xl:grid-cols-[minmax(0,1fr)_340px]` (main: (1) Validation Checklist w/ 13 check items + duration & category pills; (2) Connection Information metadata grid w/ masked credentials; (3) Performance Metrics cards + Latency Breakdown progress bars; (4) Security Validation table w/ 12 checks + TLS 1.3 / AES-256 / SHA-256 tags; (5) Permission Verification grid w/ 12 permissions [Read/Write/DDL/Admin] + active state pills; (6) Detailed Diagnostics table w/ search + log-level filter [All/INFO/WARN/DEBUG/TRACE] + Copy Log action; (7) Validation History table w/ search + status filter [All/Successful/Warning/Failed] + View action; sidebar: (1) Test Summary; (2) Validation Score radial gauge 94/100; (3) Connection Health radar; (4) Security Review; (5) Recommendations cards w/ Apply/Review actions; (6) Save Connection CTA card). Full accessibility: WAI-ARIA tables w/ `th scope="col"`, `role="progressbar"`, live filter/search counts, focus rings, token-only WCAG-AA contrast (`text-warning-strong`, `bg-warning-bg`, `text-success`, `text-danger-strong`). Figma node `124:45976` pixel fidelity `verified` — `get_metadata` (520 named nodes) + `get_screenshot` both succeeded this build session, so all 13 checklist steps, 14 connection metadata rows, 6 performance metrics, 7 latency bars, 12 security items, 12 permission checks, 18 diagnostics items, 7 historical runs, and 5 recommendation cards are transcribed from that inspection. |
| SCR-056 | Page 1 | `125:47806` | Source Health Monitoring Screen | `/data-sources/:id/health` | Default | Data Sources | DONE | DONE | Data Source List |
| SCR-057 | Page 1 | `129:656` | Destination List Screen | `/destinations` | Default | Destinations | DISCOVERED | DISCOVERED | Auth |
| SCR-058 | Page 1 | `130:1736` | Add Destination Screen | `/destinations/new` | Default; contains a fixed/modal sub-frame (`130:2100`) | Destinations | DISCOVERED | DISCOVERED | Destination List |
| SCR-059 | Page 1 | `130:2662` | Destination Connection Test Screen | `/destinations/new/test` | Default | Destinations | DISCOVERED | DISCOVERED | Add Destination |
| SCR-060 | Page 1 | `130:3610` | Destination Configuration Screen | `/destinations/:id/configure` | Default | Destinations | DISCOVERED | DISCOVERED | Destination List |
| SCR-061 | Page 1 | `131:105` | Destination Health Screen | `/destinations/:id/health` | Default; sticky sub-section (`131:1068`) | Destinations | DISCOVERED | DISCOVERED | Destination List |
| SCR-062 | Page 1 | `133:2160` | Destination History Screen | `/destinations/:id/history` | Default; multiple analytics/KPI sections | Destinations | DISCOVERED | DISCOVERED | Destination List |
| SCR-063 | Page 1 | `146:6124` | Pipline List Screen *(Figma name has a typo: "Pipline")* | `/pipelines` | Default | Pipelines | DISCOVERED | DISCOVERED | Auth |
| SCR-064 | Page 1 | `146:7807` | Create Pipeline Screen | `/pipelines/new` | Default (React-root style container `146:7808`, suggests this frame documents an embedded app root rather than static content) | Pipelines | DISCOVERED | DISCOVERED | Pipeline List |
| SCR-065 | Page 1 | `150:8519` | Visual Pipeline Builder Screen | `/pipelines/:id/builder` | Default canvas/header layout | Pipelines | DISCOVERED | DISCOVERED | Create Pipeline |
| SCR-066 | Page 1 | `151:9778` | Node Library Screen | builder side-panel | Default | Pipelines | DISCOVERED | DISCOVERED | Visual Pipeline Builder |
| SCR-067 | Page 1 | `155:2137` | Source Node Configuration Screem *(Figma name has a typo: "Screem")* | pipeline builder node panel | Default | Pipelines | DISCOVERED | DISCOVERED | Visual Pipeline Builder |
| SCR-068 | Page 1 | `157:3693` | Filter Node Configuration Screen | pipeline builder node panel | Default; sticky footer (`157:4453`) | Pipelines | DISCOVERED | DISCOVERED | Visual Pipeline Builder |
| SCR-069 | Page 1 | `170:1542` | Mapping Node Configuration Screen | pipeline builder node panel | Default; header+footer chrome | Pipelines | DISCOVERED | DISCOVERED | Visual Pipeline Builder |
| SCR-070 | Page 1 | `220:5908` | Transformation Node Configuration Screen | pipeline builder node panel | Default | Pipelines | DISCOVERED | DISCOVERED | Visual Pipeline Builder |
| SCR-071 | Page 1 | `221:7477` | Validation Node Configuration Screen | pipeline builder node panel | Default; footer actions "Cancel / Save Draft / Validate / Test Validation / Apply Configuration"; sample node ID `val_node_0052` | Pipelines | DISCOVERED | DISCOVERED | Visual Pipeline Builder |

### Shared / reusable component frames (not screens)

These are top-level Figma frames that hold reusable shell/navigation building blocks rather than distinct application screens. They are referenced by several screens above and should back a shared layout/component implementation rather than a routed page.

| Figma Frame | Figma Node ID | Purpose | Notes |
|---|---|---|---|
| Header | `211:1981` | Global top navigation bar (breadcrumb, search) | **DONE** (2026-08-22) — `frontend/src/features/shell/components/Header.jsx` |
| Footer | `211:2024` | Global footer | **DONE** (2026-08-22) — `frontend/src/features/shell/components/Footer.jsx` |
| Global UI Components | `213:3261` | Contains `AppShell` (`213:3264`) | Likely the canonical app-shell composition combining header/sidebar/footer — **DONE** (2026-08-22), composed as `frontend/src/features/shell/components/AppShell.jsx` |
| Sidebar (expanded) | `213:5159` | Expanded sidebar nav state, 224px wide | **DONE** (2026-08-22) — `frontend/src/features/shell/components/Sidebar.jsx`, role-based menu filtering via `frontend/src/features/shell/rbac/` |
| Sidebar (collapsed) | `213:5708` | Collapsed/rail sidebar nav state, 56px wide | **DONE** (2026-08-22) — same `Sidebar.jsx` component, `collapsed` prop |

### MOD-001 shared shell build notes (2026-08-22)

- Built all 4 shared frames (Header, Footer, Sidebar expanded/collapsed) as one real `AppShell` composition, per explicit task requirement that Header/Footer/Sidebar visual structure stay identical across every role while only Sidebar menu content varies. `Header.jsx`/`Footer.jsx` take no `role` prop and render unconditionally; `Sidebar.jsx` renders identical chrome (logo header, footer user card, expand/collapse toggle) for every role, filtering only the nav item list via `useVisibleNav(role)`.
- **Real product decision, escalated to and confirmed by the user before building** (agent-rules.md §3): the permission-driven RBAC design (nav items declare required permissions; a `ROLE_PERMISSIONS` map per role filters them) over a flatter static role-to-items table, and the 4-role catalog — Super Admin, Organization Admin (both named in agent.md §9.2/§10.1), Data/ETL Engineer (matches the Figma Sidebar footer's own "Jane Doe / Data Engineer" content), and Viewer (read-only, project addition — not named in agent.md or Figma). See `frontend/src/features/shell/rbac/permissions.js` for the full rationale and permission catalog.
- MOCK BOUNDARY: `frontend/src/features/shell/state/sessionSlice.js`'s `currentUser`/`role` are hardcoded (default role Data/ETL Engineer) — no MOD-002 (Auth/Session) or MOD-003 (RBAC/Permissions) backend exists yet to source a real authenticated session's role. `roleChanged`/`sidebarToggled` actions exist so the shell is real, dispatchable state, not a static mock, but the session's *origin* is still mocked. Header's org-switcher/notification-dot and Footer's live stats (active pipelines, workers, queue depth, throughput) and Sidebar's "Errors" badge count all reproduce Figma's literal shown values for the same reason (no MOD-004/MOD-008/MOD-009/MOD-010 backend yet).
- `DashboardScreen.jsx` (SCR-009) was migrated from its temporary `AppShellScaffold.jsx` scaffold to this real `AppShell` — that scaffold file was deleted. `SettingsShellScaffold.jsx` (SCR-007) was **not** migrated in this pass (it has settings-specific side-nav/breadcrumb logic beyond a simple swap); tracked as a documented follow-up in `docs/reviews/review-log.md`'s Open Issues, not done here per agent-rules.md §9 scope control.
- Every Sidebar nav item now routes to a real path; destinations with no built screen yet render a shell-wrapped `RoutePlaceholder` (added ~26 new routes in `frontend/src/app/router.jsx`) rather than dead-ending, without fabricating those screens' real content ahead of their own frontend-screen-builder pass.
- UI review (`docs/reviews/review-log.md`) found and fixed one blocker (a new sidebar section-heading color token reintroduced the exact WCAG-AA-contrast-on-real-text defect class already fixed once at SCR-001) and one high-severity gap (no `focus-visible` state on any Sidebar interactive control, breaking the Header/DashboardScreen convention) before this unit passed.

### SCR-010 build notes (2026-08-22) — resolved by existing Sidebar, not rebuilt

- Selected as the next eligible screen in tracker order. Inspecting Figma node `33:3288` ("Sidebar Navigation Screen") via `get_metadata` found its actual content is **not sidebar navigation at all** — it is a full dashboard composition (page heading + Export/Refresh actions, system-status banner, 4 KPI cards, a "Pipeline Activity" table, a "System Health" panel, a 7-day "Execution Performance" chart, "Data Quality" metrics, and a "Recent Activity" feed). This is the same content shape as SCR-009's Main Dashboard Layout Screen (node `27:2238`), not the sidebar shell already built at MOD-001 (`Sidebar.jsx`, nodes `213:5159`/`213:5708`). A mislabeled Figma frame, not a real second sidebar design — flagged per agent-rules.md §3 rather than building duplicate dashboard content under a "Sidebar" screen ID.
- Escalated to the user with three options (skip as resolved/build the dashboard content anyway/other). User chose: skip SCR-010 as resolved by the already-built shared `Sidebar.jsx`, since a real, complete sidebar navigation component already exists and node `33:3288` does not actually contain an alternate sidebar design to reconcile against.
- No code changes made for this unit — this is a documentation-only resolution of a Figma labeling conflict, not a rebuild.

### SCR-011 build notes (2026-08-22) — resolved by rename, not rebuilt

- Selected as the next eligible screen in tracker order (after SCR-010, before SCR-012/013 which were already `DONE`). Inspecting Figma node `34:4157` ("Header / Top Navigation Screen") found it genuinely conflicts with the already-built shared `Header.jsx` (node `211:1981`, MOD-001): different logomark ("Meridian" wordmark vs. the shell's existing badge icon), different accent color (`#0f5699` primary vs. `#2563eb` `--color-shell-accent`), an extra "Create pipeline" button and platform-status "Healthy" pill not present in the built Header, and a different user-menu item set. This is a real Figma/project conflict per agent-rules.md §3 — not silently resolved by picking one side.
- Escalated to the user with three options (skip as resolved/replace Header/build as a separate one-off screen). User's actual answer was none of the three verbatim — a real product decision: rename the whole project's brand to **"ConnectIQ"**, and reuse the Meridian logomark SVG (already downloaded to `frontend/src/assets/brand/meridian-logomark.svg` from an earlier screen's asset export) as the app's visual mark. This resolves the naming half of the SCR-011/Header conflict without touching Header's accent color, Create button, status pill, or user-menu content — those remain out of scope since the user's instruction was about naming/branding, not a structural redesign.
- Renamed the brand-name text (previously a mix of "DataFlow" in the shell/dashboard/router and "Meridian" in the auth/splash/settings screens — two different names were already in inconsistent use pre-rename, not introduced by this change) to "ConnectIQ" across: `Sidebar.jsx` (brand header block), `Header.jsx` (default breadcrumb + doc comment), `Footer.jsx` (copyright), `DashboardScreen.jsx` (breadcrumb prop), `router.jsx` (~20 breadcrumb arrays), `LoginScreen.jsx`/`ResetPasswordScreen.jsx`/`ForgetPasswordScreen.jsx`/`EmailVerificationScreen.jsx`/`TwoFactorAuthenticationScreen.jsx` (brand name, tagline copy, "Protected by X Enterprise Auth" footer line, copyright line), `SettingsShellScaffold.jsx` (brand name), `SplashScreen.jsx` (brand name, copyright line).
- Swapped `Sidebar.jsx`'s brand-header icon from the separate `app-logo.svg` (a colored badge-wrapped icon) to the real Meridian logomark image directly (no badge wrapper — the logomark's own navy/blue palette already carries brand weight, confirmed by ui-reviewer). The auth/splash screens already used the real Meridian logomark asset before this change (only their brand-name text needed updating, not their logo).
- Deleted the now-orphaned `frontend/src/assets/icons/shell/app-logo.svg` (flagged by ui-reviewer, confirmed unused anywhere else).
- Left as documented, out-of-scope per agent-rules.md §9: a pre-existing copyright-year mismatch (auth screens/Splash render "© 2025", Footer renders "© 2026") — not introduced by this rename, now visible in both places since both say "ConnectIQ", tracked as an Open Issue below.
- No Figma MCP re-verification of node `34:4157`'s pixel-level layout was performed beyond the initial `get_design_context` capture used to establish the conflict — this unit is a naming/branding resolution, not a pixel-fidelity build of that frame's Create-button/status-pill/user-menu content, which remains undone and out of scope until a future explicit decision to build it.

### MOD-001 AppShell visual-polish redesign (2026-08-22)

- User requested a follow-up "redesign the shellapp" pass with a brand-new minimize/collapse button icon. Node `255:1390` (the URL given for this request) turned out to be a full-page composition of the *same* Header/Sidebar-expanded/Footer content already built — no new visual variant existed there — so this was confirmed with the user as a real product decision rather than assumed: scoped to **refined visual polish only** (same layout/structure/nav tree, modernized surface details — shadows, hover/active-state treatment, transitions) plus a genuinely new collapse/expand icon design (**double-chevron, direction-flipping** — user's choice), not a structural redesign.
- New `frontend/src/features/shell/components/SidebarToggleIcon.jsx` — hand-authored inline SVG, intentionally **not** sourced from Figma (no frame defines a redesigned icon; this is new iconography per the user's explicit ask). Replaces the previously Figma-exported `sidebar-toggle.svg` asset (deleted) in both the expanded and collapsed Sidebar toggle buttons.
- Added additive-only visual polish across `Header.jsx`/`Footer.jsx`/`Sidebar.jsx`: `shadow-sm` on small avatar/logo chips, `transition-colors duration-150` on every interactive control, a left accent bar (`before:*`) on the active nav item (both expanded and collapsed-rail states), and turned the Sidebar footer user-card from a static block into a real focusable `<button>`. No nav tree, spacing, RBAC filtering, or role behavior was touched — confirmed by both quality-gate agents.
- UI review found no blockers; two low, non-blocking notes (`shadow-sm` isn't a formal design token since none exists yet in this system; a transition-duration declaration inconsistency) — the duration inconsistency was fixed, the shadow-token note left as a documented Open Issue rather than inventing a new token system for 4 sparse usages.

### SCR-014 build notes (2026-08-22)

- Selected as the next eligible screen after SCR-013 (MOD-001's 6 shell screens all `DONE`). Node `45:7`'s full-screen `get_design_context` call returned only a sparse metadata fallback (too large to fit in one response) — 13 sub-node IDs (header, executive summary strip, both 4-card KPI rows, all 3 chart cards, the alerts card, operational health card, reports card, and the business-unit table) were fetched individually via targeted `get_design_context` calls to get real code, colors, and icon asset URLs.
- All Figma colors observed (`#0f5699` primary, `#16a34a`/`#15803d` success, `#dc2626`/`#b91c1c` danger, `#d97706` warning, `#9ca3af`/`#6b7280`/`#111827` text) already match this project's existing `tokens.css`/`tailwind.config.js` semantic tokens exactly — no new tokens were needed.
- 16 real icon SVGs were downloaded byte-for-byte from their Figma asset URLs (which expire ~7 days) to `frontend/src/assets/icons/executive-dashboard/` — none hand-authored. Two of the sixteen (`icon-export.svg`, `icon-refresh.svg`, `icon-activity.svg` conceptually overlap with existing `frontend/src/assets/icons/icon-export.svg` etc.) were kept as separate files since their real exported geometry/size/color differs from the existing shell-icon versions (diff-checked; not simple duplicates).
- **Chart regions are real inline SVG, not Figma's static hand-drawn art and not a new charting-library dependency.** Figma's own SLA/execution-volume/adoption chart regions (nodes `45:1110`, `45:1288`, `45:1509`) are composed of raw `<vector>`/path primitives with no live-render contract — confirmed via targeted `get_design_context` on `45:1084`. No charting library (recharts/visx/etc.) exists in `frontend/package.json`, and agent-rules.md §4 disallows adding one without an explicit requirement. Per `DashboardScreen.jsx`'s own precedent (a documented "requires a real endpoint" placeholder for its trend chart), this screen instead renders the real mock/API data shape (5-point SLA series, 5-point daily success/fail volume, 12-week adoption series) as lightweight, dependency-free inline `<svg>` line/bar primitives (`SlaTrendChart`/`ExecutionVolumeChart`/`AdoptionTrendChart` in `ExecutiveDashboardScreen.jsx`) — a step further than DashboardScreen's placeholder, since real chart-shaped data existed to render.
- **Figma data-entry bug, not reproduced**: node `45:1661`'s "Adoption by Business Unit" table has 4 duplicate rows in the source file — all read "Sales & Revenue Ops" / "68 users" (confirmed via direct fetch; only pipeline count/% differ, and the node-id namespace jumps from `45:xxxx` to a `52:106xx` series for rows 2–4, suggesting a paste-duplicated component instance). The mock data (`executiveDashboard.api.js`) uses 8 distinct business units instead, consistent with the screen's own "Across 8 business units" KPI figure — flagged for the design file owner, not reproduced as 4 identical rows.
- Figma uses a 16px card corner radius throughout this frame; the project has no radius token beyond `--radius-sm` (3px)/`--radius-md` (6px), and `design-system-notes.md` confirms zero bound Figma variables/styles anywhere in the file (colors/radii are all hardcoded per-instance in the design) — this screen uses `rounded-md` to stay visually consistent with `DashboardScreen.jsx`'s existing cards rather than introduce a one-off larger radius with no token backing.
- MOCK BOUNDARY: `frontend/src/features/dashboard/services/executiveDashboard.api.js`'s `getExecutiveDashboard()` attempts a real `GET /api/dashboard/executive` first and falls back to mock data on any non-2xx or non-JSON response (MOD-009 is still `PLANNED`, no backend deployed) — same mock-fallback shape as `dashboard.api.js`'s `getDashboardSummary()`. The date-range toggle (7d/30d/90d/1yr) is real interactive state feeding the React Query key, but the mock dataset is a single fixed 30-day snapshot and ignores the range param; documented as a known mock-mode limitation in the file's own doc comment, not real range-aware aggregation.
- UI review (`docs/reviews/review-log.md`) found and fixed: the business-unit table used a bare `<ul>/<li>` instead of a real `<table>`/`<thead>` (inconsistent with `DashboardScreen.jsx`'s `PipelineActivityCard` convention, no column context for screen readers); the loading skeleton only covered a fraction of the real layout (risked a large load-time layout shift); the icon-only Refresh button had no `aria-label`; the platform-status summary pill hardcoded a green/healthy tone instead of reading the mock data's own `status` field. All four fixed.
- Tester (`docs/reviews/review-log.md`) found and fixed a real bug, not just an environment gap: with no dev API proxy configured in `vite.config.js`, a real fetch resolves to the Vite dev server's own HTML SPA fallback (200 OK, non-JSON) rather than a network failure, and the mock-fallback `catch` block only handled genuine thrown-error cases — so the screen got stuck on a hard error instead of showing mock data by default in this dev environment. Fixed in `executiveDashboard.api.js` by checking the response's `content-type` and always falling back to mock data in the `catch`. The identical pre-existing pattern in `dashboard.api.js` (SCR-009) was deliberately left unfixed here per agent-rules.md §9 scope control — recorded as an Open Issue in `docs/reviews/review-log.md` for a future pass.
- Follow-up (2026-08-22, explicit user request after this screen was already `DONE`): added a screen-local `ScreenFooter` inside `ExecutiveDashboardScreen.jsx`, below the business-unit table — distinct from `AppShell`'s global `Footer` (fixed pipeline/worker/queue stats shared by every screen). Shows this screen's own live query state: last-updated time / "Refreshing…", the active date-range selection, a real running total already in the payload (`executionVolume.stats.totalThisMonth`), a live count of critical alerts, and the same "Sample data" mock disclosure used elsewhere on this screen — all sourced from real data already in `useExecutiveDashboard`'s query result, nothing fabricated.

### SCR-015 build notes (2026-08-22)
- Selected as the next eligible screen after SCR-014 (MOD-001's 6 shell screens + SCR-014 all `DONE`). Node `49:1793` (`get_metadata`, split into targeted `get_design_context` calls on sub-nodes since the full-frame call exceeded the response size limit — same precedent as SCR-014) contains: header actions (Create Pipeline/Import/Export/date-range/Refresh), a 6-item global summary strip, 8 KPI cards (Total Pipelines, Running Now, Failed Today, Success Rate, Scheduled, Queue Depth, Data Processed, SLA Compliance), a Pipeline Health status breakdown card + Operational Alerts list side by side, an Active Pipelines table, 24-hour Execution Volume + 7-day Success Rate Trend charts side by side, and an All Pipelines table with search/status-filter tabs/bulk-action toolbar/pagination.
- All 18 real icon SVGs (`IcoPlus`, `IcoImport`, `IconExport`, `IconRefresh`, `IcoPipeline`, `IcoWave`, `IcoTriangle`, `IcoCircleCheck`, `IcoClock`, `IcoPlay`, `IcoDatabase`, `IcoShield`, `IcoX`, `IcoGearSm`, `IcoColumns`, `IcoFilter`, `IcoSearch`, `IcoPlay2`) were downloaded byte-for-byte from their Figma asset URLs to `frontend/src/assets/icons/pipeline-overview/` — none hand-authored. The warning-severity alert icon reuses the existing shared `frontend/src/assets/icons/icon-alert-triangle.svg` (glyph match confirmed) rather than downloading a near-duplicate `IcoAlertSm`.
- **Chart regions are real inline SVG**, same precedent and same no-new-dependency rationale as `ExecutiveDashboardScreen.jsx`'s `SlaTrendChart`/`ExecutionVolumeChart` — Figma's own 24h-execution-volume and 7-day-success-rate chart regions (nodes `49:3307`, `49:3388`) are static hand-drawn vector art with no live-render contract; this screen renders the real mock data shape as lightweight bar/line SVG primitives (`ExecutionVolumeChart`/`SuccessRateChart` in `PipelineOverviewScreen.jsx`).
- The "All Pipelines" table's search, status-filter tabs, row/select-all checkboxes, bulk-actions toolbar, and pagination are real client-side interactive state (`useState`/`useMemo`), not a static reproduction of Figma's shown counts — filtering resets to page 1 and clears selection, matching the expected UX for a real data table.
- MOCK BOUNDARY: `frontend/src/features/dashboard/services/pipelineOverview.api.js`'s `getPipelineOverview()` attempts a real `GET /api/dashboard/pipelines` first and falls back to mock data on any non-2xx or non-JSON response (MOD-008 — Pipeline Builder & Execution — is still `PLANNED`, no backend deployed), mirroring `executiveDashboard.api.js`'s dev-proxy-aware fallback shape (confirmed live by the tester agent: hitting the route without a backend returns the Vite SPA's own `text/html` fallback, correctly rejected by the content-type guard). The header's Today/7d/30d date-range toggle is real interactive state threaded into the React Query key and the (currently unbuilt) `?range=` query param, but the mock dataset is a single fixed snapshot that ignores the range — documented as a known mock-mode limitation in the API file's own doc comment, same precedent as `executiveDashboard.api.js`.
- UI review (`docs/reviews/review-log.md`) failed the first pass and was fixed before verification: the date-range toggle was decorative (local UI state only, never reaching the data layer) — fixed by threading `dateRange` through `usePipelineOverview(dateRange)` into the query key and `getPipelineOverview(dateRange)`'s `?range=` param, with the mock's range-ignoring limitation documented in a code comment, mirroring `useExecutiveDashboard`'s pattern exactly. Two low-severity notes were left as documented, non-blocking: the inline SVG chart fills use literal hex (`#0f5699`/`#dc2626`) matching `--color-primary`/`--color-danger` exactly — same pre-existing, previously-accepted constraint as `ExecutiveDashboardScreen.jsx`'s charts (SVG attributes can't consume Tailwind/CSS-var classes); and `chartPoints`/`linePath`/`Legend`/`CardHeader` are duplicated from `ExecutiveDashboardScreen.jsx` rather than extracted to a shared module — flagged as a future extraction candidate once a third MOD-009 dashboard screen needs the same primitives, not fixed here per agent-rules.md §9 scope control.
- Tester (`docs/reviews/review-log.md`) confirmed via real commands: `oxlint` (0 warnings/errors), `vite build` (clean, only the pre-existing chunk-size advisory), all 18 new icon SVGs + the shared `icon-alert-triangle.svg` resolve with `200` over the dev server, the `/dashboard/pipelines` route serves the SPA shell correctly, and the mock-fallback content-type guard was verified live against the dev server's own HTML fallback. No browser-automation tool was available in that session to click through the search/filter/pagination/checkbox interactions or capture a screenshot — recorded as a documented tooling gap (same class of gap as SCR-005–014's "Figma MCP call did not resolve" notes), not a failure; the underlying state/handler code for those interactions was independently reviewed and confirmed correctly wired by both the ui-reviewer and tester agents.

### SCR-016 build notes (2026-08-22)
- Selected as the next eligible screen after SCR-015 (MOD-001's 6 shell screens + SCR-014/SCR-015 all `DONE`). Node `50:4167` (`get_metadata` split into targeted `get_design_context` calls on 11 sub-frames — same size-limit precedent as SCR-014/015) contains: header (title/Create Rule/Run Validation/Export/date-range/Refresh), 8 KPI cards (Overall Quality Score, Healthy Datasets, Failed Validations, Active Quality Rules, Rule Pass Rate, Freshness SLA, Schema Compliance, Critical Incidents), a Quality Score Trend line chart + Quality Alerts list side by side, a Validation Results stacked bar chart + Dataset Health breakdown side by side, a Rule Violations table + Data Freshness SLA table side by side, a Schema Monitoring table, and a Failed Validations table with search/severity-filter/pagination/bulk-select.
- 6 new icon SVGs (`IcoRefreshCw`, `IcoAlertCircle`, `IcoCheckSquare`, `IcoBarChart2`, `IcoLayers`, `IcoZap`) were downloaded byte-for-byte from their Figma asset URLs to `frontend/src/assets/icons/data-quality/` — none hand-authored. 11 other icons (`IcoPlus`, `IconExport`, `IconRefresh`, `IcoCircleCheck`, `IcoDatabase`, `IcoClock`, `IcoX`, `IcoGearSm`, `IcoColumns`, `IcoFilter`, `IcoSearch`) reuse existing glyph-matched assets already downloaded for `PipelineOverviewScreen.jsx`; `IcoAlertSm` reuses the existing `executive-dashboard/icon-alert-sm.svg`.
- **Chart regions are real inline SVG**, same precedent as `ExecutiveDashboardScreen.jsx`/`PipelineOverviewScreen.jsx` — Figma's own Quality Score Trend and Validation Results chart regions (nodes `50:5262`, `50:5463`) are static hand-drawn vector art with no live-render contract; this screen renders the real mock data shape as lightweight line/stacked-bar SVG primitives (`QualityScoreTrendCard`'s inline chart, `ValidationResultsChart`).
- MOCK BOUNDARY: `frontend/src/features/dashboard/services/dataQuality.api.js`'s `getDataQuality()` attempts a real `GET /api/dashboard/data-quality` first and falls back to mock data on any non-2xx or non-JSON response (MOD-009 is still `PLANNED`, no backend deployed), mirroring `pipelineOverview.api.js`'s dev-proxy-aware content-type guard (confirmed live by the tester agent: hitting the route without a backend returns the Vite SPA's own `text/html` fallback, correctly rejected). The header's Today/7d/30d date-range toggle is real interactive state threaded into the React Query key and the (currently unbuilt) `?range=` query param, but the mock dataset is a single fixed snapshot that ignores the range — same documented mock-mode limitation precedent as `pipelineOverview.api.js`/`executiveDashboard.api.js`.
- The Failed Validations table's search, severity-filter tabs (All/Critical/Warning/Info), row/select-all checkboxes, and pagination are real client-side interactive state (`useState`/`useMemo`), matching `PipelineOverviewScreen.jsx`'s `AllPipelinesTable` convention exactly — filtering resets to page 1 and clears selection.
- UI review (`docs/reviews/review-log.md`) passed with one low-severity, pre-existing note carried over from `PipelineOverviewScreen.jsx`: the owner-initials avatar's `h-5.5 w-5.5` class is not a valid Tailwind utility (no `5.5` step in the default or project spacing scale), so it renders unsized beyond padding — inherited verbatim from the already-shipped sibling screen, not introduced here; left as a documented Open Issue rather than fixed in only one of the two screens per agent-rules.md §9 scope control.
- Tester (`docs/reviews/review-log.md`) confirmed via real commands: `oxlint` (0 errors in new files, 1 unrelated pre-existing warning in `AppShell.jsx`), `vite build` (clean, only the pre-existing chunk-size advisory), all 6 new icon SVGs are present/non-empty/valid, all 17 icon import paths resolve to real files, and the `/dashboard/data-quality` route serves the SPA shell correctly with the mock-fallback content-type guard verified live. No browser-automation tool was available in that session to click through the search/filter/pagination/checkbox interactions — recorded as a documented tooling gap (same class of gap as SCR-014/015's notes), not a failure; the underlying state/handler code was independently reviewed and confirmed correctly wired by both the ui-reviewer and tester agents.

### SCR-017 build notes (2026-08-22)
- Selected as the next eligible screen after SCR-016 (MOD-001's 6 shell screens + SCR-014/015/016 all `DONE`). Node `51:6763`'s full-frame `get_design_context` and `get_metadata` calls both exceeded the response size limit — resolved via a subagent that traversed the metadata to find 13 direct-child sub-node IDs (page header, global status banner, 8-card KPI grid, service health table + system alerts side by side, CPU/memory trend charts side by side, queue/worker + database health + API performance 3-column row, infrastructure components table with header/toolbar/body/footer), each then fetched individually via targeted `get_design_context` calls — same size-limit precedent as SCR-014/015/016.
- All 11 new icon SVGs (`icon-circle-check`, `icon-shield`, `icon-server`, `icon-wave`, `icon-cpu`, `icon-activity`, `icon-hard-drive`, `icon-bell`, `icon-sort-up`, `icon-chevron-left`, `icon-chevron-right`) were downloaded byte-for-byte from their real Figma asset URLs to `frontend/src/assets/icons/system-health/` — none hand-authored (an initial hand-drawn attempt was caught and discarded before use, per the figma-design-to-code skill's explicit prohibition on inventing vector data). 8 other icons (`refresh`, `export`, `refresh-cw`, `alert-sm`, `gear-sm`, `columns`, `filter`, `search`) reuse existing glyph-matched assets already downloaded for sibling screens.
- **Chart regions are real inline SVG**, same precedent as `ExecutiveDashboardScreen.jsx`/`PipelineOverviewScreen.jsx`/`DataQualityScreen.jsx` — Figma's own CPU/Memory utilization trend chart regions (nodes `51:8560`, `51:8637`) are static hand-drawn vector art with no live-render contract; this screen renders the real mock data shape as a lightweight inline-SVG line primitive (`UtilizationTrendCard`'s chart) shared by both the CPU and Memory cards.
- MOCK BOUNDARY: `frontend/src/features/dashboard/services/systemHealth.api.js`'s `getSystemHealth()` attempts a real `GET /api/dashboard/system-health` first and falls back to mock data on any non-2xx or non-JSON response (MOD-009 is still `PLANNED`, no backend deployed), mirroring the dev-proxy-aware content-type guard established at SCR-014 and reused by every sibling MOD-009 screen since. The header's Live/1h/24h/7d time-range toggle is real interactive state, but the mock dataset is a single fixed snapshot that ignores the range — same documented mock-mode limitation precedent as the sibling screens.
- The Infrastructure Components table's search, category-filter tabs (All/Healthy/Warning/Compute/Database/Network), row/select-all checkboxes, and pagination are real client-side interactive state (`useState`/`useMemo`), matching `PipelineOverviewScreen.jsx`'s/`DataQualityScreen.jsx`'s table convention exactly — filtering resets to page 1 and clears selection.
- No route or sidebar nav item existed for this screen prior to this build (unlike SCR-014/015/016, which had `RoutePlaceholder` entries already wired) — added both `frontend/src/app/router.jsx`'s `/dashboard/system-health` route and `frontend/src/features/shell/rbac/navConfig.js`'s "System Health" nav item under the Analytics section, alongside the existing Dashboard/Reports/Data Quality items.
- UI review (`docs/reviews/review-log.md`) failed the first pass and was fixed before verification: `InfrastructureComponentsTable` had full checkbox/select-all state with no visible bulk-selection feedback bar, unlike both sibling tables — added one matching the sibling convention exactly. A dead-code ternary in `UtilizationTrendCard`'s footer grid was simplified. A duplicate-refresh-control finding (both "Refresh Status" and a separate icon-only refresh button called the identical `refetch()`) was fixed by removing the redundant icon-only button, keeping "Refresh Status" as the sole refresh affordance per the one-refresh-control convention every sibling screen follows. One low-severity naming-clarity note (`TREND_TONE` keys encode arrow direction, not sentiment) was left as a documented Open Issue rather than renamed out of scope.
- Tester (`docs/reviews/review-log.md`) confirmed via real commands: `oxlint` (0 errors in new files), `vite build` (clean, only the pre-existing chunk-size advisory), all 11 new icon SVGs structurally valid and non-empty, all 19 unique icon import paths (new + reused) resolve with `200` over the dev server, the `/dashboard/system-health` route serves the SPA shell correctly, and the mock-fallback content-type guard was verified live against the dev server's own HTML fallback. No browser-automation tool was available in that session to click through the search/filter/pagination/checkbox interactions — recorded as a documented tooling gap (same class as SCR-005–016's notes), not a failure.

### SCR-018 build notes (2026-08-22)
- Selected as the next eligible screen after SCR-017 (MOD-001's 6 shell screens + SCR-014/015/016/017 all `DONE`). Node `56:11333` (`get_metadata` split into targeted `get_design_context` calls on sub-frames — same size-limit precedent as SCR-014–017) contains: header (Add Source/Test All/Export + Live/1h/24h/7d range toggle), 12-card KPI grid (including the inventory's named "auth-errors widget" KPI card), a Connection Health chart + Source Alerts list side by side, an "Authentication Status" card (6 credential stat tiles + a Credential Expiry Timeline list — this is the full auth-errors widget referenced in the Screen Matrix), a Source Performance chart + filterable Schema Monitoring card side by side, a Data Freshness SLA table, and an All Sources table with search/category-filter pills/checkboxes/pagination.
- 4 new icon SVGs (`icon-plug`, `icon-key`, `icon-wifi`, `icon-activity`) were downloaded byte-for-byte from their real Figma asset URLs to `frontend/src/assets/icons/source-health/` — none hand-authored, per the figma-design-to-code skill's prohibition on inventing vector data. The header's "Add Source" plus icon reuses the byte-identical existing `pipeline-overview/icon-plus.svg` rather than downloading a duplicate. All other icons (circle-check, x, alert-sm, clock, refresh-cw, layers, wave, shield, gear-sm, columns, filter, search, chevron-left/right, check) reuse existing glyph-matched assets already downloaded for sibling MOD-009 screens.
- **Chart regions are real inline SVG**, same precedent as `SystemHealthScreen.jsx`/`DataQualityScreen.jsx`/`PipelineOverviewScreen.jsx`/`ExecutiveDashboardScreen.jsx` — Figma's own Connection Health and Source Performance chart regions (nodes `56:12231`, `56:12900`) are static hand-drawn vector art with no live-render contract; this screen renders the mock data shape as lightweight inline-SVG line primitives.
- MOCK BOUNDARY: `frontend/src/features/dashboard/services/sourceHealth.api.js`'s `getSourceHealth()` attempts a real `GET /api/dashboard/source-health` first and falls back to mock data on any non-2xx or non-JSON response — this screen has a double dependency gap (MOD-006 Data Sources, which would produce real source/connection/auth data, AND MOD-009's own dashboard backend are both still `PLANNED`), same dev-proxy-aware content-type guard precedent as every sibling MOD-009 screen. The header's Live/1h/24h/7d range toggle is real interactive state, but the mock dataset is a single fixed snapshot that ignores the range — same documented limitation as the sibling screens. All header action buttons (Add Source, Test All, Export) are disabled with explanatory `title` attributes naming the blocking module, since MOD-006 doesn't exist yet to back them.
- The Schema Monitoring card's type filter (All/Breaking/Additive/Renamed/Removed) and the All Sources table's search/category-filter/checkboxes/pagination are real client-side interactive state (`useState`/`useMemo`), matching sibling screens' table convention exactly.
- UI review (`docs/reviews/review-log.md`) found and fixed one medium-severity issue before passing: `ConnectionHealthCard`'s mock data defined `timeoutRate`/`avgLatency` fields that the component never rendered (dead data, a regression vs. every sibling screen's fully-consumed mock objects) — fixed by extending the stat-tile row to a two-row layout consuming both fields, matching `SourcePerformanceCard`'s existing tile-grid pattern in the same file. Two low-severity notes were left as documented, non-blocking, repo-wide (not introduced by this build): `iconCheck` sourced from the flat `assets/icons/check.svg` rather than a per-screen subfolder (inconsistent with this file's own per-screen icon convention, but functionally correct); and the bulk-action bar's `Test Connections`/`Disable` buttons lack `focus-visible` classes, mirroring the identical pre-existing gap in `SystemHealthScreen.jsx`'s bulk bar.
- Tester (`docs/reviews/review-log.md`) confirmed via real commands: `oxlint` (clean), `vite build` (clean, only the pre-existing chunk-size advisory), all 4 new icon SVGs structurally valid and non-empty, all 22 icon import paths resolve to real files, and a live Playwright load of `/dashboard/source-health` rendered the full screen (KPI cards, charts, alerts, tables, sidebar nav highlighted, breadcrumb) with zero console errors and the mock-fallback content-type guard correctly triggering the "Sample data" badge against the dev server's own HTML fallback.

### SCR-019 build notes (2026-08-23)
- Selected as the next eligible screen after SCR-018 (MOD-001's 6 shell screens + SCR-014/015/016/017/018 all `DONE`; SCR-019 was already flagged `IN_PROGRESS` in the tracker but had no implementation, build notes, or review-log entries — it had been marked next but never actually built). Built as the OUTBOUND (destination) analog of SCR-018 Source Health, reusing that screen's exact structure/conventions: 12-KPI grid, a delivery-success inline-SVG chart + destination-alerts list side by side, a delivery-volume-by-type + retry-queue card, a sync-performance chart + delivery-methods filter card side by side, a Delivery SLA table, and an All Destinations table with search/category-filter/checkbox-select/pagination.
- **FIGMA-VERIFICATION GAP (agent-rules.md §2):** no Figma MCP tool was available in this build's session, so node `65:17501`'s pixel-level layout was **not** independently inspected. The screen was built against this inventory's own description ("Default, delivery/sync success-rate widgets") and the established MOD-009 sibling pattern (SCR-018 `SourceHealthScreen.jsx` is the direct analog). Recorded as `not-verified` fidelity, not an asserted pixel match — same class of documented gap as SCR-005–009's "Figma MCP call did not resolve" notes. A future Figma-enabled pass should re-diff node `65:17501` and reconcile any layout/section differences.
- **Icons reuse existing glyph-matched assets** rather than being hand-authored or node-exported, for the same reason (no Figma MCP to download node `65:17501`'s own icons this session — and the figma-design-to-code skill prohibits inventing vector data). All 20 icon imports resolve to real files already downloaded for sibling screens (`pipeline-overview/`, `system-health/`, `data-quality/`, `executive-dashboard/`, `source-health/`, and the flat `assets/icons/`); `KPI_ICON`'s `target` key maps to `executive-dashboard/icon-database.svg` as the closest available warehouse/destination glyph. Documented in the `KPI_ICON` comment.
- **Chart regions are real inline SVG**, same precedent and same no-new-dependency rationale as every sibling MOD-009 screen — the delivery-success and sync-performance charts render the real mock series as lightweight inline `<svg>` line primitives. The two `stroke="#0f5699"` literals are the pre-accepted inline-SVG-attribute constraint (SVG attributes can't consume Tailwind/CSS-var classes), identical to `SourceHealthScreen.jsx`'s charts.
- **DESTINATION-TYPE CAVEAT (module-plan.md MOD-007 "Global Build Risks"):** MOD-007's destination connector taxonomy is explicitly unenumerated in the current design/spec. The category set used in the mock (Warehouse / Database / Storage / Streaming / BI / Webhook) is realistic sample data, **not** a confirmed connector list — flagged in `destinationHealth.api.js`'s header and to be reconciled once MOD-007's spec lands. Note: the All Destinations `categoryFilters` expose Warehouse/Database/Storage/Streaming chips only, so the two sample rows tagged `BI`/(and any `Webhook`) aren't reachable via the category chips — acceptable for mock data, tracked as an Open Issue.
- MOCK BOUNDARY: `frontend/src/features/dashboard/services/destinationHealth.api.js`'s `getDestinationHealth()` attempts a real `GET /api/dashboard/destination-health` first and falls back to mock data on any non-2xx or non-JSON response — double dependency gap (MOD-007 Destinations, which would produce real destination/delivery/sync data, AND MOD-009's own dashboard backend are both still `PLANNED`), same dev-proxy-aware content-type guard precedent as every sibling MOD-009 screen (verified live against the Vite preview server's own `text/html` SPA fallback). The Live/1h/24h/7d range toggle is real interactive state threaded into the React Query key and `?range=` param, but the mock dataset is a single fixed snapshot that ignores the range — same documented limitation as the sibling screens. All header action buttons (Add Destination, Test All, Export) and both bulk-action buttons are disabled with explanatory `title` attributes naming the blocking module (MOD-007/MOD-009).
- The Delivery Methods filter (All/Batch/Streaming/Micro-batch) and the All Destinations table's search/category-filter/checkboxes/pagination are real client-side interactive state (`useState`/`useMemo`), matching sibling screens' table convention exactly — filtering resets to page 1 and clears selection.
- Added both the `/dashboard/destination-health` route (`frontend/src/app/router.jsx`, wired to the real `DestinationHealthScreen`, not a `RoutePlaceholder`) and the "Destination Health" nav item (`frontend/src/features/shell/rbac/navConfig.js`, under Analytics, `ANALYTICS_READ`) — neither existed before this build.
- Quality gate: `ui-reviewer` returned **PASS** with no blocker/high/medium findings (structure, tokens, states, accessibility all mirror the SCR-018 pattern-of-record; Figma fidelity flagged `not-verified` as expected). `tester` layer: `npx oxlint` clean for the unit (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings), `npm run build` clean (258 modules, only the pre-existing chunk-size advisory), all 20 icon imports resolve on disk, route + nav wired, and the mock-fallback content-type guard verified live against the preview server's own HTML fallback. No browser-automation tool available this session to click through the search/filter/pagination/checkbox interactions — documented tooling gap (same class as SCR-005–018's notes), not a failure; interactive state/handler code was reviewed and confirmed correctly wired by the ui-reviewer.

### SCR-020 build notes (2026-08-24)
- Selected as the next eligible MOD-009 screen after SCR-019 (all of MOD-001's 6 shell screens + SCR-014/015/016/017/018/019 `DONE`). Built as a real, dynamic screen: `ExecutionStatisticsScreen.jsx` + `useExecutionStatistics.js` (React Query, 30s refetch, `range` in query key) + `executionStatistics.api.js` (real-fetch-first → content-type-guarded mock fallback), wired at `/dashboard/executions` in `router.jsx` (replaced the prior `RoutePlaceholder`).
- **FIGMA FIDELITY = `verified` (agent-rules.md §2):** unlike SCR-018/019, the Figma MCP WAS available this session — node `84:5098` was inspected live via `get_metadata` + `get_screenshot`. Every KPI value, chart series, outcome breakdown, runtime percentile, environment stat, ranked list, schedule stat, insight, and all 10 execution rows are transcribed from the actual frame's text nodes.
- **FIGMA-SOURCE CONFLICT (agent-rules.md §3):** the frame quotes two different success rates — `98.3%` (headline chip) vs. `98.9%` (summary strip / KPI card). Reconciled to `98.9%` in the mock (38,037 / 38,452 = 98.92%, the arithmetically correct figure); the `98.3%` is a Figma typo, documented in `executionStatistics.api.js`'s header rather than silently reproduced.
- **Chart honesty (ui-reviewer Medium #1, fixed):** `VolumeTrendCard`'s Successful and Failed lines now share the real labelled 0–axisMax scale (Failed rides low, which is truthful) — the earlier build rescaled Failed by an invented divisor so it no longer matched the visible y-axis. No charting library dependency (same inline-SVG precedent as SCR-014–019); the one hex stroke pair is the pre-accepted SVG-attribute constraint.
- **`purple` tone → blue (ui-reviewer Medium #4, deferred):** the `purple` outcome-breakdown/insight keys render blue via the shell-accent token because no `purple` color token exists in `tokens.css`/`tailwind.config.js`. Left as-is (a screen-scoped fix would hardcode a raw hex or invent a one-off token); tracked as an Open Issue for a future project-wide token pass.
- MOCK BOUNDARY: triple dependency gap — MOD-006 (Data Sources) / MOD-008 (Pipeline Builder & Execution, the module that produces real execution-run telemetry) AND MOD-009's own dashboard backend are all still `PLANNED`. "Sample data" badge in header + footer discloses this. The 7d/30d/90d/1y range toggle is real interactive state threaded into the React Query key and `?range=` param, but the mock is a single fixed snapshot ignoring the range — same documented limitation as sibling screens. Generate Report / Export are disabled with explanatory `title` attributes naming the blocking module.
- The executions table's search + status/environment/trigger filters + pagination are real client-side interactive state (`useState`/`useMemo`); filter/search changes reset to page 1, matching the sibling table convention.

### SCR-022 build notes (2026-08-24)
- Selected as the next eligible MOD-009 screen after SCR-021 (SCR-001–SCR-021 all `DONE`). Built as a real, dynamic screen: `PerformanceAnalyticsScreen.jsx` + `usePerformanceAnalytics.js` (React Query, 30s refetch, `range` in query key) + `performanceAnalytics.api.js` (real-fetch-first → content-type-guarded mock fallback), wired at `/dashboard/performance` in `router.jsx` (replaced the prior `shellRoute('SCR-022','Reports',…)` placeholder; the label was also corrected from "Reports" to the real Performance breadcrumb). Cloned line-for-line from the SCR-020/SCR-021 pattern of record.
- **FIGMA FIDELITY = `verified` (agent-rules.md §2):** node `70:43606` was inspected live via `get_metadata` (418 text nodes extracted) + `get_screenshot` during this build. Every KPI value, chart series, resource-utilization row, runtime/throughput stat, ranked pipeline, regression, worker row, capacity forecast, recommendation, and all 8 component table rows are transcribed from the actual frame's text nodes. The ui-reviewer agent had no Figma MCP tool exposed in its session, so it marks fidelity `verified-by-cross-check` against the build-time transcription + approved siblings, not an independent re-inspection — same review-coverage gap recorded at SCR-005–009/012.
- Sections built to match the frame: headline banner (94.8% efficiency / 99.2% SLA / +8.4%), 12-KPI grid, Performance Score Trend (dual line: score + SLA, inline SVG) + Resource Utilization bars (incl. Disk I/O 88% Critical), Runtime Performance Analytics (avg/fastest/slowest + P50–P99) + Throughput Analytics (records/data/pipelines + hourly bar chart), Fastest Pipelines + Performance Regressions, Worker Performance table, Capacity Planning (90d forecast) + Performance Recommendations (Critical/High/Medium with impact chips), and the 8-of-142 Component Performance table with search + Category/SLA/Environment filters + pagination.
- **tester FAIL → fixed before sign-off:** the Component Performance table's Environment filter read `row.environment`, but the mock rows had no `environment` field, so selecting any environment option filtered out every row (latent bug — "All" default worked). Added an `environment` field (`Production`/`Staging`/`Development`) to all 8 component rows in `performanceAnalytics.api.js`, the shape-consistent fix that preserves the designed filter. Re-ran lint (clean) after the fix.
- No charting library (agent-rules.md §4): both charts are hand-rolled inline SVG (`ScoreTrendCard` dual line-path on a shared 0–100 scale; `ThroughputCard` bar chart), same precedent as SCR-014–021. The two hex stroke values (`#0f5699` score, `#16a34a` SLA) are the pre-accepted SVG-attribute constraint where Tailwind classes cannot apply.
- MOCK BOUNDARY: MOD-008 (Operations — real worker/queue/runtime telemetry) and MOD-009's own dashboard backend are both `PLANNED`. "Sample data" badge in header + footer discloses this. The 7d/30d/90d/1y range toggle is real React Query state threaded into the query key and `?range=` param, but the mock is a single fixed snapshot ignoring the range — same documented limitation as sibling screens. Export Analytics / Generate Report / Export table / all "View …" card links are disabled with explanatory `title` attributes naming the blocking PLANNED module.
- Open ui-reviewer findings (4 low, all inherited-by-design from the approved SCR-020/021 pattern, deferred to a project-wide pass per agent-rules.md §9): `text-token-xs` dense metadata + small `text-warning` badges below WCAG AA contrast; permanently-disabled primary actions are unfocusable so their `title` is mouse-only; range toggle ignored by the fixed mock; and `ScoreTrendCard` renders 8 tick labels beneath a 14-point series (cosmetic, aria-label conveys real min/max). See `docs/reviews/review-log.md`.

### SCR-025 build notes (2026-08-24)
- Selected as the next eligible MOD-004 screen after SCR-024 (Organization List `DONE`; SCR-025's only listed dependency is Organization List). Built as a real, dynamic screen: `OrganizationDetailsScreen.jsx` + `useOrganizationDetails.js` (React Query, 30s refetch, route `id` via `useParams`, `enabled: Boolean(orgId)`) + `organizationDetails.api.js` (real-fetch-first → `res.ok`+content-type-guarded mock fallback with `encodeURIComponent(orgId)`), wired at `/organizations/:id` in `router.jsx`. The SCR-024 list row "View" was upgraded from a disabled button to a real `Link` navigating to the encoded detail route.
- **FIGMA FIDELITY = `verified` (agent-rules.md §2):** node `91:9827` was inspected live this session via `get_design_context` + `get_screenshot`. Every section is transcribed from the actual frame: page header (avatar, name, Active status pill, ID/Plan/Region/Created/Admin metadata, Edit / Manage Subscription / More Actions), the 6 KPIs (Active Users 284, Active Pipelines 142, Success Rate 98.6%, Connector Health 44/46, Storage 1.8 TB, License Utilization 94.7%), Organization Overview, Organization Info, Subscription & Licensing (both meter bars), Pipeline Activity (stats + table), Platform Health, Users table, Quick Actions, Connectors (summary + table), Security Status, and the 7-item Recent Audit Activity timeline.
- **FIGMA-SOURCE CONFLICT (agent-rules.md §3):** the org id reads `org_8kx2m9q` on this frame vs. `org_8kx2n9q` in the SCR-024 list mock (differ by one character — likely a Figma typo). Reconciled by having the detail mock **echo the route's own id** rather than hardcoding either literal, so list→detail navigation stays coherent regardless of which id the row carries; documented in `organizationDetails.api.js`'s header rather than silently picking one.
- **LAYOUT (user follow-up — right sidebar clipped / global horizontal scroll):** the two-column grid initially used `xl:grid-cols-[1fr_360px]`; the `1fr` track floored at the tables' min-content width and pushed the grid past the AppShell content area, causing app-wide horizontal scroll. Fixed to `xl:grid-cols-[minmax(0,1fr)_360px]` + `min-w-0` on both column wrappers so tracks shrink to fit the shell; tables keep local `overflow-x-auto`. Renders entirely inside `AppShell` with no page-level overflow.
- MOCK BOUNDARY: MOD-004's backend is still `PLANNED` (same as SCR-024). "Sample data" badge in the page header discloses the boundary; all backend-dependent actions (header Edit / Manage Subscription / More Actions, Quick Actions, per-row actions) are disabled with explanatory `title` attributes naming MOD-004. The mock is a single fixed snapshot.
- ui-reviewer PASS (no blockers). Applied fix: the Organization Overview "Current Status" pill is now data-driven off `overview.currentStatus` via `STATUS_TONE` (was hardcoded green — would misrepresent a Suspended/Expired org). Remaining findings (catch-all fallback makes the error/Retry branch unreachable; disabled-action rationale only in `title`; 9px labels; other hardcoded cosmetic tones) are inherited-by-design from the SCR-024 pattern of record and deferred to project-wide passes per agent-rules.md §9. See `docs/reviews/review-log.md`.

### SCR-026 build notes (2026-08-24)
- Selected as the next eligible MOD-004 screen after SCR-025 (`DONE`); SCR-026's only listed dependency is Organization List (SCR-024, `DONE`). Built as a real, dynamic, controlled form: `CreateOrganizationScreen.jsx` (route `/organizations/new`, registered in `router.jsx` **before** `/organizations/:id` so the literal segment isn't captured by the param route) + `createOrganization.api.js` (design-sourced option/plan constants + a real-POST-first/`res.ok`+content-type-guarded provisioning call). The SCR-024 list "Create Organization" button was upgraded from a disabled button to a real `<Link to="/organizations/new">`.
- **FIGMA FIDELITY = `verified` (agent-rules.md §2):** node `92:2` (content `92:6`, dialog `92:1477`) was inspected live this session via `get_design_context` + `get_screenshot`. All 6 form sections (Organization Information, Regional Settings, Primary Administrator, Subscription & Licensing, Security Configuration, Default Platform Configuration), the option lists, the plan feature catalog, the live Organization Summary, the 7-item Validation Status checklist, the Security Checklist, Quick Help, the sticky action bar, and the Confirm Organization Creation dialog are transcribed from the actual frame's text nodes.
- **DYNAMIC BEHAVIOR (agent-rules.md §5):** fully controlled form with client-side `validate()` (required fields, email pattern, numeric seat count), blur/submit-gated inline errors (`aria-invalid`/`aria-required`/`aria-describedby`, `role="alert"`), auto-generated org code slug from the name (until user-overridden), plan selection seeding default seat/storage limits, a live-updating Organization Summary, a live Validation Status progress meter (`role="progressbar"`), and a live Security Checklist. Submit opens the confirm dialog (focus-trapped, Escape-to-close, focus restored to trigger — mirrors `UserProfileMenu`).
- **MOCK BOUNDARY (agent-rules.md §5):** MOD-004 has no provisioning backend (`PLANNED`). `createOrganization` attempts a real POST `/organizations` first and only falls back to a simulated `org_…` id/slug flagged `mocked:true` when unreachable (same shape as the read-side MOD-004 siblings). On a mocked result the screen shows a `role="alert"` disclosure banner and does **not** navigate; on a genuine success it routes to the detail screen. "Save as Draft" stays disabled with a `title` naming MOD-004 (consistent with SCR-024's disabled-action convention).
- ui-reviewer verdict FAIL→addressed: applied confirm-dialog keyboard a11y (focus trap + Escape + focus restore), `aria-required` on required inputs/selects, `accent-primary`+focus-visible on the trial checkbox and API-access radios, `bg-overlay-scrim` token for the dialog scrim (was `bg-black/40`), `bg-surface-card` toggle knob (was `bg-white`), and removed the dead `submitState` prop on `Header`. Deferred (inherited-by-design per §9): disabled "Save as Draft" `title` is mouse-only (project-wide unfocusable-disabled-action limitation, same as SCR-024); `text-white` on primary buttons matches org/dashboard sibling precedent. tester PASS (lint + build clean; no test/type-check tooling configured in the repo). See `docs/reviews/review-log.md`.

### SCR-027 build notes (2026-08-24)
- Selected as the next eligible MOD-004 screen after SCR-026 (`DONE`); SCR-027's only listed dependency is Organization Details (SCR-025, `DONE`). Built as a real, dynamic edit form: `EditOrganizationScreen.jsx` (route `/organizations/:id/edit`, registered in `router.jsx` **before** `/organizations/:id` so the literal `edit` segment isn't captured by the param route) + `editOrganization.api.js` (design-sourced saved baseline + real-GET-first `getEditableOrganization` and real-PATCH-first `updateOrganization`, both `res.ok`+content-type-guarded) + `useEditableOrganization.js` (React Query hook with `staleTime: Infinity` / no refetch — an edit form must not clobber in-progress edits). The SCR-025 "Edit Organization" header button was upgraded from disabled to a real navigation to this route.
- **FIGMA FIDELITY = `verified` (agent-rules.md §2):** node `93:1566` ("EditOrganizationScreen", inside the misnamed top-level frame `93:1562` "Static webpage design" — preserved, not renamed, per §3) was inspected live this session via `get_design_context` (persisted, 133KB) + `get_screenshot`; all 242 text nodes were transcribed. All 6 sections (Organization Information, Regional Settings, Primary Administrator, Subscription & Licensing, Security Configuration, Default Platform Configuration), read-only fields (Organization Code "Cannot be changed after creation.", Renewal Date, Trial Expiration), the "Modified" badges, "Previously: 300 seats" hint, license/storage usage bars, the Organization Summary / Change Summary / Validation Status (5/6) / Recent Activity right-rail cards, the sticky action bar, and the Save Changes confirm dialog are transcribed from the actual frame.
- **DESIGN-INTENT DECISION (agent-rules.md §3):** the Figma frame depicts the org **mid-edit** (3 changes already applied: name Acme Corp→Acme Corporation, license 300→500, SSO off→on). A real edit screen must load the **saved** record and derive dirty-state from the user's edits — so the implementation treats the design's "Previously"/Change-Summary values as the saved baseline and computes the "Modified" badges, the unsaved-changes counter, and the Change Summary diff card **live** by diffing `form` against `baseline.form`. This is documented in `editOrganization.api.js` rather than seeding a fake dirty state.
- **DYNAMIC BEHAVIOR (agent-rules.md §5):** controlled form with loading skeleton, error+retry (React Query), client-side `validate()` (required fields, email pattern, numeric seat count) with blur/submit-gated inline errors (`aria-invalid`/`aria-required`/`aria-describedby`, `role="alert"`); live dirty tracking; live Change Summary (before→after diffs); live 6-item Validation Status meter (`role="progressbar"`); read-only fields rendered non-editable; Reset Changes restores the baseline; Cancel with unsaved changes prompts a `window.confirm` guard; Save Changes is disabled unless valid **and** dirty; submit opens the focus-trapped Save-confirm dialog (Escape-to-close, focus restore — mirrors SCR-026 / `UserProfileMenu`).
- **MOCK BOUNDARY (agent-rules.md §5):** MOD-004 has no update backend (`PLANNED`). `getEditableOrganization` attempts a real GET `/organizations/:id/edit` first and falls back to the design baseline flagged `mocked:true` (→ "Sample data" badge); `updateOrganization` attempts a real PATCH `/organizations/:id` and only sends the **changed** keys, falling back to a simulated success flagged `mocked:true` (→ `role="alert"` simulated-save banner, no navigation). On a genuine save it routes back to the detail screen. Administrator lifecycle actions (Change Administrator / Send Password Reset / Resend Invitation) and Save Draft stay disabled with `title`s naming the blocking module (MOD-005 / MOD-002 / MOD-004), consistent with SCR-024/025/026.
- Quality gates: `oxlint` clean, `vite build` clean; `graphify update .` run. ui-reviewer + tester verdicts recorded in `docs/reviews/review-log.md`.

## Figma Notes

### SCR-032 build notes (2026-08-26)
- Selected as the next eligible screen after SCR-031 (all of MOD-001's shell screens + SCR-014–SCR-031 `DONE`; SCR-032 was flagged `IN_PROGRESS` in the tracker with a full implementation already on disk — screen + drawer, hook, mock-fallback API, route, and nav — but never carried through the quality gate to `DONE`). This is the **first MOD-005 (User Management) screen**. Selection respects MOD-005's dependency on Auth (SCR-032 lists `Auth` as its only screen-level dependency; MOD-002/MOD-003 backends remain `PLANNED` — a mock boundary, not a build blocker for a frontend list screen).
- Files: `UserListScreen.jsx` (screen + `UsersTable` + `UserDrawer` + KPI grid + license banner + screen footer + inline icons), `useUserList.js` (React Query, 30s refetch, org-scoped + filters in query key), `userList.api.js` (real-fetch-first → `res.ok` + content-type-guarded `mocked:true` fallback). Route `/users` in `router.jsx`; "Users" nav item in `navConfig.js` gated on `USER_MANAGE`.
- **FIGMA FIDELITY = `verified` (per userList.api.js header):** node `101:4147` was inspected via the Figma MCP during the original build session (`get_design_context` + `get_screenshot`). The 6 KPI cards (Total Users 247, Active 219, Pending Invitations 12, Administrators 8, License Utilization 91% / 27 of 300 remaining, Inactive 16), the license-threshold warning banner, the toolbar filters (search + Role / Department / Team / Status / Account Type / Last Login + sort), the bulk-action set, the 16 visible user rows (name, title, email, role, department/team, status pill, last login, MFA/SSO badges), the "Showing 16 of 247 users" footer, and the Sarah Chen `UserDrawer` (node `101:5921`) are transcribed from the frame's text nodes. KNOWN LIMITATION: only the 16 visible rows exist in the design (the "of 247" total is verified but the remaining rows are not designed); the detailed drawer breakdown is Figma-verified for Sarah Chen only — other users expose real row-level fields + a labeled MOD-005 boundary note.
- **DYNAMIC BEHAVIOR (agent-rules.md §5):** loading skeleton, error+retry (React Query), empty state with Clear-all, real client-side search + 6 filters + 5 sort options (`useState`/`useMemo`; every query change resets to page 1 and clears selection), active-filter chips, page-scoped bulk-selection with indeterminate select-all, per-row focus-trapped `UserDrawer` (Escape-to-close, focus restore), `sr-only` live region, and a live screen footer.
- **MOCK BOUNDARY (agent-rules.md §5):** MOD-005 has no backend (`PLANNED`) — no `User` entity or user list/invite/suspend/activate/assign endpoints; user creation is invitation-driven (agent-rules.md §10.1) so the primary action is "Invite User". "Sample data" badge (header + footer + drawer) discloses the boundary; all backend-dependent actions (Import/Export/Invite User, the 8-item bulk bar, the 4 drawer lifecycle actions, Manage Licenses) are disabled with explanatory `title`s naming the blocking module. The mock is a single fixed snapshot; the "Never" last-login sentinel uses `loginMinutes = Number.MAX_SAFE_INTEGER`, handled explicitly.
- **Quality gate — ui-reviewer found + fixed (in-scope), tester PASS:** the screen used three utility classes that don't compile against this project's token config — `text-on-primary` (the real token is `text-text-on-primary`), and the alpha modifiers `bg-warning/20` and `bg-primary/10` (Tailwind doesn't generate alpha variants for the CSS-variable color tokens, so these emitted no rule and rendered transparent). Fixed: Invite User button → `text-text-on-primary`; license-banner icon chip → solid `bg-warning text-text-on-primary`; filter-chip remove-button hover → `hover:bg-shell-accent-border`. Verified each replacement compiles into `dist` CSS. The identical `text-on-primary`/`bg-primary/10` spots in the SCR-024 sibling (`OrganizationListScreen.jsx`) are left as-is to preserve sibling parity and avoid out-of-scope edits (agent-rules.md §9) — recorded as an Open Issue for a project-wide token-hygiene pass. tester: `oxlint` clean on the unit (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings), `vite build` clean, `/users` + `/organizations/current/users` serve the SPA shell (200 HTML), mock-fallback content-type guard verified live, and full data-contract audit confirmed every field/filter/sort maps to a real field on all 16 rows (no SCR-022/SCR-029-class bug). `graphify update .` run.
- **Tooling gap (documented, not a failure):** the ui-reviewer subagent terminated on an infrastructure API error (quota) after diagnosing the token findings but before writing the fixes; the builder applied and verified the fixes it had identified. No browser-automation dependency is installed in `frontend/`, so interactive behavior (filter chips, drawer focus trap, pagination, live-region announcements) was verified by static review + build/lint/data-contract, not automated DOM testing — same class of gap as SCR-005–031.

### SCR-033 build notes (2026-08-26)
- Selected as the next eligible screen after SCR-032 (`DONE`). Second MOD-005 (User Management) screen. This is the admin-invite user-provisioning form: user creation is invitation-driven, not self-registration (agent-rules.md §10.1), so the primary action is "Send Invitation", not "Create User".
- Files: `AddUserScreen.jsx` (screen — 6 numbered form sections [Basic Information, Organization Assignment, Role & Permissions, License Assignment, Authentication & Security, Notifications] + right rail [User Summary, License Availability meter, Validation Status, Security Checklist, Quick Help] + header + sticky ActionBar + nested focus-trapped `ConfirmDialog` + inline `currentColor` SVG icons), `addUser.api.js` (design-sourced option/constant sets + `inviteUser` real-fetch-first → content-type-guarded `mocked:true` fallback). Route `/users/new` in `router.jsx`; reached via the now-enabled "Invite User" button on `UserListScreen.jsx`.
- **FIGMA FIDELITY = `verified` (per addUser.api.js header):** node `103:6282` was inspected via the Figma MCP during the build (`get_design_context` + `get_screenshot`). The six numbered form sections, the "Employee ID already in use" validation state (`TAKEN_EMPLOYEE_IDS`), the per-role access-scope callout (`ROLE_ACCESS_SCOPE`), the license-availability banner (273/300 assigned, 27 remaining, 90% threshold — `LICENSE_POOL`), the Feature Access grid (5 included/4 excluded for the Engineer profile — `FEATURE_ACCESS_BY_LICENSE`), the right-rail cards, the Cancel/Save Draft/Send Invitation header actions, and the Confirm Invitation dialog ("What happens next" info box, node `103:7455`) are transcribed from the frame's text nodes.
- **DYNAMIC BEHAVIOR (agent-rules.md §5):** fully controlled form (`setField`), live `validate()` + 6-item `computeChecklist()` (the design's "Employee ID unique" item fails when the entered ID is in `TAKEN_EMPLOYEE_IDS`), field-level `showError()`, bounded CIDR-list validation for the IP allowlist, license-availability meter reacting to license-type selection, role→scope-copy + license→feature-matrix reactivity, submit opens the focus-trapped Confirm dialog (Escape + Tab cycling + focus restore), confirm calls `inviteUser`. On a mocked success the screen shows a warning banner and does NOT navigate (nothing was persisted); a real success navigates to `/users`. Post-mock, the Send Invitation button is disabled and Cancel becomes "Back to Users" so the simulated invite can't be re-submitted.
- **MOCK BOUNDARY (agent-rules.md §5):** MOD-005 has no backend (`PLANNED`) — no `User` entity and no invite/provision endpoint. `inviteUser(orgId, payload)` attempts a real org-scoped POST first (`/organizations/${orgId}/users`, `encodeURIComponent`) and falls back to a simulated `{ id: 'usr_…', status: 'invited', mocked: true }` only on failure/non-JSON (same content-type guard as the SCR-014 HTML-fallback defense and the SCR-026 sibling). Save Draft is disabled (needs the same PLANNED endpoint) with an explanatory `title`. The design-sourced select options / role scopes / feature matrix / license pool / taken-ID set are exported constants, not fetched, because SCR-033 has no live directory endpoint — a real MOD-005 backend would serve them per org.
- **Quality gate — ui-reviewer PASS (2 medium + 4 low, no blockers), tester PASS.** In-scope fixes applied: (#1 medium) license-count contradiction — sidebar "Available" now reads `LICENSE_POOL.remaining` (27) so the 91%-used bar and the text agree, while `remainingAfter` (26) stays only in Section 4's "after this assignment" banner; (#2 medium) meaningful content moved off the 9px `--text-xs` (decorative-only per the SCR-001 rule) — bulk `text-token-xs`→`text-token-meta`; (#3 low) toggle description associated via `id`+`aria-describedby`; (#4 low) loose IP-allowlist regex replaced with a bounded octet/prefix CIDR-list pattern; (#6 low) post-mock re-submission guarded — `Header`/`ActionBar` now take a `submitted` prop and disable Send Invitation (change `disabled={!isValid}` → `disabled={!isValid || submitted}`). (#5 low) "Additional Roles" single-select-vs-array shape flagged by the reviewer "for design check" — the reviewer could not access Figma to confirm, so it is recorded as a **design-verification item**, not a code defect; the field keeps its current shape pending a Figma re-check. tester PASS earlier in the build: `oxlint` clean on the unit (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings), `vite build` clean, no secrets in `addUser.api.js`, mock-fallback pattern correct.
- **Re-verification after reviewer fixes:** `npm run lint` clean (same two pre-existing warnings only), `npm run build` clean (299 modules), `graphify update .` run.
- **Tooling gap (documented, not a failure):** the ui-reviewer could not independently reach the Figma MCP in its own session, so pixel fidelity was assessed against the code + the `addUser.api.js` FIGMA VERIFICATION comment (which records the build-session MCP transcription). No browser-automation dependency is installed in `frontend/`, so interactive behavior (validation, focus trap, license-meter reactivity, post-mock guard) was verified by static review + build/lint, not automated DOM testing — same class of gap as SCR-005–032.

## Figma Notes

### Design system
- Typography: Two font families used consistently across the file — `Inter` (body/UI text, weights Regular/Medium/Semi Bold) and `JetBrains Mono` (uppercase labels, metadata/system strings like "PRODUCTION", "TLS 1.3 · v7.2.1", timestamps — Regular/Medium). No Figma text styles or variables are bound (`get_variable_defs` returned empty on every sampled node); type sizes/weights are hard-coded per instance (e.g. Heading 1 ≈ 16px/Semi Bold, Body ≈ 12.5–13.5px/Regular, micro-labels ≈ 8.5–11px).
- Color system: No Figma color variables/styles are bound to the file (`get_libraries` shows zero libraries added; `get_variable_defs` returned `{}`). Colors are hard-coded hex values repeated consistently: primary/brand `#0f5699`, dark text `#0f172a` / `#111827`, secondary text `#64748b` / `#6b7280` / `#94a3b8` / `#9ca3af`, borders `#e2e8f0` / `#f1f5f9`, success `#16a34a` / `#15803d` / `#f0fdf4`, error `#dc2626` / `#b91c1c`, warning `#d97706` / `#fffbeb`, neutral surfaces `#fafbfc` / `#f3f5f7` / `#eceef2`. Since there are no bound variables, a future implementation should define these as design tokens (CSS variables / Tailwind theme) rather than re-hardcoding hex values.
- Spacing: No spacing variables bound. Observed spacing is consistent with a small base unit (multiples of ~4px: 4/6/8/12/16/20/24/28/36px commonly recur), and card corner radii are small (3–6px).
- Components: The file does not use native Figma components/variants (`instance` nodes exist — 471 — but no `component`/`component_set` nodes were found in the traversed metadata, and no design-system libraries are attached). Repeated UI patterns (buttons, form fields, dialogs, drawers, banners, cards) are built as plain frames with consistent naming conventions ("Button", "Field", "Label", "Container", "ConfirmDialog", "DetailDrawer") rather than reusable Figma components. A future frontend implementation will need to define its own component library since Figma doesn't provide one.
- Icons/assets: Icons are SVG vectors (209 `vector` nodes) referenced by name, e.g. `EyeIcon`, `BuildingIcon`, "Image - Meridian logomark". No icon library/kit is attached to the file.
- Layout: Screens are desktop-only. Frame widths vary by section — early auth screens are sized ~911px (component/mockup scale), while the main application (dashboards, org/user management, data sources, pipelines) is sized 1315–2218px (full desktop viewport). No tablet or mobile breakpoint frames exist anywhere in the file.

### Conflicts / ambiguities
- SCR-027 "Edit Organization Screen": the top-level Figma frame is named **"Static webpage design"** (`93:1562`), which does not match its purpose. Its inner content frame is correctly named `EditOrganizationScreen` (`93:1566`). Documented here per agent-rules.md §3 rather than silently renamed; the Figma source should be corrected by the design owner, but this audit preserves the exact top-level frame name/ID as found.
- Five screen names contain typos in the Figma source (preserved verbatim, not normalized, per `docs/figma-mcp.md`): "Forget Password **Scren**" (`16:4495`), "Performance Analytics Dashboard **Screeb**" (`70:43606`), "Database Connector Setup **Scren**" (`116:31212`), "**Pipline** List Screen" (`146:6124`), "Source Node Configuration **Screem**" (`155:2137`).
- SCR-008 "Logout Confirmation Screen" contains two sibling `App` root frames (`20:5461` and `20:5675`) inside one top-level frame — likely a before/after (confirm dialog → confirmed) state pair, but this could not be confirmed without further visual inspection; flagged as an ambiguity rather than assumed.
- "Add Destination Screen" (SCR-058) contains a `div.fixed` sub-frame (`130:2100`) suggesting a modal overlay variant; not independently verified as a distinct state beyond the metadata structure.
- No Figma prototype/interaction (click-through flow) data was inspected in this pass — `get_metadata` exposes structural hierarchy only, not prototype connections. If flow-order between screens matters for module planning, re-run a prototype-focused inspection through Figma MCP before finalizing the module plan.

### SCR-002 build notes (2026-08-22)
- Two duplicate top-level `App` instances exist under this frame (`14:4055...` and `16:4949...`), pixel-identical in the design-context dump — treated as a duplicate/redundant Figma layer, not a second state, since the Screen Matrix lists only one state set for this screen (Default/SSO/toggle/keep-signed-in) and both instances render the same content.
- "Continue with SSO" is rendered per the Figma design but shipped **disabled** with an explanatory `title` — no SSO provider integration exists in any module (MOD-002's spec lists it as a Login variant but no SSO endpoint/flow is defined in `agent.md` or the module plan). Do not wire a fake success path per agent-rules.md §5 rule against fabricated backend behavior.
- No error/loading Figma state exists for this screen; error state (invalid credentials, network failure) was implemented per agent-rules.md §5 general requirement, not from a Figma variant — same extrapolation precedent as SCR-001's retry state.
- MOCK BOUNDARY: `frontend/src/features/auth/authApi.ts` attempts a real `POST /api/auth/login` first and only falls back to a mocked success when the request is unreachable (no MOD-002 backend deployed yet); malformed 2xx responses and 400/401 are treated as real errors, never silently mocked.

### SCR-003 build notes (2026-08-22)
- **Confirmed naming/content swap** (escalated to user before building, per agent-rules.md §3 — decision recorded, not silently resolved): node `14:4162` (inventory-labeled "Reset Password Screen") actually renders the email-entry "Forgot password?" request form; node `16:4495` (inventory-labeled "Forget Password Scren", SCR-004) actually renders the token-based "Reset your password" form with a password-strength meter and requirements checklist. User explicitly chose to keep SCR-003 = node `14:4162` at route `/reset-password`, per inventory order, rather than swap the IDs/routes to match content. SCR-004 (still `DISCOVERED`) will need the same explicit-decision treatment when it's built next, since its real content is the token-based reset form.
- Sent-confirmation state (`role="status"`, focus moved to the confirmation heading on transition) added beyond Figma per agent-rules.md §5's success-feedback requirement — same extrapolation precedent as SCR-001/SCR-002.
- MOCK BOUNDARY: `frontend/src/features/auth/passwordResetApi.js` attempts a real `POST /api/auth/forgot-password` first, falling back to a mocked success only when the endpoint is unreachable (no MOD-002 backend deployed yet) — mirrors authApi.js's `login()` pattern. A 2xx response carries no body to validate (endpoint should not leak account existence via response shape).
- Extracted `frontend/src/features/auth/formStyles.js` (shared `inputBase` Tailwind class string) out of `LoginScreen.jsx` and reused it here, after UI review flagged the identical literal duplicated across both files — both screens now import from the shared constant.
- Fixed a UI-review-flagged navigation bug: `LoginScreen.jsx`'s "Forgot password?" button previously routed to `/forgot-password` (the still-unbuilt SCR-004 placeholder) instead of `/reset-password` where this screen now lives; repointed to `/reset-password`.
- Fixed a UI-review-flagged state bug: the `passwordReset` Redux slice is app-level (not screen-scoped), so a prior visit's `sent`/`error` status could survive into a fresh mount with local `email` state already cleared, showing a broken confirmation message. `useResetPasswordRequest.js` now dispatches `resetRequestReset()` on unmount.

### SCR-004 build notes (2026-08-22)
- Confirms the SCR-003 build notes' documented content swap: node `16:4495` (inventory-labeled "Forget Password Scren") renders the token-based "Reset your password" form (new password + confirm password, live requirements checklist, strength meter, "Reset Password" submit) — not a second email-entry form. Per the same explicit product decision recorded at SCR-003, this screen keeps its inventory ID (SCR-004) and route (`/forgot-password`) while rendering node `16:4495`'s real content.
- The reset token arrives as a `?token=` query param on the route (the value the forgot-password email link would carry). No Figma variant models a missing/invalid token; the guard rendering "Reset link invalid or expired" was added per agent-rules.md §5's real-error-state requirement — same extrapolation precedent as SCR-001/002/003's added states. A done-confirmation state (`role="status"`, focus moved to its heading) was added for the same reason, mirroring SCR-003's sent-confirmation pattern.
- Strength meter and requirements checklist share one evaluation (`usePasswordStrength.js`) so they can never disagree with each other. On a failed submit, the still-unmet checklist rows are highlighted in the danger color to tie the generic banner message to the specific missing requirement(s), rather than leaving the user to compare a vague error against a plain list.
- MOCK BOUNDARY: `frontend/src/features/auth/passwordResetConfirmApi.js` attempts a real `POST /api/auth/reset-password` first, falling back to a mocked success only when the endpoint is unreachable (no MOD-002 backend deployed yet) — mirrors passwordResetApi.js's `requestPasswordReset()` pattern. **Known limitation**: without a real backend, the mock fallback cannot genuinely validate a token — it treats any token shorter than 16 characters as invalid/expired (to exercise the error UI) and anything longer as a mocked success. This is a heuristic for demonstrating the error path in mock mode only, not real token validation; must be replaced once MOD-002's backend is live.
- UI review flagged that the missing-token guard originally covered only the "no token at all" case, not "token present but invalid" — the mock's invalid-token detection was broadened (see limitation above) so the same guard now demonstrates both. UI review also flagged inconsistent focus management between the `done` and missing-token terminal states (only `done` moved focus to its heading) — fixed so both states move focus to their heading on entry, matching SCR-003's precedent.

### SCR-005 build notes (2026-08-22)
- Figma only models the "pending" happy-path state (masked email badge, resend button, resend-cooldown countdown text) at node `18:5023`. No verify-code entry field exists in the design — verification is link-based (the user clicks a link in the sent email), matching MOD-002's spec ("verify-email (send/confirm)" endpoints).
- Two states beyond the Figma pending state were added per agent-rules.md §5's real-error/success-feedback requirement, same extrapolation precedent as SCR-001–SCR-004's added states: a `verified` success panel (reached when a `?token=` query param — the value the emailed verification link would carry, same URL-param convention as SCR-004's `?token=`) resolves successfully, and an "invalid or expired" error panel (bad/expired token). A transient "Verifying your email…" state covers the async confirm request in between.
- The masked email (`u••••@company.com`) is read from a `?email=` query param, since no Figma variant or module-plan contract defines how this screen would otherwise learn the address; the inviting flow (post-login unverified-account gate, per MOD-002's admin-invitation user-creation model) is expected to pass it. Documented as an extrapolation, not asserted as a real API contract.
- MOCK BOUNDARY: `frontend/src/features/auth/emailVerificationApi.js` attempts real `POST /api/auth/verify-email/resend` and `POST /api/auth/verify-email/confirm` requests first, falling back to a mocked outcome only when the endpoint is unreachable (no MOD-002 backend deployed yet) — mirrors the rest of the auth feature's mock-fallback shape. Same heuristic limitation as SCR-004's passwordResetConfirmApi.js: in mock mode, a token shorter than 16 characters is treated as invalid/expired to exercise the error UI; not real token validation.
- UI review flagged and fixed: the "Verifying…" transient state had no heading/focus target (inconsistent with every other async-transition state on this screen and its siblings) — added an `<h1>` with the same focus-on-mount pattern used by the `verified`/error states. Also flagged and fixed: the resend-failure error banner was placed below the "Didn't receive the email?" sub-heading instead of at the top of the content block where every sibling screen places its error banner — moved to match convention. Also removed a stale `aria-live="polite"` on the static masked-email badge (content never changes after mount, so the live region announced nothing) and softened the `maskEmail` helper's doc comment from a fidelity claim ("matching the Figma mask") to an approximation, since Figma's one captured sample doesn't define a dot-count rule across different email lengths.

### SCR-006 build notes (2026-08-22)
- Figma models only the default code-entry state at node `19:5127`: a 6-digit OTP split into two groups of three (dash separator), "Authenticator app" method badge, resend-cooldown text, and a "trust this device" checkbox. An inline error-banner state was added beyond Figma per agent-rules.md §5's real-error requirement — same extrapolation precedent as SCR-001 through SCR-005's added states.
- "Use another method" has no corresponding screen anywhere in the 71-screen Figma inventory (only one verification-method badge, "Authenticator app", exists in the whole file) and MOD-002's spec does not define alternate 2FA delivery channels. Rendered disabled with an explanatory `title`, mirroring LoginScreen's disabled "Continue with SSO" precedent, rather than fabricating a destination screen per agent-rules.md §5.
- The account/session this 2FA challenge belongs to is not re-collected on this screen — assumed to live server-side via a short-lived challenge token set by the prior login response, since no Figma variant or module contract defines how this screen would otherwise identify the in-flight login. Documented as an extrapolation, not a confirmed API contract, same precedent as SCR-005's `?email=` query-param note.
- MOCK BOUNDARY: `frontend/src/features/auth/twoFactorApi.js` attempts real `POST /api/auth/2fa/verify` and `POST /api/auth/2fa/resend` requests first, falling back to a mocked outcome only when the endpoint is unreachable (no MOD-002 backend deployed yet) — mirrors the rest of the auth feature's mock-fallback shape. In mock mode, any 6-digit numeric code is treated as valid (no real backend to check against); not real code validation.
- UI review (`docs/reviews/review-log.md`) flagged and fixed several accessibility gaps before verification: the OTP cell group had no accessible group name (added `role="group"` + `aria-labelledby`); OTP cells didn't expose their error state to assistive tech (added `aria-invalid`/`aria-describedby`); verify failure didn't move focus to the error banner (added a focus-managed `role="alert"` banner, mirroring EmailVerificationScreen's heading-focus precedent); the resend button's status text had no live region (added `aria-live="polite"`); the "code resent" confirmation incorrectly disappeared when the cooldown reached zero (removed the cooldown gate to match EmailVerificationScreen's persistence). The review's note that `disabled`+`title` on "Use another method" (and LoginScreen's SSO button) offers keyboard/screen-reader users no explanation at all was left unresolved as a pre-existing, shared-precedent limitation, not introduced by this screen — flagged for a future accessibility pass across both screens rather than fixed in scope here.
- Per agent-rules.md §2, the UI review's Figma MCP call did not resolve in that agent's session, so pixel-level fidelity to node 19:5127 was reviewed via code/screenshot comparison in this build (design context + screenshot were fetched directly during implementation) but not independently re-verified by the reviewer agent — recorded here as a known gap in the review's coverage, not a fidelity failure.

### SCR-007 build notes (2026-08-22)
- Figma frame `19:5237` renders inside the full authenticated app shell (top header + settings side-nav), but MOD-001 (Shell/Bootstrap) has not built the real shared `AppShell`/`Sidebar`/`Header` yet — SCR-009/010/011 are still `PLANNED`. Escalated to the user before building, per agent-rules.md §3/§9 (real missing prerequisite, not invented): user explicitly chose to build a temporary local layout scaffold (`frontend/src/features/settings/SettingsShellScaffold.jsx`) reproducing just enough shell chrome for this screen, rather than blocking on MOD-001 or building the real shell out of order. The scaffold is documented as temporary and must be replaced once MOD-001 ships the real AppShell.
- Current-password-incorrect and confirm-mismatch error states are modeled directly in Figma (node `19:5237`'s error variants); a done-confirmation state was added beyond Figma per agent-rules.md §5's success-feedback requirement, same extrapolation precedent as SCR-001–006.
- Reused `PASSWORD_REQUIREMENTS`/`evaluatePasswordStrength` from SCR-004's `usePasswordStrength.js` rather than reimplementing — same requirement list and strength meter, now shared by both an authenticated (this screen) and unauthenticated (SCR-004) password-entry flow.
- MOCK BOUNDARY: `frontend/src/features/settings/changePasswordApi.js` attempts a real `POST /api/auth/change-password` first, falling back to a mocked success only when the endpoint is unreachable (no MOD-002 backend deployed yet) — mirrors the rest of the auth feature's mock-fallback shape. In mock mode, the literal current-password value `wrong-password` is treated as incorrect to exercise the Figma-specified error state; not real credential verification.
- UI review (`docs/reviews/review-log.md`) flagged and fixed: server-reported `errorField` targeting only wired up the `currentPassword` input's error styling/`aria-invalid`/`aria-describedby`, not `newPassword`/`confirmPassword` — extended to all three fields; real/meaningful error text was hardcoded at `text-[11.5px]` instead of the `--text-meta` token floor (same defect class as SCR-001/002) — moved to `text-token-meta`; `aria-describedby` on `current-password`/`confirm-new-password` pointed at a conditionally-absent element id in the no-error state — now conditional, matching the sibling-screen pattern. Two low-severity findings were deferred (not fixed, since the affected component is itself temporary): the scaffold's non-active nav labels have no `aria-disabled` cue, and its active-nav-item background uses an inline rgba() value instead of a token — both noted in `SettingsShellScaffold.jsx`'s doc comment for resolution if that pattern is ever carried into the real MOD-001 shell. A third low finding (three near-duplicate password show/hide field blocks) was deferred as a non-blocking maintainability note.
- Per agent-rules.md §2, the UI review's Figma MCP call did not resolve in that agent's session, so pixel-level fidelity to node 19:5237 was reviewed via code/screenshot comparison in this build (design context + screenshot were fetched directly during implementation) but not independently re-verified by the reviewer agent, who instead cross-checked against already-Figma-verified sibling screens — same known review-coverage gap recorded at SCR-005/006.

### SCR-008 build notes (2026-08-22)
- Confirms the documented ambiguity: node `20:5458` contains two sibling `App` instances (`20:5461` background, `20:5675` overlay) — `get_design_context` confirms `20:5461` renders the (unrelated) Change Password screen's full authenticated shell and `20:5675` is the actual "Log out?" confirm dialog on a `rgba(15,23,42,0.28)` dimmed backdrop. Per the same precedent as SCR-007 (MOD-001's shared AppShell still `PLANNED`), only the dialog is reproduced; the background shell bleed-through is incidental Figma-canvas composition, not this screen's design intent.
- A transient "signed out" confirmation state was added beyond the single static Figma state per agent-rules.md §5's success-feedback requirement — same extrapolation precedent as SCR-001–007's added states — since the dialog's own copy promises the user will be "returned to the login screen," so a silent redirect would contradict that promise on a slow network round-trip.
- The dimmed backdrop's exact color (`rgba(15,23,42,0.28)`) is now a real design token, `--color-overlay-scrim` in `frontend/src/styles/tokens.css`, sourced from the Figma overlay observed in this screen's `get_design_context` dump — not reused from `--color-decorative-muted` (that token is documented as decorative-accent-only, not a page-covering scrim).
- MOCK BOUNDARY: `frontend/src/features/auth/logoutApi.js` attempts a real `POST /api/auth/logout` first, falling back to a mocked acknowledgement only when the endpoint is unreachable (no MOD-002 backend deployed yet) — mirrors the rest of the auth feature's mock-fallback shape. Local session teardown (clearing both storages) always happens regardless of server outcome, since a failed server-side logout must not trap the user in the app; a non-2xx/non-401 server response is now logged via `console.warn` as a non-blocking diagnostic (mirroring `useLogin.js`'s mock-boundary logging precedent) rather than computing an unused `serverAcknowledged` signal.
- UI review (`docs/reviews/review-log.md`) failed the first pass and was fixed before verification: added `role="dialog"`/`aria-modal`/`aria-labelledby` and focus-on-mount to the confirm dialog (previously a plain unlabeled `div` with no focus management, unlike the `done` state which already moved focus); replaced fabricated `rounded-[10px]` and an ungoverned box-shadow literal (no matching token, no precedent anywhere else in the codebase) with the existing `rounded-sm` + `border-border` convention shared by every sibling auth card; replaced a `navigate(-1)`-only Cancel handler (unsafe if `/logout` is reached directly, e.g. bookmark/typed URL/new tab, with no prior in-app history) with a guarded fallback to `/dashboard`; added a `reset()`-on-mount call to `useLogout`, mirroring `useResetPasswordRequest.js`'s fix for the identical stale-app-level-slice-state defect class (previously, navigating back to `/logout` after a completed logout could render the "signed out" panel without a fresh confirmation).
- Per agent-rules.md §2: the UI review's own Figma MCP call did not resolve in that agent's session, so its findings on fidelity (button copy, dialog-only scope) were logged as `not-verified` there; this build's own `get_design_context` call (used during implementation, see above) independently confirms the two-instance structure and the overlay's backdrop color, closing that gap for the values that mattered most (the backdrop token). Full pixel-level fidelity beyond that (exact spacing/typography) was not re-diffed against a fresh screenshot after the review fixes; no visual regression is expected since the fixes only touched a11y attributes, class-name value swaps to existing tokens, and unrelated JS logic.

### SCR-009 build notes (2026-08-22)
- Figma frame `27:2238` renders only its "Main Content - Dashboard" region (node `27:2316`) — no sidebar/header nodes exist inside this frame. SCR-010 (Sidebar) and SCR-011 (Header) own that pixel design separately and are still `DISCOVERED`/unbuilt. Per the same precedent as SCR-007/SCR-008 (MOD-001's shared AppShell still `PLANNED`), built against a temporary local scaffold (`frontend/src/features/dashboard/AppShellScaffold.jsx`) reproducing just enough shell chrome (top bar + nav) rather than blocking on MOD-001. Must be replaced once MOD-001 ships the real AppShell/Sidebar/Header.
- Figma models only the happy-path populated state (4 KPI cards, pipeline activity table, system health panel, execution performance stats). Loading skeleton, error+retry, and empty-pipeline-list states were added beyond Figma per agent-rules.md §5's real-state requirement — same extrapolation precedent as SCR-001–008's added states.
- The 7-day execution trend chart region (Figma nodes past `27:2626`) renders only a static image in the design with no underlying data contract for an interactive chart library — rendered as a labeled placeholder note instead of a fabricated chart, since no charting library or time-series API exists yet (MOD-009 still `PLANNED`).
- MOCK BOUNDARY: `frontend/src/features/dashboard/dashboardApi.js` attempts a real `GET /api/dashboard/summary` first, falling back to a fixed mock snapshot (reproducing the literal Figma-shown numbers) only when the endpoint is unreachable — no MOD-008 (Pipeline Builder & Execution) or MOD-009 (Analytics/Monitoring Dashboards) backend exists yet. A non-OK response or malformed body is a real thrown error, never silently converted to a mock success. Since 100% of current traffic will be mocked until those modules ship, the screen surfaces a visible "Sample data" badge next to the page title whenever `mocked: true`, so users are never misled into treating placeholder numbers as live metrics.
- Server data is owned by React Query (`useDashboardSummary.js`, 30s `refetchInterval`), not Redux, per agent.md §4.1 — no Redux slice was added for this screen since there is no shared client-side state to hold, only server data.
- Per-row/per-section action buttons ("View", "Retry", "View all →", "View Details →", "View executions →") have no destination screen anywhere in the current 71-screen Figma inventory beyond this dashboard frame itself (pipeline detail, execution history, and analytics-detail screens are separate unbuilt MOD-008/MOD-009 scope) — rendered `disabled` with an explanatory `title`, mirroring LoginScreen's disabled "Continue with SSO" precedent, rather than fabricating a destination route.
- UI review (`docs/reviews/review-log.md`) flagged and fixed before verification: no `aria-live`/`role="status"` region announced loading/error/refresh-in-progress transitions to screen readers (added a visually-hidden live region reflecting the query's current phase); the six action buttons above had no `disabled`/`title` state despite doing nothing (fixed to match the Export button's existing disclosure pattern); the pipeline table's trailing actions column header had no screen-reader label (added `sr-only` "Actions" text); `AppShellScaffold.jsx`'s doc comment didn't carry forward the same "known deferred gap" caveat about inert nav items/untokenized active-state color that `SettingsShellScaffold.jsx` documents for the identical pattern (added). A duplicated-header-markup note between `AppShellScaffold.jsx` and `SettingsShellScaffold.jsx` was left unresolved as a low-priority, non-blocking observation, since both scaffolds are already scheduled for deletion once MOD-001 ships the real shell.
- Per agent-rules.md §2, the UI review's Figma MCP call did not resolve in that agent's session, so pixel-level fidelity to node 27:2238 was reviewed via code/screenshot comparison in this build (design context was fetched directly during implementation, confirming the real KPI/table/health-panel content and literal figures) but not independently re-verified by the reviewer agent — same known review-coverage gap recorded at SCR-005–008.

### SCR-012 build notes (2026-08-22)
- Figma node `36:5210` models the notification-bell dropdown as a shell overlay attached to Header (same "shell overlay" treatment as SCR-013's user menu, not a routed page). Built as a real, stateful `NotificationPanel` toggled from the shared `Header.jsx`'s bell button — since Header is shared/identical across every role per MOD-001's build notes, this panel is automatically available to every role without any role-specific wiring.
- Figma only models one populated "All" tab happy-path state. The tab-filter chrome (All/Unread/Alerts/System with per-tab counts) is real, client-side filtering over the fetched list — not a static reproduction of the four count badges shown in the design. Loading skeleton, error+retry, and per-tab empty-state copy were added beyond Figma per agent-rules.md §5's real-state requirement, same extrapolation precedent as SCR-001/SCR-009's added states.
- All 7 category/status icons (`IcoGear`, `IcoNotifFail`, `IcoNotifWarn`, `IcoNotifCheck`, `IcoNotifWorker`, `IcoDropShield`, `IcoDropSettings`) were downloaded as real Figma-exported assets to `frontend/src/assets/icons/notifications/` rather than hand-authored, per the figma-design-to-code skill's icon-fidelity rule.
- MOCK BOUNDARY: `frontend/src/features/notifications/services/notifications.api.js` attempts real `GET /notifications` and `POST /notifications/mark-all-read` requests first, falling back to a mock snapshot/no-op only when the endpoint is unreachable (no MOD-010 backend deployed yet) — mirrors the rest of the app's mock-fallback shape. The mock list reproduces the literal notifications shown in the Figma design as a fixed snapshot; a "Sample data" badge discloses this in the panel whenever `mocked: true`.
- The settings gear and "View all notifications →" link have no destination screen anywhere in the 71-screen Figma inventory — rendered `disabled` with an explanatory `title`, mirroring LoginScreen's disabled "Continue with SSO" precedent, rather than fabricating a destination.
- UI review (`docs/reviews/review-log.md`) failed the first pass and was fixed before verification: added focus-on-open and a real Tab focus trap to the `role="dialog"` panel (it previously left focus stranded on the bell button with no trap, contradicting SCR-008's focus-on-mount precedent); completed the filter tabs into a valid ARIA tabs pattern (`role="tabpanel"`, `aria-controls`/`aria-labelledby` pairing, roving tabindex, arrow-key navigation — previously `role="tab"`/`role="tablist"` were used with none of the required pairing); converted "Mark all read" to a true optimistic update with rollback-on-error (previously updated the cache in `onSuccess`, not `onMutate`, and had no failure feedback); replaced one overly broad `aria-live="polite"` region wrapping the entire scrollable list with a small dedicated status line, since the original re-announced large content chunks on every tab switch.
- Per agent-rules.md §2, the UI review's own Figma MCP call did not resolve in that agent's session, so pixel-level fidelity to node 36:5210 was reviewed via this build's own `get_design_context` capture (used during implementation) but not independently re-verified by the reviewer agent — same known review-coverage gap recorded at SCR-005–009/MOD-001.

### SCR-013 build notes (2026-08-22)
- Figma node `39:6447` ("User Profile Menu Screen") is a shell overlay whose styled content lives at its child `39:6826` ("Menu - User account menu") — fetched directly via `get_design_context`, since the parent node's `get_design_context` response exceeded the size limit and only returned sparse structural metadata. Built as a real, stateful `UserProfileMenu` toggled from the shared `Header.jsx`'s previously-inert avatar button — Header is shared/identical across every role per MOD-001's build notes, so this menu is automatically available to every role without role-specific wiring, mirroring SCR-012's Notification Panel precedent exactly (dialog semantics, focus-on-open, focus trap, Escape-to-close, click-outside-to-close).
- All 13 icons (`IcoDropUser`, `IcoDropSettings`, `IcoDropShield`, `IcoSliders`, `IcoBuilding`, `IcoExternalLink`, `IcoUsers`, `IcoCreditCard`, `IcoBook`, `IcoDropHelp`, `IcoMessageSquare`, `IcoFileText2`, `IcoDropLogout`) were downloaded as real Figma-exported assets to `frontend/src/assets/icons/profile-menu/`, per the figma-design-to-code skill's icon-fidelity rule — none were hand-authored.
- The avatar/org-badge two-stop gradient fill (`rgb(30,58,95)` → `rgb(15,86,153)`, confirmed directly from node `39:6826`'s design context) was added as a new `--gradient-shell-avatar` token in `tokens.css` / `bg-shell-avatar` Tailwind utility, rather than reusing `--color-shell-accent` (a flat fill) — no other shell element uses a gradient, so this is a screen-specific token, not a generalized one.
- MOCK BOUNDARY: "Switch Organization"/"Manage Organization"/"Billing" (with its literal "Pro" plan badge) reproduce the exact Figma-shown static content — no MOD-004 (Organization Management) backend exists yet to source a real organization list or plan tier — rendered `disabled` with an explanatory `title` rather than fabricating an org-switch flow, mirroring LoginScreen's disabled-SSO precedent.
- "Documentation", "Help Center", "Contact Support", and "Release Notes" have no destination anywhere in the 71-screen Figma inventory or the module plan (no external docs/help/support URLs are defined by any module) — rendered `disabled` with an explanatory `title` for the same reason, rather than linking to a fabricated URL.
- "Account Settings", "Preferences", and "Security" all route to `/account/change-password` (SCR-007, the only real account-settings screen built so far) — a deliberate consolidation, not a routing bug, since no separate settings/security/preferences screen exists in the inventory. The Figma "Alt+S" keyboard-shortcut hint next to "Security" is rendered as static decorative text only; this codebase has no keyboard-shortcut system to wire it to a real handler.
- "Sign Out" navigates to `/logout` (SCR-008's real confirm-before-logout screen) rather than calling `useLogout().confirm()` directly from the dropdown, so the user always sees the existing confirmation step rather than signing out instantly from a menu click.
- `frontend/src/features/shell/state/sessionSlice.js`'s mocked `currentUser` gained an `email` field (`jane.doe@acmecorp.io`) to populate the menu's header block — same mock-boundary precedent already documented in that slice for `name`/`initials`/`role`. `roleLabel` is now also passed from `AppShell.jsx` through `Header.jsx` into this menu, reusing the same `ROLE_LABELS` lookup `Sidebar.jsx` already relies on.
- UI review (`docs/reviews/review-log.md`) failed the first pass and was fixed before verification: the avatar/org-badge gradient was a hardcoded, untokenized inline style (`#1e3a5f`/`#0f5699`) duplicated twice — even though independently confirmed correct against Figma, `tokens.css`'s "do not re-hardcode hex values" rule requires it as a real token; fixed by adding `--gradient-shell-avatar`/`bg-shell-avatar`. Two low findings were left as documented, non-blocking notes: the open/focus-trap/Escape effect lives inside `UserProfileMenu.jsx` itself rather than centralized in `Header.jsx` like the bell pattern (behaviorally verified equivalent, an intentional encapsulation choice); disabled items communicate their reason via `disabled`+`title` only, the same pre-existing accessibility gap already tracked for SCR-006/SCR-012.

### SCR-031 build notes (2026-08-25)
- Built the Organization Activity Screen (node `99:2052`, drawer `99:3801`) as a real MOD-004 screen following the SCR-030 `TeamManagementScreen.jsx` pattern-of-record: `OrganizationActivityScreen.jsx` + `useOrganizationActivity.js` (React Query, 30s `refetchInterval`, org-scoped `queryKey`) + `organizationActivity.api.js` (real-fetch-first → content-type guard → `mocked:true` fallback). Route `/organizations/:id/activity` registered before the `/organizations/:id` catch-all in `router.jsx`, and `/organizations/current/activity` (the Administration → Audit Logs nav target) now renders the real screen instead of the placeholder.
- FIGMA VERIFICATION (Figma MCP available this session): the 6 KPI values/labels, saved-view tab set, filter toolbar, activity table columns (Time/ID/Event/Category/Performed By/Resource/Department/Result/Severity/Source), the inline expandable Event Timeline row, and the full EVT-0841 "Pipeline Execution Failed" drawer (Event Summary / Actor / Resource / Event Timeline / schema-mismatch error banner / Technical Metadata) are transcribed from the frame + drawer design context — `verified`. The masked source-IP octets (`10.0.4.●●●` / `203.0.113.●●`) are preserved exactly as the design masks them.
- DERIVED (labeled `Sample data`, not frame-verified): the individual event rows, their per-event timelines, and the KPI helper subtitles are internally-consistent sample data, not a one-to-one transcription of every row text node (the frame's own list is illustrative). The service doc comment was corrected after UI review to stop over-claiming row-level fidelity.
- Real dynamic behavior: client-side search + 7 filters (event type/resource/user/department/team/result/severity) + saved-view predicates + sort (newest/oldest/severity-rank/category) + pagination, all reset-to-first-page on query change; result/severity-toned active-filter chips; focus-trapped drawer (Escape/Tab-cycle/focus-restore); `sr-only` `role="status"` live region; loading skeleton; error+retry; empty state.
- MOCK BOUNDARY: MOD-004 has no backend deployed. Every backend-dependent action (Save Filter, Export Activity Log, Save current view, View Full Audit Record, View Resource/User, Copy ID, per-metadata copy) is `disabled` with a `title` naming the blocking PLANNED module (MOD-004; MOD-005 for View User). "Sample data" badges appear in the header, footer, and drawer when `mocked:true`.
- Quality gates: `npm run build` passes; `npm run lint` (oxlint) reports only the two pre-existing warnings (AppShell.jsx, SplashScreen.jsx) — zero new findings from SCR-031 files. UI review returned an initial FAIL on fidelity over-claims + minor gaps; in-scope fixes applied (softened the "verified" wording to separate verified structure from derived rows, aligned KPI helper subtitles to the frame, added the drawer Resource "Organization" row, toned active-filter chips by result/severity). Remaining reviewer notes (date-range/Columns toolbar controls, row-selection checkboxes, numbered pagination at rows-per-page 25, full ARIA tablist roving-tabindex) are deferred as backend-dependent or pattern-of-record-consistent (Prev/Next mirrors SCR-030) and recorded in `docs/reviews/review-log.md`.

### Missing or inaccessible Figma information
- No design tokens/variables are defined in Figma (confirmed via `get_variable_defs` returning `{}` on multiple sampled screens across different sections of the file, and `get_libraries` showing no libraries attached). Colors/typography/spacing exist only as raw values baked into each layer.
- No responsive/mobile/tablet variants exist for any screen (single desktop-only design).
- No component/variant system exists for shared UI patterns (buttons, inputs, cards, dialogs) — everything is authored as one-off frames.
- Prototype/interaction wiring between screens was not inspected in this pass (see conflict note above).
