# Review Log

Maintained automatically by the frontend/module workflows and their quality agents.

| Timestamp | Unit | Review | Scope | Findings | Fixes | Result |
|---|---|---|---|---|---|---|
| 2026-08-22 | SCR-001 Splash Screen | tester | typecheck, build, lint, static logic review of bootstrap sequence | No defects found | none needed | PASS |
| 2026-08-22 | SCR-001 Splash Screen | ui-reviewer | Figma fidelity, accessibility, conventions (first screen) | 1 blocker: `--color-text-muted`/`--color-text-faint` tokens failed WCAG AA contrast (2.56:1 / 1.48:1) on most metadata text. 2 high: meaningful metadata text set at 9px (`--text-xs`, "decorative/technical, never body copy" per design-system-notes.md) instead of a legible size; one hardcoded `10.5px` bypassing the token system. 2 medium: error state signaled by color alone with no icon; `aria-live` region scoped to the whole card causing noisy re-announcements. 2 low: logomark `alt="Meridian"` redundant with adjacent visible brand text; brand heading 19px vs. Figma-observed ~16px (unverifiable this session, no Figma MCP access; extrapolation flagged, not changed). | Retargeted `--color-text-muted`/`--color-text-faint` tokens to the accessible `--color-text-secondary(-alt)` values (≥4.76:1) in `tokens.css`; added `--color-decorative-*` tokens for non-text use only. Added `--text-meta` (11px) token and moved all metadata text (env badge, license, tagline, status label, version, active-step text, footer, copyright) off `--text-xs` onto it; replaced the hardcoded `10.5px` with the same token. Added a `⚠` glyph to the error status label. Scoped `role="status"`/`aria-live="polite"`/`aria-busy` to just `.statusBlock`. Set the logomark `<img alt="">` (decorative, adjacent text already announces the brand name). Left the two low-severity items as documented, unverified extrapolations per agent-rules.md §2 (no invented evidence) — no Figma MCP re-check was available this session. | PASS after fixes — rerun `npx tsc --noEmit`, `npm run build`, `npm run lint`: all clean. |
| 2026-08-22 | SCR-002 Login Screen | ui-reviewer | Figma fidelity (node `14:4055`), accessibility, token/convention compliance | 1 blocker: same decorative-color-on-real-text defect as SCR-001 recurred (`--color-decorative-muted`/`-faint` used for license/env/tagline/footer/copyright text, WCAG AA fail). 3 high: several hardcoded hex colors bypassing tokens.css (`#475569`, `#fff`, `#fef2f2`/`#fecaca`, `#374151`); dead CSS line (`.formError` background set twice); no `<main>` landmark. 4 medium: no `:focus-visible` on `.link`/`.checkbox`; "Keep me signed in" write-only (bootstrap's `restoreSession` only read `localStorage`, missing the `sessionStorage` case — cross-file bug); "Continue with SSO" had no handler and was fully enabled (dead control). 2 low: metadata text at `--text-xs` instead of `--text-meta` (same class of defect as SCR-001, different elements); password-visibility icon never visually changed state. | Fixed all metadata color/size tokens to `--color-text-muted`/`--color-text-faint`/`--text-meta`. Added `--color-danger-bg`, `--color-danger-border`, `--color-text-on-primary`, `--color-text-secondary-strong`, `--color-text-tertiary` to `tokens.css` and replaced every hardcoded hex in `LoginScreen.module.css`; removed the dead background line. Wrapped the card in `<main>`. Added `:focus-visible` rings to `.link`, `.checkbox`, `.submit`, `.sso`. Fixed `bootstrapApi.ts#restoreSession` to check `sessionStorage` as a fallback after `localStorage`. Disabled the SSO button permanently with an explanatory `title` (no SSO backend/module exists — not wired to a fake success path, per agent-rules.md §5/§2). Added an opacity-based active state to the eye icon so the toggle is perceivable visually, not just via `aria-pressed`. | PASS after fixes. |
| 2026-08-22 | SCR-002 Login Screen | tester | typecheck, lint, build, static logic review of validation/mock-boundary/storage | 2 bugs: `authApi.ts`'s success path called `res.json()` unguarded — a malformed 2xx body from a real backend would fall into the mock-fallback catch and be silently reported as a mocked success instead of a real error; `bootstrapApi.ts#restoreSession` only read `localStorage`, so a session started without "Keep me signed in" (stored in `sessionStorage`) was incorrectly reported absent on next bootstrap. 1 minor: `login()`'s `mocked` flag was computed but never read by the caller. | Wrapped the success-path `res.json()` in its own try/catch plus a shape check, both throwing `LoginError` (never falls through to the mock path). Fixed `restoreSession` to check both storages (shared fix with the ui-reviewer pass above). Added a `console.info` trace in `useLogin.ts` when a login actually used the mock fallback. | PASS after fixes — rerun `npx tsc -b`, `npx oxlint`, `npm run build`: all clean. |

| 2026-08-22 | SCR-003 Reset Password Screen | ui-reviewer | Figma fidelity (node `14:4162`, no working Figma MCP call this session — compared structurally against Figma-verified sibling SCR-002 instead), accessibility, token/convention compliance | 1 blocker: `LoginScreen.jsx`'s "Forgot password?" button routed to `/forgot-password` (unbuilt SCR-004 placeholder) instead of `/reset-password`, so the new screen was unreachable from the real UI. 2 high: app-level `passwordReset` Redux slice could desync from local `email` state across remounts, showing a broken/blank confirmation after a second visit (`resetRequestReset` action existed but was never dispatched); `aria-describedby` hint pattern diverged from `LoginScreen.jsx`'s email field with no documented rationale. 2 medium: no in-place recovery path from the "sent" success state short of leaving the screen; `role="status"` success region had no focus management, so keyboard/sighted users got no visual cue of the view change. 2 low: duplicated `inputBase` literal across `ResetPasswordScreen.jsx`/`LoginScreen.jsx`; mock-boundary console log verified consistent with `useLogin.js` (no issue). | Repointed `LoginScreen.jsx`'s "Forgot password?" button to `/reset-password`. Dispatched `resetRequestReset()` on unmount in `useResetPasswordRequest.js`. Extracted the shared `inputBase` string into `frontend/src/features/auth/formStyles.js`, imported by both screens. Documented the `aria-describedby` hint-pattern divergence inline in `LoginScreen.jsx` as intentional (SCR-002 is an already-DONE, Figma-verified screen with no such hint in its design — not retrofitted). Added focus management: the "Check your email" heading now receives focus via `ref`/`tabIndex={-1}` when the success state appears. Left the "no in-place resend" medium finding as a documented gap (Open Issues below) rather than expanding scope with a resend/retry affordance not requested. | PASS after fixes — rerun `npx oxlint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-003 Reset Password Screen | tester | lint, build, route-wiring, asset-resolution verification | No defects found in lint/build/routing. Runtime-in-browser check BLOCKED (no Playwright/Puppeteer/Cypress installed in `frontend/`) — environment gap, not a product bug. | none needed for the passing checks; recommended (not applied) adding a browser-automation dev dependency for future runtime smoke tests | PASS (browser-runtime layer explicitly unverified, flagged rather than assumed). |
| 2026-08-22 | SCR-004 Forget Password Screen | ui-reviewer | Figma fidelity (node `16:4495`, no working Figma MCP call available to the reviewer this session — cross-checked against prior audit docs and Figma-verified siblings LoginScreen/ResetPasswordScreen instead; get_design_context had in fact been called earlier in the same build to confirm content), accessibility, token/convention compliance | 1 blocker (docs-process, not code): screen-inventory.md/module-plan.md still showed SCR-004 as `DISCOVERED` despite the implementation existing — resolved by this same documentation pass. 1 high (unconfirmed, disputed): card width `420px` vs. siblings' `400px` — verified via an earlier `get_design_context` call in this build that node `16:4495`'s actual card is 420px wide, not a fidelity regression; left as-is. 3 real high/medium: generic "does not meet all requirements" field error gave no visual pointer to which checklist row(s) were unmet; the missing-token guard only covered "no token at all," not "token present but invalid," and the mock fallback's `token === 'invalid-token'` sentinel made every other non-empty token silently "succeed" in mock mode; missing-token terminal state had no focus management unlike the sibling `done` state. 1 low: strength-label fallback duplicated `'Too weak'` as an inline literal instead of reading the hook's own already-correct empty-string label. | Updated screen-inventory.md (SCR-004 row + build notes section) and module-plan.md (MOD-002 screen count) to `DONE`. Unmet checklist rows now render in the danger color when a failed submit's field error is present, tying the generic banner to the specific missing requirement(s). Broadened the mock fallback's invalid-token heuristic from one sentinel literal to "any token shorter than 16 chars," so an arbitrary present-but-wrong token now demonstrates the same error path as a missing one (documented as a known mock-mode limitation, not real validation). Added the same `ref`/`tabIndex={-1}`/focus-on-mount treatment to the missing-token heading that the `done` heading already had. Simplified the strength-label span to read `strength.label` directly. | PASS after fixes — rerun `npx vite build` and `npm run lint`: both clean. |
| 2026-08-22 | SCR-004 Forget Password Screen | tester | lint, build, route-wiring (both with and without `?token=`), asset-resolution, isolated logic verification of `usePasswordStrength` | No defects found. Runtime-in-browser console check BLOCKED (no Playwright/Puppeteer/Cypress installed in `frontend/`, same environment gap as SCR-003) — HTTP-level checks (200 on both token variants) plus a code-path trace substituted. | none needed | PASS (browser-runtime layer explicitly unverified, flagged rather than assumed). |
| 2026-08-22 | SCR-007 Change Password Screen | ui-reviewer | Figma fidelity (node `19:5237`, no working Figma MCP call available to the reviewer this session — cross-checked against Figma-verified siblings ResetPasswordScreen/ForgetPasswordScreen/TwoFactorAuthenticationScreen and design-system-notes.md instead; get_design_context had been called earlier in this build to fetch the real node content), accessibility, token/convention compliance | 2 high: server-reported `errorField` targeting was only wired to the `currentPassword` input's error styling/`aria-invalid`/`aria-describedby` — a server error naming `newPassword`/`confirmPassword` fell through to the generic banner only, with no red border/`aria-invalid` on the actual field; real/meaningful error text hardcoded at `text-[11.5px]`, below the `--text-meta` token floor established (and previously fixed) in SCR-001/002. 2 medium: scaffold's nav-section labels (`text-[8px]`/`text-[9.5px]`) also bypassed the token system; `aria-describedby` on `current-password`/`confirm-new-password` pointed at a conditionally-absent element id in the no-error state, diverging from the sibling screens' pattern of switching the attribute between an error id and `undefined`. 3 low (deferred, not fixed — scoped to the explicitly-temporary `SettingsShellScaffold.jsx`): non-active nav labels have no `aria-disabled`/visual cue distinguishing them from real links; active-nav-item background uses an inline rgba() value instead of a token; three near-duplicate password show/hide field blocks are copy-pasted rather than extracted to a shared sub-component. | Extended `errorField`-driven error styling/`aria-invalid`/inline messaging to the `newPassword` and `confirmPassword` fields (previously only `currentPassword`); replaced all `text-[11.5px]` literals with `text-token-meta`; fixed `aria-describedby` on `current-password`/`confirm-new-password` to be conditional on an error being present, matching sibling screens; moved the scaffold's nav-label font sizes onto `--text-xs`/`--text-meta` tokens. The three low-severity findings were left as documented, deferred notes in `SettingsShellScaffold.jsx`'s and `ChangePasswordScreen.jsx`'s doc comments rather than fixed, since the affected component is itself a temporary scaffold slated for replacement once MOD-001 ships the real AppShell. | PASS after fixes — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-007 Change Password Screen | tester | lint, build, route-wiring, Redux slice registration, mock-boundary logic consistency vs. `passwordResetConfirmApi.js` | No defects found. No automated test suite exists in this project (pre-existing gap, not introduced by this screen). Runtime-in-browser check not performed this pass (build output used as the strongest available proxy for import/module-resolution correctness, same substitution as prior screens' tester passes). | none needed; recommended (not applied) a manual smoke test of mismatched-password, incorrect-current-password (mock `wrong-password` sentinel), and successful-submission paths before merge | PASS (browser-runtime layer explicitly unverified, flagged rather than assumed). |
| 2026-08-22 | SCR-005 Email_Verification Screen | ui-reviewer | Figma fidelity (node `18:5023`, no live Figma MCP call available to the reviewer this session — cross-checked against this build's own earlier `get_design_context` capture and Figma-verified siblings LoginScreen/ResetPasswordScreen/ForgetPasswordScreen instead), accessibility, token/convention compliance | 1 dead/misleading `aria-live="polite"` on the static masked-email badge (content never changes post-mount, so nothing was ever announced). 1 high: the transient "Verifying your email…" state had no heading or focus target, breaking the focus-on-state-change convention every other state on this screen and its siblings follows. 2 medium: resend-failure error banner was placed mid-block (below the "Didn't receive the email?" sub-heading) instead of at the top of the content block like every sibling screen's banner; `maskEmail`'s doc comment overclaimed exact Figma fidelity for its fixed-4-dot padding rule, which no Figma sample actually specifies. 2 low (not fixed, non-blocking): 3-level nested ternary for the token/confirming/verified/error branching is harder to scan than siblings' flatter 2-level pattern; a stale "Verification email resent." message could theoretically remain visible alongside a fresh `resendError` banner on a fail-after-success resend sequence. | Removed `aria-live="polite"` from the masked-email badge. Added an `<h1>` heading with the same `ref`/`tabIndex={-1}`/focus-on-mount pattern as the `verified`/error states to the `confirming` branch. Moved the resend-error banner to the top of the pending-state content block. Reworded the `maskEmail` comment to state it approximates the Figma mask rather than claiming an exact match. Left the two low findings as documented, non-blocking observations per agent-rules.md §9 (no scope expansion for cosmetic/theoretical-only items not requested). | PASS after fixes — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-005 Email_Verification Screen | tester | lint, build, dev-server route/asset serving (`/verify-email` with and without `?token=`), logic trace of `maskEmail`/mock-fallback branching | No defects found. Runtime-in-browser DOM check BLOCKED (no Playwright/Puppeteer/Cypress installed in `frontend/`, same environment gap as SCR-003/SCR-004) — HTTP-level route/asset checks plus a direct logic trace substituted. | none needed | PASS (browser-runtime layer explicitly unverified, flagged rather than assumed). |
| 2026-08-22 | Frontend stack | migration (explicit user instruction) | Converted `frontend/` from TypeScript to plain JavaScript (`.js`/`.jsx`), added Tailwind CSS wired to `tokens.css` via `tailwind.config.js` theme extensions, dropped CSS Modules in favor of Tailwind utility classes + custom CSS. Removed `tsconfig*.json`, `typescript`/`@types/*` deps, `tsc -b` from the build script. Rewrote `App`, `main`, router, store, hooks, queryClient, and both built screens (SplashScreen, LoginScreen) plus their slices/APIs/hooks as JS. Documented as an override in `agent.md` §4.5/§5.1 (which mandate TypeScript project-wide) rather than silently editing that spec, per agent-rules.md §3/§4. | n/a — greenfield conversion, no defects to fix | `npm run build` and `npx oxlint` clean; dev server serves `/`; Tailwind utility classes confirmed present in built CSS output (not purged). |
| 2026-08-22 | SCR-006 Two Factor Authentication Screen | tester | lint, build, route/store-wiring verification | No defects found. No test runner is configured in this repo (`frontend/package.json` has no `test` script) — unit/integration and type-check layers are not applicable, flagged as a general project gap rather than a SCR-006-specific issue. | none needed; recommended (not applied) adding Vitest for future hook/reducer coverage | PASS — `npm run lint` and `npm run build` both clean. |
| 2026-08-22 | SCR-006 Two Factor Authentication Screen | ui-reviewer | Figma fidelity (node `19:5127`), accessibility, token/convention compliance | FAIL (pre-fix). 3 high: OTP cell group had no accessible group name (`aria-label` on each cell overrode the visible "Verification code" `<label>`, and no `fieldset`/`role="group"`/`aria-labelledby` wrapped the six cells); OTP cells didn't expose their error state via `aria-invalid`/`aria-describedby` despite LoginScreen's precedent of wiring both on invalid fields; verify failure cleared and refocused the first OTP cell but never moved focus to the error banner itself, unlike EmailVerificationScreen's heading-focus convention. 3 medium: resend button's dynamic status text ("Sending…"/cooldown/"Resend code") had no `aria-live` region, unlike EmailVerificationScreen's dedicated live-region paragraph; the "code resent" confirmation was gated on `cooldown > 0` and incorrectly disappeared ~38s later even though the slice's `resendStatus` was still `'sent'`; disabling "Use another method" via `disabled`+`title` (mirroring LoginScreen's SSO precedent) leaves keyboard/screen-reader users with zero explanation, since `disabled` controls are unfocusable and `title` is mouse-hover-only — flagged as a shared, pre-existing weakness in both screens, not unique to this one. 1 low: `setDigit`'s `.slice(-1)` (last-digit-wins on multi-char input) had no comment explaining the choice, unlike the codebase's convention of documenting extrapolated edge-case behavior (e.g. `maskEmail`). 1 low (documentation-only): `twoFactorApi.js`'s header comment described resend as "re-triggers delivery," which doesn't match an authenticator-app-only flow where codes are generated locally, not delivered. Also recorded: this reviewer session had no working Figma MCP call, so pixel-level fidelity to node `19:5127` was not independently re-verified — layout/spacing/typography/color/icon usage were judged via code comments and `design-system-notes.md` convention comparison only. | Wrapped the six `OtpCell`s in `role="group"` + `aria-labelledby` pointing at the visible label; added `aria-invalid`/`aria-describedby` to `OtpCell`, wired to the error banner's new `id`. Added a focus-managed `ref`/`tabIndex={-1}` `role="alert"` error banner (mirrors EmailVerificationScreen's heading-focus pattern) that now receives focus on verify failure. Added `aria-live="polite"` to the resend button. Removed the `cooldown > 0` gate from the "code resent" confirmation so it persists like EmailVerificationScreen's equivalent message. Documented the `.slice(-1)` choice inline. Corrected `twoFactorApi.js`'s header comment to describe resend as a server-side resync rather than an SMS/email-style delivery. Left the shared `disabled`+`title` accessibility weakness on "Use another method"/LoginScreen's SSO button as a documented, cross-screen limitation (Open Issues below) rather than redesigning both screens' disabled-control pattern out of scope for this unit. | PASS after fixes — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-008 Logout Confirmation Screen | tester | lint, build, static wiring verification (router, store), dev-server route smoke check | No defects found. `npm run lint` and `npm run build` both clean. `npm run preview` + HTTP request to `/logout` returned 200. No test runner configured in this repo (pre-existing gap). In-browser interaction (confirm/cancel behavior, focus management) not independently verified — no Playwright/Puppeteer/Cypress installed, same environment gap as prior screens. | none needed | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than assumed). |
| 2026-08-22 | SCR-008 Logout Confirmation Screen | ui-reviewer | Figma fidelity (node `20:5458`, no working Figma MCP call available to the reviewer this session — cross-checked structurally against Figma-verified siblings and `tokens.css`/`tailwind.config.js` instead; this build's own earlier `get_design_context` call had already confirmed the two-`App`-instance structure and backdrop color), accessibility, token/convention compliance | FAIL (pre-fix). 3 blockers: the confirm dialog was a plain unlabeled `div` with no `role="dialog"`/`aria-modal`/`aria-labelledby` and no focus moved into it on mount (unlike its own `done` state, which did move focus); `rounded-[10px]` and a bespoke box-shadow literal had no matching token and no precedent anywhere else in the codebase (every sibling card uses `rounded-sm`); the full-viewport backdrop scrim reused `--color-decorative-muted` at 30% opacity, a token explicitly documented as decorative-accent-only, not a page-covering overlay, and no dedicated scrim token existed. 2 high: `navigate(-1)` was the sole Cancel path, unsafe if `/logout` is reached directly (bookmark/typed URL/new tab) with no real prior history; the destructive "Log Out" action had no defined initial focus target in the confirm state (contrast: `done` state did). 2 medium: `logoutApi.js` computed a `serverAcknowledged` signal on non-401 server rejections that `useLogout.js` never read or surfaced anywhere; `logoutSlice` is app-level state with no `reset()`-on-mount call, so navigating back to `/logout` after a completed logout could re-render the "signed out" panel without a fresh confirmation — the same defect class already found and fixed once at SCR-003. 2 low: body copy across the two states was slightly redundant; the 1200ms redirect delay was an unexplained magic number. | Added `role="dialog"`, `aria-modal="true"`, `aria-labelledby` (pointing at the current state's heading), and focus-on-mount to both dialog states. Replaced the fabricated `rounded-[10px]` + box-shadow with the existing `rounded-sm` + `border-border` convention shared by every sibling card. Added a new `--color-overlay-scrim: rgba(15,23,42,0.28)` token to `tokens.css`/`tailwind.config.js`, sourced from this build's own `get_design_context` capture of the Figma overlay, and used it in place of `--color-decorative-muted`. Replaced the `navigate(-1)`-only Cancel handler with a guarded fallback (`navigate(-1)` only when `window.history.state?.idx > 0`, else `navigate('/dashboard')`). Simplified `logoutApi.js` to drop the unused `serverAcknowledged` signal in favor of a non-blocking `console.warn` diagnostic, mirroring `useLogin.js`'s mock-boundary logging precedent. Added a `reset()` call on mount in `LogoutScreen.jsx`, mirroring `useResetPasswordRequest.js`'s fix for the identical stale-slice-state defect. Left the two low findings as documented, non-blocking observations (timeout rationale added inline). | PASS after fixes — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-009 Main Dashboard Layout Screen | ui-reviewer | Figma fidelity (node `27:2238`, no working Figma MCP call available to the reviewer this session — this build's own earlier `get_design_context` call had already captured the real KPI/table/health-panel content and literal figures), accessibility, token/convention compliance | PASS (with findings, no blockers). 2 medium: no `aria-live`/`role="status"` region announced loading/error/refresh-in-progress transitions to screen readers, unlike every other reviewed screen; six action buttons ("View Details →", "View all →", per-row "View"/"Retry", "View executions →") had no `onClick`, `disabled`, or `title`, so keyboard/screen-reader users got zero feedback that they do nothing — unlike the Export button's own disclosed-disabled pattern in the same file. 3 low: `AppShellScaffold.jsx`'s doc comment didn't carry forward the same "known deferred gap" caveat about inert nav items/untokenized active-state color that `SettingsShellScaffold.jsx` documents for the identical pattern; pipeline table's trailing actions column header had no screen-reader label; the `mocked` flag was computed in `dashboardApi.js` but never surfaced anywhere in the UI, so 100%-mocked dashboard data (MOD-008/009 unbuilt) looked indistinguishable from live metrics. | Added a visually-hidden `role="status"`/`aria-live="polite"` region reflecting the query's current phase (loading/error/refreshing/updated). Disabled all six inert action buttons with explanatory `title`s naming the missing MOD-008/MOD-009 endpoint, mirroring the Export button's existing pattern. Added the missing deferred-gap caveat to `AppShellScaffold.jsx`'s doc comment. Added `sr-only` "Actions" text to the empty table header cell. Added a visible "Sample data" badge next to the page title, shown whenever `data.mocked` is true. | PASS after fixes — rerun `npm run build` and `npm run lint`: both clean. |
| 2026-08-22 | SCR-009 Main Dashboard Layout Screen | tester | build, lint, manual component-tree trace (conditional-render branches, prop shapes, icon-asset resolution, route wiring) | No defects found. No test runner is configured in this repo (pre-existing gap, not introduced by this screen). All six new icon assets (`icon-export.svg`, `icon-refresh.svg`, `icon-activity.svg`, `icon-alert-triangle.svg`, `icon-check-circle.svg`, `icon-trend-up.svg`) confirmed present and non-empty. `/dashboard` route confirmed wired to `DashboardScreen`, not the prior `RoutePlaceholder`. | none needed | PASS — `npm run build` and `npm run lint` both clean. |
| 2026-08-22 | MOD-001 Shared AppShell (Header 211:1981, Footer 211:2024, Sidebar expanded 213:5159 / collapsed 213:5708) | ui-reviewer | Figma fidelity (all 4 nodes fetched directly via `get_design_context` during this build, file key `FU593OPeUscEvpu3hbOZrT`), role-based sidebar filtering across all 4 roles, accessibility, token/convention compliance | FAIL (pre-fix; reviewer itself lacked live Figma MCP access this session and flagged that limitation, but this build's own earlier `get_design_context` calls on all 4 nodes already captured real structure/content, closing that gap). 1 blocker: new `--color-shell-nav-heading` token (`#cbd5e1`) applied to real section-heading text ("DATA", "PIPELINES", etc.) — the exact WCAG-AA-contrast-on-real-text defect class `tokens.css` already documents as fixed once before (SCR-001's `--color-text-faint`), reintroduced on a new token. 1 high: no `focus-visible` outline on any Sidebar interactive control (both collapse/expand toggles, group-toggle buttons, `SidebarLink` NavLinks, collapsed-rail NavLinks/icon buttons) despite Header.jsx and DashboardScreen.jsx establishing `focus-visible:outline-2 ...outline-primary` as the project's standing convention for the same shell. 1 medium (informational, not a defect): asymmetric default-open state between top-level `NavSection` (open) and nested `NestedNavGroup` (closed) had no rationale comment — confirmed correct per Figma screenshot, just needed documenting. 2 low: 3 exported icon assets (`nav-analytics-mask.svg`, `chevron-down-sm.svg`, `breadcrumb-sep.svg`) were downloaded but never referenced by any component; Header's search `<input>` removed the native focus outline (`focus:outline-none`) without adding a `focus-visible` replacement, unlike its sibling icon buttons in the same file. RBAC/structural-parity findings: role-based filtering (`useVisibleNav`/`filterNode`/`hasAnyPermission`) confirmed correct for all 4 roles (empty groups never render); Header/Footer take no `role` prop and render unconditionally; Sidebar's chrome (logo header, footer user card, toggle) is identical across roles — only `visibleNav` content varies. No blockers found on requirements 2/3/5/6. | Retargeted `--color-shell-nav-heading` to `var(--color-text-secondary)` (real, WCAG-AA-passing token) instead of the raw `#cbd5e1` value, with a comment cross-referencing the SCR-001 precedent. Added `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary` to every Sidebar interactive control (both toggles, `NavSection`/`NestedNavGroup` buttons, `SidebarLink` NavLink, collapsed-rail NavLink). Added a `has-[:focus-visible]:outline-*` treatment to Header's search `<label>` wrapper so the input's focus state is visible again. Documented the default-open asymmetry inline in both `NavSection`/`NestedNavGroup`. Deleted the 3 unused icon assets. | PASS after fixes — rerun `npm run build`: clean (162 modules transformed). |
| 2026-08-22 | MOD-001 Shared AppShell | tester | lint, build, RBAC-logic trace against real `navConfig.js`/`permissions.js` for all 4 roles, dangling-reference check for deleted `AppShellScaffold.jsx`, icon-asset resolution, router path/href integrity | No defects found. `npm run lint` (oxlint) and `npm run build` both clean. Programmatically traced `filterNode`/`hasAnyPermission` against the real `NAV_TREE`/`ROLE_PERMISSIONS`: `super_admin`/`org_admin` see all 6 groups/26 hrefs; `data_etl_engineer` sees 5 groups/20 hrefs (Administration correctly excluded); `viewer` sees 4 groups/10 hrefs (Data and Administration correctly excluded, Pipeline Builder correctly excluded since `PIPELINE_CREATE` isn't granted) — zero permission violations, zero empty-group renders across all 4 roles. No import of the deleted `frontend/src/features/dashboard/components/AppShellScaffold.jsx` remains anywhere (one comment-only mention in `AppShell.jsx` documenting the migration, not a usage). All icon imports in `navIcons.js`/`Header.jsx`/`Sidebar.jsx` resolve to real files. 35 router paths, 26 nav hrefs: zero duplicates, zero unmatched hrefs. No test runner configured in this repo (pre-existing gap, not introduced by this unit). | none needed | PASS — `npm run lint` and `npm run build` both clean. |
| 2026-08-22 | MOD-001 AppShell — visual-polish redesign + new minimize icon (explicit user request, no Figma source — user confirmed "refined visual polish" scope and a double-chevron flip-direction toggle icon design) | ui-reviewer | New `SidebarToggleIcon.jsx` icon design/direction correctness, layout/structure-regression check (nav tree, RBAC filtering, spacing must be untouched — only surface polish added), accessibility of the new icon + changed footer user-card (`<span>` → `<button>`), token usage, build/lint sanity | PASS (with 2 low, non-blocking notes). Verified the new icon is a genuinely distinct double-chevron glyph (not a recolor of the deleted `sidebar-toggle.svg`) with correct rotation semantics (unrotated = "click to collapse leftward" when expanded; `rotate-180` = "click to expand rightward" when collapsed). Confirmed zero layout/nav-tree/RBAC regression — only additive `shadow-sm`/`transition-colors`/`before:*` active-accent/`focus-visible:*` classes were added. Confirmed correct `aria-hidden`/`aria-label` pairing (icon hidden, parent button carries the real name) and that the footer user-card's new `<button>` wrapper contains no nested interactive elements (valid HTML). No raw hex values introduced. 2 low findings: `shadow-sm` is Tailwind's default utility rather than a project design token (acceptable — no shadow token exists in the design system to begin with, usage is sparse/4 spots); `transition-colors` in Header.jsx/Footer.jsx omitted an explicit `duration-150` that Sidebar.jsx's nav links already declared (cosmetic-only, relied on Tailwind's matching default). | Added explicit `duration-150` to every `transition-colors` in Header.jsx/Footer.jsx to match Sidebar.jsx's convention. Left the `shadow-sm`-vs-token note as documented, non-blocking (Open Issues below) since introducing a new shadow token for 4 sparse usages would be scope creep beyond what was asked. | PASS after the duration-150 fix — rerun `npm run build` and `npm run lint`: both clean. |
| 2026-08-22 | MOD-001 AppShell — visual-polish redesign + new minimize icon | tester | lint, build, dangling-reference check for deleted `sidebar-toggle.svg`, new-component JSX validity, nested-interactive-element check on the footer button, RBAC-regression re-check, AppShell-consumer prop-contract check (DashboardScreen.jsx, router.jsx) | No defects found. `npm run lint` and `npm run build` both clean. Confirmed zero remaining references to the deleted `sidebar-toggle.svg` (one comment-only mention, not a usage). Confirmed `SidebarToggleIcon.jsx` is valid, self-contained JSX. Confirmed the footer user-card `<button>` nests no other interactive element. Re-derived the 4-role RBAC filter from current file contents and confirmed no regression (`rbac/*.js` untouched by this change). Confirmed `AppShell.jsx`'s prop contract to `Header`/`Sidebar`/`Footer` is unchanged, so `DashboardScreen.jsx`/`router.jsx` (which only consume `AppShell`, never the inner components directly) are unaffected — confirmed end-to-end via a clean production build. | none needed | PASS — `npm run lint` and `npm run build` both clean. |
| 2026-08-22 | SCR-011 conflict resolution — ConnectIQ brand rename (explicit user decision: real Figma frame `34:4157` "Header / Top Navigation Screen" conflicted with already-built shared Header.jsx — different logo/accent/content; user chose neither full replace nor a duplicate one-off build, but a project-wide brand rename to "ConnectIQ" while keeping the Meridian logomark asset as the visual mark) | ui-reviewer | Text-rename completeness across 12 files (Sidebar.jsx, Header.jsx, Footer.jsx, DashboardScreen.jsx, router.jsx, 5 auth screens, SettingsShellScaffold.jsx, SplashScreen.jsx), Sidebar.jsx logo-badge removal (app-logo.svg → meridian-logomark.svg direct image, no colored wrapper), accessibility (alt text, aria-labels), aspect-ratio/visual-regression reasoning (no live Figma MCP session available) | PASS (2 low). No stray "DataFlow"/bare "Meridian" brand text remained in any of the 12 files; only intentional Meridian references (asset path, SVG internal id) were left untouched. All ~20 router.jsx breadcrumb arrays, Header's default breadcrumb prop, Footer's copyright, and all 5 auth screens' brand name/tagline/"Protected by X Enterprise Auth"/copyright lines correctly read ConnectIQ. Sidebar logo-badge removal was clean (no orphaned wrapper markup), alt="" correctly kept decorative since "ConnectIQ" text sits adjacent. 2 low: `frontend/src/assets/icons/shell/app-logo.svg` left orphaned/unused after the Sidebar swap; pre-existing copyright-year inconsistency (auth screens/Splash say "© 2025", Footer says "© 2026") is now visible in both branded surfaces but was not introduced by this rename. | Deleted the orphaned `app-logo.svg`. Left the pre-existing 2025-vs-2026 copyright-year mismatch undocumented-fix per agent-rules.md §9 (not introduced by this unit, out of scope) — recorded here as an Open Issue for a future pass. | PASS after fix — rerun `npx vite build`: clean. |
| 2026-08-22 | SCR-011 conflict resolution — ConnectIQ brand rename | tester | lint (`oxlint`), build (`vite build`), asset-resolution check for `meridian-logomark.svg` import in Sidebar.jsx | No defects found. `npm run lint` and `npm run build` both clean (185 modules transformed, no import errors). No test runner configured in this repo (pre-existing gap, not introduced by this unit). | none needed | PASS — `npm run lint` and `npm run build` both clean. |
| 2026-08-22 | SCR-010 conflict resolution — mislabeled Figma frame (explicit user decision: node `33:3288` labeled "Sidebar Navigation Screen" in the inventory actually contains full dashboard content — KPI cards, pipeline table, system health, execution chart, data quality, recent activity — near-duplicate of SCR-009's Dashboard, no sidebar nav content at all; user chose to skip building it and mark it resolved by the already-complete shared `Sidebar.jsx`) | n/a — documentation-only resolution, no code change | n/a (no implementation produced to review/test) | Confirmed via `get_metadata` on node `33:3288`: zero nav-item/sidebar-shaped nodes present, full match to SCR-009's dashboard section structure (Key metrics, Pipeline activity, System health, Execution performance, Data quality, Recent activity). | none needed — no code written | Resolved by documentation update only (screen-inventory.md, module-plan.md); no build/lint/test gate applicable since nothing was implemented. |
| 2026-08-22 | SCR-012 Notification Panel Screen | ui-reviewer | Figma fidelity (node `36:5210`, fetched directly via `get_design_context` during this build, file key `FU593OPeUscEvpu3hbOZrT`; reviewer itself had no Figma MCP access this session and flagged that as a coverage gap, closed by this build's own capture), accessibility (dialog semantics, focus management, ARIA tabs pattern), token/convention compliance, real-state coverage, mock-boundary labeling | FAIL (pre-fix). 4 blockers: docs (`screen-inventory.md`/`module-plan.md`) still showed SCR-012 `DISCOVERED`/`PLANNED` despite a real implementation existing (docs-process gap, resolved by this same pass); no focus moved into the `role="dialog"` panel on open (contradicts the SCR-008 focus-on-mount precedent); the panel declared `role="dialog"` with no focus trap and no `aria-modal`, an incomplete/misleading pattern; `role="tablist"`/`role="tab"` filter tabs had no matching `role="tabpanel"`, no `aria-controls`/`aria-labelledby` pairing, and no roving-tabindex/arrow-key support — an invalid ARIA tabs implementation. 2 high: "Mark all read" updated the cache in the mutation's `onSuccess` rather than `onMutate`, so it wasn't actually optimistic, and a failed mutation had no rollback or user-facing error. 2 medium (not blocking): the whole scrollable notification list sat inside one broad `aria-live="polite"` region, re-announcing large content chunks on every tab switch instead of a small dedicated status element; tab-switch had no `role="status"` count announcement. Low findings passed clean: real token usage (no new hardcoded hex), all 7 icons confirmed genuine downloaded Figma exports (not hand-authored), empty/loading/error state coverage, mock-boundary labeling. | Added focus-on-open (first focusable element inside the panel, mirroring the SCR-008 fix) and a real Tab/Shift+Tab focus trap in `Header.jsx`; added `aria-modal="true"` to the panel. Rebuilt the filter tabs as a complete ARIA tabs pattern: added `role="tabpanel"` with `aria-labelledby`, `aria-controls` on each tab, roving `tabIndex` (active tab `0`, others `-1`), and ArrowLeft/ArrowRight/Home/End keyboard navigation. Converted `markAllRead` to a true optimistic update in `useNotifications.js` (`onMutate` snapshot-and-set, `onError` rollback), added a `markAllReadFailed` flag surfaced as a `role="alert"` banner in the panel. Replaced the broad `aria-live="polite"` wrapper around the whole list with a dedicated visually-hidden `role="status"` line reporting loading/error/"showing X of Y, N unread" — the list itself is no longer a live region. Updated `docs/figma/screen-inventory.md` and `docs/modules/module-plan.md` to reflect the real DONE status. | PASS after fixes — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-012 Notification Panel Screen | tester | lint, build, icon-asset-existence check, import/prop-contract static check (Header.jsx ↔ NotificationPanel.jsx ↔ useNotifications.js) | No defects found. `npm run lint` (oxlint) and `npm run build` both clean. All 7 icons imported by `NotificationPanel.jsx` confirmed present on disk under `frontend/src/assets/icons/notifications/`. `Header.jsx`'s new imports/hooks/refs all used correctly; props passed to `NotificationPanel` match `useNotifications()`'s returned object shape exactly. No test runner is configured in this repo (pre-existing gap, not introduced by this screen) — no typecheck/unit-test layer applies. One inert, non-blocking wire noted: the disabled "View all notifications" button still carries an `onClick` that can never fire while disabled. | none needed | PASS — `npm run lint` and `npm run build` both clean (re-verified after the ui-reviewer's accessibility/mutation fixes above). |
| 2026-08-22 | SCR-013 User Profile Menu Screen | tester | lint, build, icon-asset-existence check (13 new SVGs), Header/AppShell prop-contract check for the new `roleLabel` prop and other-consumer regression check | PASS. `npm run lint` (oxlint) and `npm run build` both clean. All 13 icons imported by `UserProfileMenu.jsx` confirmed present on disk under `frontend/src/assets/icons/profile-menu/`. Confirmed `AppShell.jsx` is the only import/render site of `Header`, so no other consumer could break from the new `roleLabel` prop; confirmed `Header`/`UserProfileMenu` both handle a missing `roleLabel` gracefully (optional, guarded render). `sessionSlice.js`'s new `email` field is read defensively (`currentUser?.email ?? ''`) everywhere it's consumed. No test runner configured in this repo (pre-existing gap). | none needed | PASS — `npm run lint` and `npm run build` both clean. |
| 2026-08-22 | SCR-013 User Profile Menu Screen | ui-reviewer | Figma fidelity (node `39:6447` trigger / `39:6826` dropdown content, fetched directly via `get_design_context` during this build, file key `FU593OPeUscEvpu3hbOZrT`; reviewer itself had no Figma MCP access this session and flagged that as a coverage gap — closed by this build's own capture, confirmed correct against the review's one disputed finding below), accessibility (dialog semantics, focus trap, Escape/click-outside), token/convention compliance, consistency with the sibling SCR-012 bell/panel pattern | FAIL (pre-fix). 1 high: the avatar/org-badge fill used a hardcoded `linear-gradient(135deg, #1e3a5f 0%, #0f5699 100%)` inline style, duplicated twice, with `#1e3a5f` matching no existing token — flagged against `tokens.css`'s "do not re-hardcode hex values" rule. This gradient is confirmed correct against node `39:6826`'s real fill (captured directly in this build's own earlier `get_design_context` call, `rgb(30,58,95)`→`rgb(15,86,153)`), so the fidelity itself was never in question — only its lack of a token. 1 medium (docs-process, resolved by this same pass): screen-inventory.md still showed SCR-013 as `REVIEW_REQUIRED` with no "build notes" section, unlike every other completed screen. 1 low (structural note, not a defect): the open/focus-trap/Escape/click-outside effect lives inside `UserProfileMenu.jsx` itself rather than centralized in `Header.jsx` like the bell pattern — behaviorally equivalent and independently traced as correct (each dropdown closes the other properly), left as an intentional, arguably cleaner encapsulation rather than changed to match structurally. 1 low (pre-existing, shared pattern, not unique to this screen): disabled menu items (Switch/Manage Organization, Billing, Documentation, Help Center, Contact Support, Release Notes) communicate their reason via `disabled`+`title` only, the same accessibility gap already tracked for SCR-006/SCR-012's disabled controls. All dialog semantics (`role="dialog"`/`aria-modal`/`aria-labelledby`), trigger ARIA (`aria-haspopup`/`aria-expanded`/`aria-controls`), focus-on-open, focus trap, Escape-to-close, click-outside-to-close, danger-token styling on Sign Out, and section-divider/heading treatment all matched the SCR-012 convention exactly and passed clean. | Added `--gradient-shell-avatar` to `tokens.css` and a matching `bg-shell-avatar` Tailwind utility in `tailwind.config.js`; replaced both inline-style gradient instances in `UserProfileMenu.jsx` with the token class. Updated `docs/figma/screen-inventory.md` (SCR-013 row + build notes) to `DONE`. Left the structural (effect-location) and shared disabled+title findings as documented, non-blocking notes rather than refactored/redesigned out of scope. | PASS after fixes — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-014 Executive Dashboard Screen | ui-reviewer | Figma fidelity (node `45:7` and 13 sub-node targeted fetches — the full-screen `get_design_context` call returned a sparse metadata-only fallback due to size, so header/summary-strip/KPI-cards/alerts/charts/operational-health/reports/business-unit-table were each fetched individually via `get_design_context`, file key `FU593OPeUscEvpu3hbOZrT`), accessibility, token/convention compliance vs. sibling SCR-009 `DashboardScreen.jsx` | PASS (with non-blocking findings; no Figma MCP access in the reviewer's own session, but this build's own direct `get_design_context` captures across all 13 sub-nodes closed that gap). 1 high: `BusinessUnitAdoptionTable` rendered genuinely tabular data (unit / adoption bar / users / pipelines / %) as a bare `<ul>/<li>` list with no column headers, unlike `DashboardScreen.jsx`'s `PipelineActivityCard` convention of a real `<table>`/`<thead>`/`<th scope="col">` for equivalent tabular data — screen-reader users got no column context. 2 medium: the loading skeleton only approximated a fraction of the real layout (missing the summary strip, second KPI row, two of three chart rows, and the table), risking a large layout shift on data load; the icon-only Refresh button relied solely on a `title` attribute with no `aria-label`. 2 low: `SummaryStrip`'s platform-status pill hardcoded a green/healthy tone regardless of the mock data's own `status` field (latent bug, harmless today since only `'healthy'` exists in mock data); disabled buttons (Export/Share/report links) communicate their disabled reason via `title` only — same shared, pre-existing pattern already tracked at SCR-006/SCR-012/SCR-013's entries above. | Converted `BusinessUnitAdoptionTable` to a real `<table>` with `<thead>`/`<th scope="col">` (Business Unit/Adoption/Users/Pipelines/%), matching `PipelineActivityCard`'s convention. Expanded `ExecutiveDashboardSkeleton` to cover the summary strip, both KPI rows, all three chart+side-card rows, and the business-unit table. Added `aria-label` to the Refresh button (kept the icon's own `alt=""` since the button now carries the name). Added a `SUMMARY_STATUS_TONE` lookup (healthy/warning/down → pill+dot classes) so `SummaryStrip` reads `item.status` instead of hardcoding green. | PASS after fixes — rerun `npx vite build`: clean. |
| 2026-08-22 | SCR-014 Executive Dashboard Screen | tester | build, lint (`oxlint`), live dev-server + Playwright browser check of `/dashboard/executive` (KPI cards, charts, alerts, operational health, reports, business-unit table, date-range toggle `aria-pressed` states, Refresh spinner, Export/Share disabled state, console-error check) | FAIL (pre-fix) — real bug found, not just an environment gap. `npx vite build` and `npx oxlint` both clean. Live in dev server: the screen got stuck on its error state ("Couldn't load the executive dashboard" / "Received an unexpected response from the server.") instead of showing mock data. Root cause: no dev API proxy exists in `vite.config.js`, so `fetch('/api/dashboard/executive')` resolves to the Vite dev server's own SPA-fallback `index.html` (200 OK, `text/html`) rather than a network failure; `res.ok` was `true`, so the code proceeded to `readJson(res)`, which failed to parse HTML as JSON and threw — but the `catch` block only fell back to mock data when the thrown error was *not* the domain `ExecutiveDashboardError`, so the "bad response" case re-threw instead of falling back. Confirmed the identical pre-existing pattern in `dashboard.api.js` (SCR-009, already `DONE`) fails the same way in this same dev environment — a systemic, pre-existing gap this screen inherited rather than introduced from scratch, but one that broke this screen's own mock-data path by default. Once a genuine network failure was simulated directly (bypassing the HTML-fallback case), the mock fallback fired correctly and every checked element rendered: 6-item summary strip, 8 KPI cards, both inline SVG charts with legends/stat cells, 5-item alerts list with correct severity badges, operational health panel, reports list, 8-row business-unit table, "Sample data" badge, zero console errors. Date-range toggle `aria-pressed` states, Refresh spinner toggle, and permanently-disabled Export/Share all verified correct. | Changed `getExecutiveDashboard` (`executiveDashboard.api.js`) to also treat a non-JSON `200` response (checked via `content-type`) as "unable to load," and simplified the `catch` block to always fall back to mock data rather than distinguishing by error type — so a dev environment with no API proxy (or any non-JSON response) now correctly shows sample data instead of a hard error screen. `dashboard.api.js`'s identical pre-existing pattern was deliberately left unfixed here (out of scope for this unit per agent-rules.md §9) and recorded as an Open Issue below. | PASS after fix — rerun `npx vite build`: clean. Fix verified by code review of the new content-type/catch logic (browser-automation dependency used for the original repro run was a temporary, non-persisted tester install, not carried forward). |
| 2026-08-22 | SCR-014 Executive Dashboard Screen — screen-local footer follow-up (explicit user request via IDE selection: "create a dynamic footer for screen") | n/a | User asked for a "dynamic footer" after SCR-014 was already `DONE`. Clarified via AskUserQuestion since `AppShell`'s existing global `Footer` (fixed pipeline/worker/queue stats, shared across every screen) already renders below every page including this one — user confirmed the ask was a screen-specific footer bar inside this screen's own content, reflecting this screen's own live data, distinct from the global shell footer. | Added `ScreenFooter` to `ExecutiveDashboardScreen.jsx`, rendered below `BusinessUnitAdoptionTable`, inside the screen's own content (above the global shell `Footer`). Reads real values already in the query payload — `data.updatedAt`, `data.mocked`, the active `dateRange` state, `data.executionVolume.stats.totalThisMonth`, and a live count of `data.alerts` with `severity === 'critical'` — so it changes on every refetch/date-range switch rather than being a static caption. Shows a spinner-dot + "Refreshing…" while `isFetching`, and the same "Sample data" disclosure convention used elsewhere on this screen when `data.mocked` is true. No new mock/fabricated values were introduced. | PASS — rerun `npx vite build` and `npx oxlint`: both clean. |
| 2026-08-22 | SCR-015 Pipeline Overview Dashboard Screen | ui-reviewer | Figma fidelity (node `49:1793`, split into targeted `get_design_context` sub-node calls during the build since the full-frame call exceeded the response size limit — same precedent as SCR-014; reviewer's own session had no working Figma MCP call, so it cross-checked against this build's captured structure and the already-verified sibling `ExecutiveDashboardScreen.jsx`), accessibility, token/convention compliance, real-vs-decorative interactivity check | FAIL (pre-fix) — 1 blocker. The header's Today/7d/30d date-range toggle was purely decorative: `dateRange` state only drove the pill's own `aria-pressed`/active styling and was never passed into `usePipelineOverview()` or `getPipelineOverview()`, so clicking it had zero effect on any KPI/summary/chart/table — unlike the sibling `ExecutiveDashboardScreen.jsx`'s `useExecutiveDashboard(dateRange)`, which genuinely threads the range into the query key (with its own mock-limitation disclosure). 2 low, non-blocking: inline SVG chart fills use literal hex (`#0f5699`/`#dc2626`) matching `--color-primary`/`--color-danger` exactly — same pre-existing, previously-accepted SVG-attribute constraint as `ExecutiveDashboardScreen.jsx`'s charts; `chartPoints`/`linePath`/`Legend`/`CardHeader` are duplicated from `ExecutiveDashboardScreen.jsx` rather than extracted to a shared module. All other checks passed clean: focus-visible on every interactive control, `aria-live` status region, table `scope="col"`/`sr-only` labels on icon-only columns, consistent `disabled`+`title` pattern on every unimplemented action, genuine (non-decorative) search/status-filter/checkbox/pagination state in the All Pipelines table, correct mock-fallback API shape mirroring `executiveDashboard.api.js`. | Threaded `dateRange` through `usePipelineOverview(dateRange)` into the React Query key and `getPipelineOverview(dateRange)`'s `?range=` query param, mirroring `useExecutiveDashboard`'s pattern exactly; added a code comment in `pipelineOverview.api.js` disclosing that the mock snapshot ignores the range, same precedent as `executiveDashboard.api.js`. Left the two low findings as documented, non-blocking notes per agent-rules.md §9 (SVG hex constraint is pre-existing/accepted; chart-primitive extraction deferred until a third MOD-009 screen needs it). | PASS after fix — rerun `npx vite build`: clean. |
| 2026-08-22 | SCR-015 Pipeline Overview Dashboard Screen | tester | lint (`oxlint`), build (`vite build`), live dev-server HTTP checks (route response, all 18 new icon SVGs + 1 shared icon asset resolution, mock-fallback content-type guard verified against the dev server's own SPA-fallback HTML) | PASS (static/structural layers) — no browser-automation tool (Playwright/Puppeteer/CDP) was available in that session to click through the search/status-filter/checkbox/pagination interactions or capture a screenshot at the 1440px reference viewport; recorded as a documented tooling gap (same class as SCR-005–014's "Figma MCP call did not resolve" notes), not a failure. `npx oxlint`: 0 warnings/errors. `npx vite build`: clean (only the pre-existing chunk-size advisory). All 18 new SVGs under `assets/icons/pipeline-overview/` plus the shared `icon-alert-triangle.svg` returned `200` over the dev server; `/dashboard/pipelines` served the SPA shell correctly with the right component wired at `router.jsx`. Confirmed live that hitting `/api/dashboard/pipelines` without a backend returns the Vite SPA's own `text/html` fallback (200 OK), correctly rejected by `getPipelineOverview`'s content-type guard, forcing the mock path — same dev-proxy gotcha already fixed at SCR-014. Static code review of the search/filter/checkbox/pagination state (`useMemo` filtering, page-reset-on-filter-change, `Set`-based row/select-all toggling, clamped pagination) found the logic correctly implemented, though not exercised live. | none needed for the passing layers; recommended (not applied) enabling a browser-automation MCP tool in a future session to close the live-interaction verification gap | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than assumed — same standard as SCR-003 through SCR-014's tester entries). |
| 2026-08-22 | SCR-016 Data Quality Dashboard Screen | ui-reviewer | Figma fidelity (node `50:4167`, split into 11 targeted `get_design_context` sub-node calls during the build — same size-limit precedent as SCR-014/015; reviewer's own session had no working Figma MCP call, so it cross-checked against this build's captured structure and the already-verified sibling `PipelineOverviewScreen.jsx`), accessibility, token/convention compliance, mock-boundary correctness, icon-asset resolution, interactive-state correctness | PASS (1 low, non-blocking, pre-existing). All 17 icon imports (6 new + 11 reused) confirmed resolving to real files. `dataQuality.api.js`'s mock-fallback pattern confirmed identical to `pipelineOverview.api.js` (real-fetch-then-mock, content-type guard). Focus-visible states, `aria-pressed`/`aria-label`/`scope="col"`/`role="status" aria-live="polite"` region all present and matching sibling-screen convention — no gaps found. Failed Validations table's search/severity-filter/pagination/selection logic traced correct (filter resets page to 1 and clears selection, `clampedPage` guards stale pages, "Showing X–Y of Z" math correct, Previous/Next disable correctly at bounds). 1 low: the owner-initials avatar's `h-5.5 w-5.5` class (not a valid Tailwind utility — no `5.5` step in the default or project spacing scale) was copy-pasted verbatim from the already-shipped, already-reviewed `PipelineOverviewScreen.jsx:793` into this screen — inherited, not introduced here. | Left the `h-5.5 w-5.5` finding as a documented Open Issue rather than fixed in only one of the two screens, per agent-rules.md §9 scope control (fixing it here alone would create inconsistency with the sibling screen; both should be fixed together in a future pass). | PASS — no fix applied this pass (documented, non-blocking, pre-existing). |
| 2026-08-22 | SCR-016 Data Quality Dashboard Screen | tester | lint (`oxlint`), build (`vite build`), live dev-server HTTP checks (route response, all 6 new + 11 reused icon SVG asset resolution, mock-fallback content-type guard verified against the dev server's own SPA-fallback HTML) | PASS (static/structural layers) — no browser-automation tool (Playwright/Puppeteer/CDP) was available in that session to click through the search/severity-filter/checkbox/pagination interactions; recorded as a documented tooling gap (same class as SCR-005–015's notes), not a failure. `npx oxlint`: 0 errors in new files (1 unrelated pre-existing warning in `AppShell.jsx`). `npx vite build`: clean (234 modules transformed, only the pre-existing chunk-size advisory). All 6 new SVGs under `assets/icons/data-quality/` confirmed present/non-empty/valid; all 17 icon import paths resolve to real files. `/dashboard/data-quality` served the SPA shell correctly with `DataQualityScreen` wired at `router.jsx`. Confirmed live that hitting the data-quality endpoint without a backend returns the Vite SPA's own `text/html` fallback (200 OK), correctly rejected by `getDataQuality`'s content-type guard, forcing the mock path. | none needed for the passing layers; recommended (not applied) enabling a browser-automation MCP tool in a future session to close the live-interaction verification gap | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than assumed — same standard as SCR-003 through SCR-015's tester entries). |
| 2026-08-22 | SCR-017 System Health Dashboard Screen | ui-reviewer | Figma fidelity (node `51:6763`, split into 12 targeted `get_design_context` sub-node calls during the build since the full-frame call exceeded the response size limit — same precedent as SCR-014/015/016; reviewer's own session had no Figma MCP access, cross-checked structurally against this build's captured content and sibling `DataQualityScreen.jsx`/`PipelineOverviewScreen.jsx`), accessibility, token/convention compliance, icon-asset resolution, real-vs-decorative interactivity check | FAIL (pre-fix) — 1 high, 1 low. High: `InfrastructureComponentsTable` implemented full row/select-all checkbox state but never surfaced a "N selected" bulk-action feedback bar, unlike both sibling tables (`PipelineOverviewScreen`'s pipeline-selection bar, `DataQualityScreen`'s issue-selection bar). Low: `UtilizationTrendCard`'s footer grid used a dead-code ternary (`data.used ? 'grid-cols-3' : 'grid-cols-3'`, both branches identical). Additional non-blocking findings: duplicate "Refresh Status" + icon-only-refresh header controls both calling the same `refetch()` with no distinct behavior (medium, fixed post-review by removing the redundant icon-only button); `TREND_TONE` key naming (`up`/`down`) encodes arrow direction rather than sentiment, a latent authoring footgun for future editors (low, not fixed — documented here). All other checks passed clean: mock-fallback API boundary documented identically to sibling `.api.js` files, real (non-decorative) search/category-filter/checkbox/pagination state in the Infrastructure Components table, loading/error/empty states present, `aria-live` status region, focus-visible on every interactive control, all reused shared icons (refresh, export, alert-sm, gear-sm, columns, filter, search) correctly resolved rather than duplicated, all 11 new icon SVGs confirmed real Figma-exported assets (not hand-authored). | Added the missing bulk-selection feedback bar (Acknowledge/Restart, both disabled with the standard "requires MOD-009" tooltip) to `InfrastructureComponentsTable`, matching sibling class structure/copy exactly. Simplified the dead-code ternary to a plain string. Removed the redundant icon-only refresh button (kept "Refresh Status" as the sole refresh control, matching the one-refresh-affordance convention every sibling screen follows) and its now-unused `icon-refresh` import. Left the `TREND_TONE` naming footgun as a documented, non-blocking note (Open Issues below) rather than renaming out of scope. | PASS after fixes — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-22 | SCR-017 System Health Dashboard Screen | tester | lint (`oxlint`), build (`vite build`), live dev-server HTTP checks (route response, all 11 new + 8 reused icon SVG asset resolution, mock-fallback content-type guard verified against the dev server's own SPA-fallback HTML), icon file-integrity check (structural SVG validity, non-empty) | PASS. `npx oxlint`: 0 errors in new files (1 unrelated pre-existing warning in `AppShell.jsx`). `npx vite build`: clean (248 modules transformed, only the pre-existing chunk-size advisory). All 11 new SVGs under `assets/icons/system-health/` confirmed structurally valid (balanced `<svg>...</svg>`, 258–1307 bytes) and non-empty; all 19 unique icon import paths (new + reused from `pipeline-overview`/`data-quality`/`executive-dashboard`) resolve to real files, confirmed live via dev server (`200` on every path). `/dashboard/system-health` served the SPA shell correctly with `SystemHealthScreen` wired at `router.jsx`; sidebar nav item confirmed registered in `navConfig.js` with `PERMISSIONS.ANALYTICS_READ`. Confirmed live that hitting `/api/dashboard/system-health` without a backend returns the Vite SPA's own `text/html` fallback (200 OK), correctly rejected by `getSystemHealth`'s content-type guard, forcing the mock path — same dev-proxy pattern as sibling screens. No browser-automation tool was available to click through the search/category-filter/checkbox/pagination interactions; recorded as a documented tooling gap (same class as SCR-005–016's notes), not a failure. | none needed | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than assumed — same standard as SCR-003 through SCR-016's tester entries). |
| 2026-08-22 | SCR-018 Source Health Dashboard Screen | ui-reviewer | Figma fidelity (node `56:11333`, split into targeted `get_design_context` sub-node calls during the build — same size-limit precedent as SCR-014–017; reviewer's own session had no Figma MCP access, cross-checked against this build's captured content and the recon report against sibling `SystemHealthScreen.jsx`), accessibility, token/convention compliance, mock-boundary correctness, icon-asset resolution | PASS (1 medium fixed, 2 low non-blocking). Medium: `ConnectionHealthCard`'s mock data defined `timeoutRate`/`avgLatency` fields never rendered by the component — dead data vs. every sibling screen's fully-consumed mock objects. Fixed by extending the stat-tile row to a two-row layout consuming both fields, matching `SourcePerformanceCard`'s existing tile-grid pattern in the same file. All 25 icon imports resolve to real files; mock-fallback shape identical to `systemHealth.api.js`; `useSourceHealth.js` matches `useSystemHealth.js`'s React Query pattern; accessibility (`role="status"`/`aria-live`, `aria-pressed`, `scope="col"`, `aria-label`, `focus-visible`) all present and consistent with sibling convention; all disabled/mock-boundary buttons carry explanatory `title` attributes naming the blocking module (MOD-006/MOD-009). 2 low, non-blocking, not introduced by this build: `iconCheck` sourced from the flat `assets/icons/check.svg` rather than a per-screen subfolder (breaks the file's own per-screen icon-folder convention, functionally correct); bulk-action bar's `Test Connections`/`Disable` buttons lack `focus-visible` classes, mirroring the identical pre-existing gap in `SystemHealthScreen.jsx`'s bulk bar. | Extended `ConnectionHealthCard`'s stat grid to add Timeout Rate/Avg Latency tiles consuming the previously-dead mock fields. Left the two low findings as documented, non-blocking, pre-existing/repo-wide notes per agent-rules.md §9 scope control. | PASS after fix — rerun `oxlint`: clean. |
| 2026-08-22 | SCR-018 Source Health Dashboard Screen | tester | lint (`oxlint`), build (`vite build`), live dev-server + Playwright browser check of `/dashboard/source-health` (icon asset resolution, mock-fallback content-type guard, console-error check, KPI-to-icon mapping trace) | PASS. `npm run lint` and `npm run build` both clean (255 modules transformed, only the pre-existing chunk-size advisory). All 4 new SVGs under `assets/icons/source-health/` confirmed structurally valid and non-empty; all 22 icon import paths resolve to real files, confirmed live via dev server. `/dashboard/source-health` served correctly with `SourceHealthScreen` wired at `router.jsx`; sidebar nav item confirmed registered in `navConfig.js` with `PERMISSIONS.ANALYTICS_READ`. Live Playwright load rendered the full screen (KPI cards, charts, alerts, auth-status widget, tables, sidebar nav highlighted, breadcrumb) with zero `pageerror`/console errors. Confirmed live that hitting the source-health endpoint without a backend returns the Vite SPA's own `text/html` fallback, correctly rejected by `getSourceHealth`'s content-type guard, forcing the mock path (visually confirmed via the "Sample data" badge). KPI icon-key-to-SVG mapping traced correct via DOM inspection (Auth Errors → key icon, Failed Connections → wifi icon, etc.). | none needed | PASS. |
| 2026-08-23 | SCR-019 Destination Health Dashboard Screen | ui-reviewer | Structure/token/state/accessibility review against the SCR-018 pattern-of-record (`SourceHealthScreen.jsx`), mock-boundary correctness, icon-asset resolution, routing/nav wiring. No Figma MCP available this session — Figma fidelity to node `65:17501` flagged `not-verified` per agent-rules.md §2, reviewed against the sibling convention + design tokens instead. | PASS (no blocker/high/medium; 5 low/informational). All 20 icon imports resolve to real files; `KPI_ICON` covers every `icon` key in the mock data. Mock boundary in `destinationHealth.api.js` structurally identical to `sourceHealth.api.js` (real request → content-type guard → `mocked` flag), "Sample data" badge gated on `data?.mocked` in header + footer. Hook matches sibling (range in query key, 30s refetch). Route + nav wired under Analytics/`ANALYTICS_READ`. All states present (loading skeleton `aria-hidden`, error `role="alert"`+Retry, empty states, `sr-only role="status" aria-live="polite"`). Accessibility: `aria-pressed` toggles, `scope="col"` headers, per-row/select-all checkbox `aria-label`, pagination `aria-label`/`aria-current`, disabled-button `title`, focus-visible everywhere, h1→h2. Only raw hex is the two pre-accepted `stroke="#0f5699"` inline-SVG chart strokes. Data reconciles (38+32+24+34=128; 121+5+2=128). 5 low/informational: (1) Figma fidelity `not-verified` (expected, documented); (2) reviewer couldn't read `docs/agent-rules.md` from its cwd — file exists at repo root, cited anchors are valid; (3) BI/Webhook sample rows unreachable via category chips — the documented MOD-007 destination-taxonomy caveat, acceptable for mock; (4) `updateSearch` doesn't clear selection (identical to sibling `AllSourcesTable` — consistency-preserving, fix both together if ever); (5) dead `linkLabel` branch in `CardHeader`, carried verbatim from sibling for parity, harmless. | None — no defects warranted a reviewer fix; the two behavioral nits are intentional carryovers from the SCR-018 pattern-of-record (changing only SCR-019 would break the parity the review checks for). | PASS. |
| 2026-08-23 | SCR-019 Destination Health Dashboard Screen | tester | lint (`oxlint`), build (`vite build`), icon-asset resolution (20 imports), route/nav wiring, live `vite preview` HTTP check of `/dashboard/destination-health` + mock-fallback content-type guard against the preview server's own SPA HTML fallback | PASS (static/structural + HTTP layers). `npx oxlint` clean for the unit (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings). `npm run build` clean (258 modules transformed, only the pre-existing chunk-size advisory). All 20 icon import paths in `DestinationHealthScreen.jsx` confirmed resolving to real files on disk. `/dashboard/destination-health` served the SPA shell (`HTTP 200 text/html`) wired to the real `DestinationHealthScreen` (not a placeholder); nav item present under Analytics. Confirmed live via `vite preview` that hitting `/api/dashboard/destination-health` without a backend returns the server's own `text/html` SPA fallback (`HTTP 200`), correctly rejected by `getDestinationHealth`'s content-type guard, forcing the mock path. No browser-automation tool available this session to click through the search/filter/pagination/checkbox interactions — documented tooling gap (same class as SCR-005–017's notes), not a failure; interactive state/handler code independently reviewed and confirmed correctly wired by the ui-reviewer. No test runner configured in this repo (pre-existing gap). | none needed | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than assumed — same standard as prior tester entries). |
| 2026-08-24 | SCR-020 Execution Statistics Dashboard Screen | tester | lint (`oxlint`), build (`vite build`), data-contract verification (mock↔component key coverage), icon-key resolution (18 KPI/insight icons), SVG-calc NaN check, route/nav wiring | PASS. `npm run lint` clean for the unit (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings). `npx vite build` clean (only the pre-existing chunk-size advisory). Every mock key (`headline`/`summary`/`kpis`/`volumeTrend`/`outcomeBreakdown`/`runtimePerformance`/`environmentStats`/`mostExecuted`/`highestFailure`/`scheduleAnalytics`/`insights`/`executions`) is consumed by `ExecutionStatisticsScreen.jsx`; all 18 KPI/insight icon keys resolve through `KPI_ICON`/`INSIGHT_TONE`; no NaN in the inline-SVG `trendPath` math. `/dashboard/executions` wired to the real screen at `router.jsx`. One non-blocking note: `volumeTrend` carries 14 data points against 7 axis ticks — the extra points render as a denser polyline, cosmetic only. No browser-automation/test runner in repo (pre-existing gap). | none needed | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than assumed — same standard as prior tester entries). |
| 2026-08-24 | SCR-020 Execution Statistics Dashboard Screen | ui-reviewer | Structure/token/state/accessibility review of node `84:5098` (Figma content transcribed during the build via a live `get_metadata`+`get_screenshot` pass, so content fidelity is `verified` — unlike SCR-018/019; reviewer's own session had no Figma MCP, cross-checked against the captured transcription + sibling MOD-009 convention), mock-boundary correctness, chart honesty, accessibility | PASS after fixes (4 medium, 2 low). Medium #1: `VolumeTrendCard`'s Failed line was rescaled by an invented `successMax/max(failed)/12` divisor so it no longer corresponded to the labelled 0–6000 axis — misrepresents data. Medium #2: headline quoted `98.3%` success while summary/KPI quoted `98.9%` (a genuine internal inconsistency in the Figma source, agent-rules.md §3). Medium #4: `purple` outcome/insight keys render blue via the shell-accent token (no purple token exists). Low #6: `headline.context` was rendered twice (sub-h1 line + `HeadlineBanner`). Low #7: footer mislabelled the period failure total as `(24h)`. | Fixed #1: both series now plot on the real shared 0–axisMax scale (Failed rides low, which is truthful) — removed the invented divisor and added an SVG `aria-label` describing both series' true ranges. Fixed #2: reconciled the headline to `98.9%` (38,037/38,452 = 98.92%, the arithmetically correct figure) and documented the `98.3%` Figma typo as a FIGMA-SOURCE CONFLICT block in `executionStatistics.api.js` per agent-rules.md §3 (conflict surfaced, not silently chosen). Fixed #6: dropped the duplicate sub-h1 render, replaced with a static screen description; `HeadlineBanner` remains the single home for `headline.context`. Fixed #7: relabelled to `{failed} failed this period`. #4 (purple→blue) left as a documented open issue below — no purple token exists project-wide, adding one is out of this screen's scope (agent-rules.md §9). | PASS after fixes — rerun `npm run lint` + `npx vite build`: both clean. |
| 2026-08-24 | SCR-021 Error Analytics Dashboard Screen | tester | graphify orientation, lint (`oxlint` full + scoped), build (`vite build`), data-contract verification (mock↔component key coverage across all tone/badge maps), icon-asset resolution (17 SVG imports), undefined-lookup/missing-key trace, route wiring, sibling-pattern conformance | PASS. `npm run lint` clean for the unit (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings); scoped `npx oxlint` on the three new files clean (no unused imports). `npm run build` clean (264 modules transformed, only the pre-existing chunk-size advisory). All 17 icon imports resolve to real files under `assets/icons/` (a strict subset of SCR-020's set + `data-quality/icon-alert-circle.svg`). `KPI_ICON` covers every `kpi.icon` value; all tone/badge maps (`KPI_TONE`/`BAR_TONE`/`SEVERITY_BADGE`/`INCIDENT_STATUS`/`ERROR_STATUS`/`DELTA_TONE`/`SUMMARY_TONE`/`TREND_TONE`) cover the data with `?? fallback` on each risky lookup; `ScreenFooter`'s `headline.stats.find(key==='critical'/'open')` both resolve. Hook + service mirror the SCR-020 sibling (real-fetch-first → content-type guard → `mocked` flag; `range` in query key + 30s refetch). `/dashboard/errors` wired to the real `ErrorAnalyticsScreen` at `router.jsx:72` (replaced the SCR-021 placeholder), nav item already present under Operations/`OPERATIONS_READ`. No TS/test runner in repo (pre-existing gap). | none needed | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than assumed — same standard as prior tester entries). |
| 2026-08-24 | SCR-021 Error Analytics Dashboard Screen | ui-reviewer | graphify orientation, structure/token/state/accessibility review of node `69:37077` (Figma content transcribed during the build via a live `get_metadata`+`get_screenshot` pass, so content fidelity is `verified` — reviewer's own session had no Figma MCP, cross-checked code↔captured transcription + sibling SCR-020 convention), data-integrity math, chart honesty, mock-boundary correctness | PASS (no blocker/major; 4 minor). Data integrity: severity counts (12+19+86+112+19=248), root-cause counts, and category counts each sum to exactly 248, matching Total Errors and the "of 248" table total; Critical=12/High=19 reconcile with the headline banner; all 12 KPI values match the transcribed Figma text nodes; all 8 sections + the "1–8 of 248" 8-row table present. Chart honesty: both `VolumeTrendCard` series share the real 0–axisMax scale (Critical genuinely rides low) — carries the SCR-020 Medium #1 fix forward. Accessibility: charts `role="img"`+descriptive `aria-label`, decorative elements `aria-hidden`, `scope="col"` headers, `sr-only` labels + `aria-label` on search/each `FilterSelect`, pagination `aria-current="page"`/labeled prev-next, focus-visible everywhere, h1→h2, `role="status" aria-live="polite"` live region, `role="alert"` error block. Consistency with SCR-020 confirmed on every convention; `PAGE_SIZE=8` (vs 10) correctly reflects this frame's 8 rows. 4 minor, all low-risk and 3 inherited-by-design from the approved SCR-020 pattern: (1) `text-token-xs` (9px) on real table metadata — matches sibling, best fixed as a project-wide token pass not screen-scoped; (2) `text-warning` small text below AA 4.5:1 on Open/Mitigating badges + footer — same sibling pattern; (3) permanently-disabled red "Create Incident" primary (MOD-008 PLANNED) — honest, `title`-mitigated, same disabled+title precedent as SCR-006/012/013/020; (4) range toggle vs. static snapshot — documented mock limitation, same as sibling. | None — no defects warranted a reviewer fix; the four minors are low-risk carryovers from the SCR-020 pattern-of-record (findings 1–2 are best addressed as a project-wide token/contrast pass, not a screen-scoped patch that would break parity). | PASS after review — `npm run build` clean. |
| 2026-08-24 | SCR-022 Performance Analytics Dashboard Screen | tester | graphify orientation, lint (`oxlint` full + scoped), build (`vite build`), data-contract verification (mock↔component key coverage across all tone/icon/badge maps), icon-key resolution, filter-logic data-flow trace, route/nav wiring, sibling-pattern conformance | FAIL (pre-fix) — 1 real data-flow bug. `ComponentsTable`'s Environment filter tested `row.environment !== envFilter`, but `MOCK_PERFORMANCE_ANALYTICS.components.rows` carried no `environment` field, so selecting Production/Staging/Development emptied the table (the "All" default masked it). FIXED by adding an `environment` field (Production/Staging/Development) to all 8 component rows in `performanceAnalytics.api.js`; re-ran `npm run lint` — clean. Otherwise: `npm run build` clean (only the pre-existing chunk-size advisory); every mock key (`headline`/`kpis`/`scoreTrend`/`resourceUtilization`/`runtime`/`throughput`/`fastestPipelines`/`regressions`/`workers`/`capacity`/`recommendations`/`components`) is consumed; all 12 `KPI_ICON` keys + every tone/badge map resolve with fallbacks; hook + service mirror the SCR-020/021 sibling (real-fetch-first → content-type guard → `mocked` flag; `range` in query key + 30s refetch). `/dashboard/performance` wired to the real `PerformanceAnalyticsScreen` at `router.jsx` (replaced the SCR-022 placeholder). Secondary non-defect: `categoryFilters` lists Validate/Route which no visible row uses, but `category` exists on every row so empty results there are legitimate with only 8 of 142 rows mocked. No browser-automation/test runner in repo (pre-existing gap). | Added `environment` to all 8 `components.rows`. | PASS after fix — rerun `npm run lint` clean (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |
| 2026-08-24 | SCR-022 Performance Analytics Dashboard Screen | ui-reviewer | graphify orientation, structure/token/state/accessibility review of node `70:43606` (Figma content transcribed during the build via a live `get_metadata`+`get_screenshot` pass, so content fidelity is `verified`; reviewer's own session had no Figma MCP, cross-checked code↔captured transcription + sibling SCR-020/021 convention), chart honesty, mock-boundary correctness | PASS (no blocker/major; 4 low, all inherited-by-design from the approved SCR-020/021 pattern-of-record). Every KPI value, chart series, resource-utilization row, runtime/throughput stat, ranked pipeline, regression, worker row, capacity forecast, recommendation, and the 8 component rows transcribed from the actual frame's text nodes. `ScoreTrendCard`'s dual-line series both plot on the real shared 0–100 scale (score/SLA both truthful); `ThroughputCard`'s bar chart shares its 0–axisMax scale. Accessibility: charts `role="img"`+descriptive `aria-label`, decorative elements `aria-hidden`, `scope="col"` headers, `sr-only` labels + `aria-label` on search/each `FilterSelect` (Category/SLA/Environment), pagination `aria-current="page"`/labeled prev-next, `aria-pressed` range toggle, focus-visible everywhere, h1→h2, `role="status" aria-live="polite"` live region, `role="alert"` error block. 4 low: (1) `text-token-xs` (~9px) dense metadata + small `text-warning` badges below AA 4.5:1 contrast — same sibling pattern, best fixed as a project-wide token/contrast pass; (2) permanently-disabled "Export Analytics"/"Generate Report" primaries unfocusable so `title` is mouse-only — same disabled+title precedent as SCR-006/012/013/020/021; (3) range toggle ignored by fixed mock — documented mock limitation, same as siblings; (4) `ScoreTrendCard` renders 8 tick labels beneath a 14-point series (cosmetic, same class as SCR-020's polyline note). | None — no defects warranted a reviewer fix; the four lows are low-risk carryovers from the SCR-020/021 pattern-of-record (findings 1 best addressed as a project-wide token/contrast pass, not a screen-scoped patch that would break parity). The tester's environment-filter data-flow bug was the only real defect, fixed in the tester pass above. | PASS after review — `npm run build` clean. |
| 2026-08-24 | SCR-023 Real-Time Monitoring Dashboard Screen | ui-reviewer | graphify orientation, structure/token/state/accessibility review of node `72:45894` (Figma content transcribed during the build via a live `get_metadata`+`get_screenshot` pass, so content fidelity is `verified`; reviewer's own session had no Figma MCP, cross-checked code↔captured transcription + sibling SCR-014–022 convention), "live"-simulation honesty, mock-boundary correctness | PASS (with findings; no blockers). 1 medium: `connectivity` declared `connected:9, reconnecting:1, failed:3` in its summary counters, but the rendered remote+local connection rows totalled 8 connected / 1 reconnecting / 1 failed — a self-contradiction within the same card between the headline counts and the enumerated rows. 2 low: the monitoring-table search placeholder ("Search by component, execution ID, worker…") overpromised, since the filter only matched `name`/`owner`; the `liveActivity` status-filter chips lacked a `Completed` option even though `Completed` rows exist in the data, making those rows unreachable via the filter UI. Accessibility passed clean: `role="status" aria-live="polite"` live region reflecting poll/refresh phase, `role="alert"` error block, `aria-pressed` on the Pause/Resume + range toggles, `aria-label` on the icon-only Refresh, inline-SVG throughput sparkline as `role="img"` with descriptive `aria-label`, `scope="col"` headers, `sr-only` labels + `aria-label` on the search/three `FilterSelect`s, pagination `aria-current="page"`/labeled prev-next, focus-visible everywhere, h1→h2. Charts hand-drawn as inline-SVG primitives (no charting lib, same precedent as SCR-014–022). | Corrected `connectivity` counters to `connected:8, reconnecting:1, failed:1` to match the enumerated rows exactly. Narrowed the monitoring-table search placeholder to "Search by component or owner…" to match the actual filter fields. Added `Completed` to `liveActivity.statusFilters` so `Completed` rows are reachable (now `['All','Running','Completed','Failed','Queued']`). Disabled "Export Snapshot" primary carries an explanatory `title` naming the blocking PLANNED modules — same disabled+title precedent as SCR-006/012/013/020/021/022. | PASS after fixes — rerun `npm run build` and `npm run lint`: both clean. |
| 2026-08-24 | SCR-023 Real-Time Monitoring Dashboard Screen | tester | graphify orientation, lint (`oxlint` full + scoped), build (`vite build`), data-contract verification (mock↔component key coverage across every tone/icon/badge map), filter/pagination/status-filter logic trace, route/nav wiring, sibling-pattern conformance, mock-fallback content-type-guard consistency vs. `executionStatistics.api.js`/`systemHealth.api.js` | PASS. `npm run build` clean (only the pre-existing chunk-size advisory); `npm run lint` clean. Every mock key (`status`/`kpis`/`liveActivity`/`activePipelines`/`workerQueue`/`infrastructure`/`throughput`/`connectivity`/`eventStream`/`alerts`/`monitoringTable`) is consumed; all 10 `KPI_ICON` keys + every tone/badge map (KPI_TONE, TREND_TONE, STATUS_METRIC_TONE, ACTIVITY_BADGE, PIPELINE_STATUS_TONE, UTIL_TONE, INFRA_TONE, CONN_TONE, EVENT_TONE, ALERT_TONE, TABLE_STATUS_TONE) resolve with fallbacks. `useRealTimeMonitoring(range, {live})` mirrors the sibling hook (real-fetch-first → content-type guard → `mocked` flag; `range` in the query key; 15s `refetchInterval` gated by the `live` toggle so Pause halts polling). `/dashboard/realtime` wired to the real `RealTimeMonitoringScreen` at `router.jsx` (replaced the SCR-023 `shellRoute` placeholder). Monitoring-table search + three filters (type/status/environment) + pagination logic traced correct; filters reset to page 1. `liveActivity` status-filter now includes `Completed` (reachable). First tester run aborted on a transient Cloudflare 524 origin timeout after confirming icons/lint; re-launched and completed clean. No browser-automation/test runner in repo (pre-existing gap). | Confirmed the ui-reviewer's three fixes (connectivity counters, search placeholder, `Completed` filter) landed and rebuild clean; no additional tester-found defect. | PASS — `npm run build` and `npm run lint` both clean (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |
| 2026-08-24 | SCR-024 Organization List Screen | ui-reviewer | graphify orientation, structure/token/state/accessibility review of node `90:8293` (Figma content transcribed during the build via a live `get_metadata`+`get_screenshot` pass, so content fidelity is `verified`; reviewer's own session had no Figma MCP, cross-checked code↔captured transcription + `PipelineOverviewScreen` pattern-of-record), MOD-004 mock-boundary correctness, nav-reachability | FAIL (pre-fix) — 1 high (gates PASS) + 3 medium + 2 low. HIGH: the footer's "(filtered from …)" clause was gated on `filtered.length !== data.total`, always true because the mock ships 12 rows against `total:1284`, so the default unfiltered view rendered `Showing 1–12 of 12 (filtered from 1,284)` instead of Figma's `Showing 1–12 of 1,284` — a fidelity mismatch + misleading "filtered" wording. MEDIUM #2: no Sidebar entry for `/organizations` — the screen's own `Administration › Organizations` breadcrumb pointed nowhere; reachable only by typing the URL. MEDIUM #3: `text-token-xs` (9px) on load-bearing org ID + admin email + plan/status badge text, which `tokens.css` reserves for decorative micro-labels (9px flagged as a defect in SCR-001). MEDIUM #4: disabled-button rationale conveyed only via `title` on `disabled` controls (unfocusable, mouse-only) — pattern-of-record a11y gap. LOW #5: dead `KPI_TONE[*].iconWrap` keys (KpiGrid renders no icon). LOW #6: select-all checkbox had no indeterminate state for a partial selection. Fidelity otherwise passes: KPI tone mapping, all 12 columns, colored avatars, plan badges, status pills, "No admin assigned" fallback, toolbar, bulk bar, and responsive grid all match. | Fixed HIGH: footer now shows `of {data.total}` with no suffix when no filters are active, and only appends `(filtered from {total})` when `hasActiveFilters`. Fixed #2: added an `organization:manage` permission to `permissions.js`, an `Administration › Organizations` leaf to `navConfig.js` (gated on it), and a `nav-organizations.svg` asset wired into `navIcons.js`. Fixed #3: ID/email/plan-badge/status-pill text raised from `text-token-xs` to `text-token-meta` (11px). Fixed #5: removed the unused `iconWrap` keys. Fixed #6: select-all now uses per-page id-set membership (`pageRows.every(has)`) with an `indeterminate` ref for partial selections, and `toggleAll` adds/removes only the current page's ids (correct for a future multi-page dataset). #4 (disabled+title) left as a documented open issue — same pattern-of-record a11y limitation as SCR-006/012/013/020/021/022, best fixed project-wide (agent-rules.md §9). | PASS after fixes — rerun `npm run build` and `npm run lint`: both clean. |
| 2026-08-24 | SCR-024 Organization List Screen | tester | graphify orientation, lint (`oxlint`), build (`vite build`), full data-shape contract trace (mock↔hook↔screen), tone-map coverage (KPI_TONE/ORG_TONE/PLAN_TONE/STATUS_TONE), client-side search/filter/pagination/select-all logic trace, "Showing X–Y of N" math, mock-fallback content-type-guard consistency vs. sibling MOD-009 api modules | PASS (1 low cosmetic, since fixed). `npm run build` clean (only pre-existing chunk-size advisory); `npm run lint` clean (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings). Every field the screen reads exists in `MOCK_ORGANIZATION_LIST`: `kpis[].key/label/value/helper/tone`, `statusFilters`/`planFilters`/`regionFilters`, `total`, `updatedAt`, `mocked`, and all 13 row fields across all 12 rows (null `adminName/adminEmail` on `org_8hpsr7m` handled by the `?:` guard + optional-chaining in search). All four tone maps cover every mock value and carry `?? fallback`. Filters (search/status/plan/region, chip clears, both Clear-all paths) reset to page 1 + clear selection; page clamping via `Math.max/Math.min`; "Showing X–Y of N" arithmetic zero-guarded and correct. Content-type guard `!res.ok || !content-type?.includes('application/json')` is byte-identical to the sibling MOD-009 modules; `catch → {...MOCK, mocked:true}` matches. `/organizations` → `OrganizationListScreen` wired in `router.jsx`. The one low cosmetic defect (misleading "filtered from" footer with no active filters) was reported and then fixed in the same pass. No unit/test framework/tsconfig/browser-automation in repo (pre-existing gap). | Applied the footer-wording fix (gate the "(filtered from …)" suffix on `hasActiveFilters`, not the count mismatch); rebuild clean. | PASS — `npm run build` and `npm run lint` both clean (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |

| 2026-08-24 | SCR-025 Organization Details Screen | tester | lint (`oxlint`), build (`vite build`) | PASS. `npm run lint` clean for the unit (only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings); zero warnings in any touched file (`OrganizationDetailsScreen.jsx`, `organizationDetails.api.js`, `useOrganizationDetails.js`, `router.jsx`, `OrganizationListScreen.jsx`). `npm run build` clean (277 modules, only the pre-existing chunk-size advisory). No test runner / tsconfig / browser-automation in repo (pre-existing gap) — no unit/type/e2e layer applies. | none needed | PASS (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |
| 2026-08-24 | SCR-025 Organization Details Screen | ui-reviewer | graphify orientation, structure/token/state/accessibility review of node `91:9827` (Figma content transcribed during the build via a live `get_design_context`+`get_screenshot` pass, so content fidelity is `verified`; reviewer's own session cross-checked code↔`scr025.png` + the SCR-024 `OrganizationListScreen`/`organizationList.api.js` pattern-of-record), MOD-004 mock-boundary correctness, route wiring, list→detail navigation | PASS (no blockers; findings are polish/robustness). Verified full Figma fidelity: page header (avatar, name, Active pill, ID/Plan/Region/Created/Admin metadata, Edit/Manage Subscription/More Actions), all 6 KPIs, Organization Overview, Organization Info, Subscription & Licensing with both meter bars (`role="progressbar"`+aria), Pipeline Activity stats+table, Platform Health, Users table, Quick Actions, Connectors summary+table, Security Status, and the 7-item Recent Audit Activity timeline. No hard-coded hex/px (only `text-white` on primary buttons, matching SCR-024 convention). Mock boundary real-fetch-first → `res.ok`+content-type JSON guard → `mocked:true` fallback with `encodeURIComponent(orgId)`, faithful to `organizationList.api.js`. Sample-data badge + disabled backend buttons with PLANNED-module titles present. `/organizations/:id` wired; list "View" navigates with encoded id. Single `h1` + `h2` cards, no skips. MEDIUM #1: catch-all fallback makes `isError` unreachable (dead error/Retry branch) — same silent-fallback shape as SCR-024, left as pattern-of-record. MEDIUM #2: disabled-action rationale conveyed only via `title` on unfocusable `disabled` controls — same cross-screen a11y gap tracked since SCR-006. LOW: `text-token-xs` (9px) on meaningful labels (inherited SCR-024 convention, best fixed project-wide); hard-coded tones not data-driven (Current Status pill, Subscription meter/billing tones). | Fixed the Current Status pill to be data-driven off `overview.currentStatus` via `STATUS_TONE` (was always green — would misrepresent a Suspended/Expired org). Left the two mediums and the remaining lows as documented, non-blocking carryovers per agent-rules.md §9 (dead-error-branch + disabled+title are pattern-of-record, 9px is a project-wide token pass; Subscription tones are cosmetic on the fixed Active snapshot). ID reconciliation (`org_8kx2m9q` Figma vs `org_8kx2n9q` list mock) documented in `organizationDetails.api.js` — detail echoes the route id so list→detail stays coherent. | PASS after fix — rerun `npm run lint` and `npm run build`: both clean. |
| 2026-08-24 | SCR-025 Organization Details Screen — layout follow-up (user report: right sidebar cut off / global horizontal scroll) | n/a | The two-column grid used `xl:grid-cols-[1fr_360px]`; the `1fr` track floored at its content's min-content width and the tables' intrinsic width pushed the grid wider than the AppShell content area, producing an app-wide horizontal scroll and clipping the right-hand sidebar column. | Changed the grid to `xl:grid-cols-[minmax(0,1fr)_360px]` and added `min-w-0` to both column wrappers so the tracks can shrink to fit the shell; the tables keep their own local `overflow-x-auto`. | PASS — rerun `npm run build`: clean. |
| 2026-08-24 | SCR-026 Create Organization Screen | tester | graphify orientation, lint (`oxlint` full + scoped to the 4 SCR-026 files), build (`vite build`), import-resolution + router-wiring verification, availability check for type-check/test tooling | PASS. `npm run lint` clean (exit 0) — zero findings on any of the four changed files (`CreateOrganizationScreen.jsx`, `createOrganization.api.js`, `router.jsx`, `OrganizationListScreen.jsx`); the only two repo warnings are the pre-existing `SplashScreen.jsx`/`AppShell.jsx` ones, unrelated. `npx vite build` clean (exit 0, 279 modules) — validates all new imports resolve, incl. `createOrganization.api.js → services/api/client` (`apiFetch`/`readJson`) and the `CreateOrganizationScreen` router wiring; the 500 kB chunk-size notice is the pre-existing whole-app advisory. Type-check N/A (no `tsconfig`, no `typecheck` script, TS only transitive — JS/JSX project). No test runner (no vitest/jest/@testing-library, no `*.test.*`/`*.spec.*`), so no unit layer run per the prefer-existing-tooling rule; browser-runtime unverified (pre-existing gap). | none needed | PASS — `npm run lint` + `npx vite build` both clean (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |
| 2026-08-24 | SCR-027 Edit Organization Screen | tester | graphify orientation, lint (`oxlint` scoped to the SCR-027 files + full `npm run lint`), build (`vite build`), route-ordering + dirty-tracking + mock-boundary static trace, type-check/test tooling availability | PASS. Scoped `oxlint` of the SCR-027 files (`EditOrganizationScreen.jsx`, `editOrganization.api.js`, `useEditableOrganization.js`, `router.jsx`, `OrganizationDetailsScreen.jsx`) clean, exit 0; full `npm run lint` shows only the two pre-existing `SplashScreen.jsx`/`AppShell.jsx` warnings, unrelated. `npx vite build` clean, exit 0, 282 modules transformed (all SCR-027 imports resolve; the >500 kB chunk note is the pre-existing whole-app advisory). Statically verified: route ordering (`/organizations/:id/edit` before `/organizations/:id`), `EditForm` keyed on `data.id` (fresh baseline per org), dirty diff normalizes trimmed strings + booleans with `code` excluded, Save Changes `disabled={!isValid || !dirty}` in both Header + ActionBar, Reset restores baseline, Cancel confirm gated on `dirty`, payload = changed keys only via `Object.fromEntries(changes)`, mock fallback returns `mocked:true` → "Sample data" badge + simulated-save `role="alert"` + no navigation (navigation only on real success). No test runner / tsconfig / browser-automation in repo (pre-existing gap) — no unit/type/e2e layer applies. | none needed | PASS — `oxlint` + `vite build` both clean (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |
| 2026-08-24 | SCR-027 Edit Organization Screen | ui-reviewer | graphify orientation, structure/token/state/accessibility review of node `93:1566` (Figma content transcribed during the build via a live `get_design_context`+`get_screenshot` pass with all 242 text nodes extracted to `.tmp/scr027-texts.txt`, so content fidelity is `verified`; reviewer's own session had no Figma MCP, cross-checked code↔transcription + SCR-024/025/026 + `CreateOrganizationScreen`/`UserProfileMenu` conventions), MOD-004 mock-boundary correctness, live dirty-tracking design-intent, route wiring, list/detail→edit navigation | FAIL (pre-fix) — 0 blocker + 2 high + 2 medium + 4 low; gates (build/lint) PASS. HIGH #1: the "Advanced Configuration" collapsible section (Figma: heading + "Custom rate limits, webhook endpoints, feature fla…" + "Expand") was absent from the implementation. HIGH #2: the Organization Information metadata block (Organization ID `ORG-00142`, Created `Jan 14, 2023`, Last Updated `Jul 28, 2026 · 14:32 UTC`) — present in Figma and supplied by the baseline (`createdLabel`/`lastUpdatedLabel`) — was never rendered. MEDIUM #3: the License Usage caption denominator was the hardcoded string `247 seats / 300 seats` while the percentage recomputed live, so editing the License Limit to 500 produced the contradictory `247 seats / 300 seats (49%)` instead of Figma's `247 seats / 500 seats (49%)`. MEDIUM #4: the unsaved-changes navigation guard covered only Cancel (`window.confirm` when dirty) — the "← Back to organization" `Link` and "View Full Audit Log →" `Link` navigated away silently discarding edits, and there was no `beforeunload` guard. LOW #5: Change Summary SSO after-value showed "Enabled" vs Figma's "Enabled (Okta SAML)". LOW #6: `text-warning` on `warning-bg` ≈3:1 (below WCAG AA) on Modified badges / warning notes / unsaved pill — shared-token issue also on sibling screens. LOW #7: header unsaved pill CSS-uppercased "3 UNSAVED CHANGES" vs Figma title-case. LOW #8: Role field marked required but not in `validate()`/checklist (harmless — always has a baseline value). Verified passing: live diffing (Modified badges / Change Summary / unsaved counter / Validation Status all derived from `baseline.form` comparison, not seeded to the design's mid-edit state — matches documented intent); convention reuse (`ORG_FORM_OPTIONS`/`PLAN_CATALOG`/`SUBSCRIPTION_PLANS` re-export, AppShell breadcrumb); design tokens (no raw hex/inline color); route ordering + details "Edit Organization" button navigation; mock boundary (Sample-data badge on `mocked`, MOD-004 simulated-save alert, disabled backend actions with MOD-004/005/002 titles); accessibility (`htmlFor`, `aria-invalid`/`aria-required`/`aria-describedby`, `role="alert"` errors, `role="status"` live regions, `role="progressbar"` meters, `role="switch"` toggles, `fieldset`/`legend` for API Access); focus-trapped dialog (Escape + focus restore); loading/error/empty states + focus-to-first-error. | Fixed HIGH #1 (added the `AdvancedConfiguration` collapsible with `aria-expanded`/`aria-controls`, Expand/Collapse chevron, and a MOD-004-PLANNED disclosure body). HIGH #2 (rendered the ID/Created/Last Updated metadata block from the baseline at the top of Organization Information). MEDIUM #3 (drove the License Usage denominator + percentage off `form.licenseLimit` via a new `licenseUsedSeats: 247` baseline field, so the caption now reads `247 seats / N seats (P%)` live). MEDIUM #4 (added a `beforeunload` guard when dirty and a shared `guardedNavigate` used by Cancel, ← Back, and View Full Audit Log — all now confirm before discarding). LOW #5 (`displayValue('enableSso', true)` → "Enabled (Okta SAML)"). Deferred: LOW #6 amber contrast is a shared cross-screen token issue best fixed in a project-wide token pass (agent-rules.md §9); LOW #7 uppercase pill is cosmetic and matches the sibling badge styling; LOW #8 the required Role marker is harmless (always populated) and enforcing it would diverge from the Create screen's identical treatment. | PASS after fixes — rerun `npx oxlint` (scoped, exit 0) + `npx vite build` (exit 0, 282 modules): both clean; `graphify update .` run. |
| 2026-08-24 | SCR-028 Organization Setting Screen | tester | discovered tooling from `package.json` (dev/build/lint/preview only), lint (`oxlint` scoped to the 3 SCR-028 files + feature dir + full `npm run lint`), build (`vite build`), runtime smoke (`vite preview` + curl on `/` and `/organizations/ORG-00142/settings`), test/typecheck-tooling availability | PASS. Scoped `oxlint` of `OrganizationSettingsScreen.jsx`/`organizationSettings.api.js`/`useOrganizationSettings.js` clean (exit 0, zero warnings); feature-dir lint surfaced only the pre-existing SCR-027 `EditOrganizationScreen.jsx:329` `jsx-no-undef` (`AdvancedConfiguration`) warning, unrelated to SCR-028; full `npm run lint` shows only the three pre-existing repo warnings (`AppShell.jsx`, `EditOrganizationScreen.jsx`, `SplashScreen.jsx`). `npm run build` clean (285 modules, only the whole-app chunk-size advisory). Runtime smoke: `vite preview` served `GET /` → 200 and `GET /organizations/ORG-00142/settings` → 200 with a clean server log (client-rendered SPA catch-all, so this confirms the built app serves + the route is wired at `router.jsx:94`, not component interaction). No test runner / tsconfig / browser-automation in repo (pre-existing gap, siblings SCR-024–027 have none either) — no unit/type/e2e layer applies. | none needed (flagged the unrelated SCR-027 `jsx-no-undef` to that screen's owner) | PASS — `oxlint` + `vite build` both clean, route serves 200 (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |
| 2026-08-24 | SCR-029 Department Management Screen | tester | lint (`oxlint` scoped to the 4 SCR-029 files + full `npm run lint`), build (`vite build`), route-ordering verification | PASS. `oxlint` clean on `DepartmentManagementScreen.jsx`/`useDepartmentManagement.js`/`departmentManagement.api.js`/`router.jsx` — zero findings; full `npm run lint` shows only the two pre-existing `AppShell.jsx`/`SplashScreen.jsx` warnings. `npm run build` clean (288 modules, only the whole-app chunk-size advisory). Route ordering verified: `/organizations/:id/departments` (`router.jsx:96`) precedes the `/organizations/:id` catch-all (`:97`), so the departments path resolves to this screen. No test runner / tsconfig / browser-automation in repo (pre-existing gap, siblings SCR-024–028 have none either). | none needed | PASS — `oxlint` + `vite build` both clean; route ordering correct (browser-runtime interaction layer explicitly unverified, flagged rather than asserted — same standard as prior tester entries). |
| 2026-08-24 | SCR-029 Department Management Screen | ui-reviewer | structure/token/state/accessibility review of node `94:5039` + drawer `94:6298` (Figma content transcribed during the build into `departmentManagement.api.js`'s header via a get_metadata+get_screenshot pass, so content fidelity is `verified`; reviewer's own session had no callable Figma MCP, cross-checked code↔transcription + SCR-024 `OrganizationListScreen`/`organizationList.api.js` pattern-of-record), MOD-004 mock-boundary correctness, route wiring | FAIL (pre-fix) — 0 blocker + 1 high + 2 medium + 4 low; gates (build/lint) PASS. HIGH #1: the "Last Activity" sort option was wired to `b.attention - a.attention` — it ordered by the `attention` boolean instead of by activity recency, silently mis-sorting a shipped control. Verified passing: real-fetch-first + `res.ok`+content-type JSON guard + `mocked:true` fallback with `encodeURIComponent(orgId)`, byte-faithful to `organizationList.api.js`; all design-token classes resolve (no raw hex/off-scale spacing); route ordering correct; full state coverage (loading skeleton `aria-hidden`, error `role="alert"`+Retry, filter-aware empty state, `sr-only role="status" aria-live` announcer); drawer focus management (focus close button on open, Escape, Tab/Shift-Tab wrap trap, focus restore to trigger, `role="dialog" aria-modal aria-labelledby`); responsive KPI grid `2→sm:3→xl:6`, table `overflow-x-auto`, drawer `w-full max-w-md`; page-scoped select-all with correct `indeterminate`; disabled destructive bulk actions; footer + KPI figures match spec. MEDIUM #2: disabled bulk/toolbar/drawer action rationale conveyed only via `title` on unfocusable `disabled` controls — cross-screen a11y gap shared since SCR-006. MEDIUM #3: row `onClick` open is mouse-affordance (`cursor-pointer`) with keyboard parity via the focusable per-row "View" button (acceptable, redundant path exists). LOW #4: `purple` badge tone maps to warning/amber tokens (no purple token exists — verbatim from SCR-024 `ORG_TONE`, deliberate palette approximation). LOW #5: faint-on-muted contrast pairings (`gray` badge, Archived pill, mono meta) may fall below AA 4.5:1 — shared token issue, unmeasured. LOW #6: sort change preserves selection while search/status/BU clears it (arguably more correct; noted for deliberateness). LOW #7: `refetchInterval: 30_000` polls even in permanent mock-fallback (harmless, matches SCR-024 cadence). | Fixed HIGH #1 — added a sortable `activityMinutes` field to every mock row (`Never` → `Number.MAX_SAFE_INTEGER`) and changed the sort to `(a.activityMinutes ?? Infinity) - (b.activityMinutes ?? Infinity)` (ascending = most recent first). Deferred (documented, non-blocking): MEDIUM #2 disabled+title is cross-screen pattern-of-record for a project-wide a11y pass (agent-rules.md §9); MEDIUM #3 keyboard path exists via the "View" button; LOW #4 purple→amber is a deliberate no-purple-token approximation shared with SCR-024; LOW #5 amber/faint contrast is the shared cross-screen token issue for a project-wide pass; LOW #6 selection-on-sort is acceptable; LOW #7 mock-poll cadence mirrors SCR-024 by design. | PASS after fix — rerun `npm run lint` (clean) + `npm run build` (exit 0, built in 26.8s): both clean; `graphify update .` run. |
| 2026-08-24 | SCR-028 Organization Setting Screen | ui-reviewer | graphify orientation, structure/token/state/accessibility review of node `93:3192` (Figma content transcribed during the build into `organizationSettings.api.js`'s header, so content fidelity is `verified`; reviewer's own session had no callable Figma MCP, cross-checked code↔transcription + SCR-024/025/026/027 pattern-of-record), MOD-004 mock-boundary correctness, dirty-tracking design-intent, route wiring | PASS (no blockers; 2 medium + 4 low + 3 optional). Verified passing: real-fetch-first + content-type guard + `mocked:true` fallback faithful to the sibling MOD-004 write modules; mock disclosed via header "Sample data" badge (title-attributed) + post-save "Simulated save (no backend)" banner; all backend-dependent actions (logo upload, key rotate/revoke/generate) disabled with explanatory `title` citing MOD-002/004/005; live dirty tracking (Modified badges, unsaved pill, Change Summary diff) computed against the saved baseline, not seeded to the design's mid-edit state (matches documented intent); focus-trapped Save `ConfirmDialog` (`role="dialog"`, `aria-modal`, labelled title, Escape, Tab trap, focus restore); toggles `role="switch"`/`aria-checked`; fields wire `aria-describedby`/`aria-invalid`/`aria-required`; progressbar value/min/max; `focus-visible` throughout; every color/spacing/font class maps to a defined token (only intended `#0F5699` picker fallback + toggle-knob `bg-white`/`ring-black/5`, matching siblings). MEDIUM #1: router navigation escape hatches (AppShell sidebar links, breadcrumb crumbs, browser Back) bypass the `beforeunload`+`guardedNavigate` unsaved-changes guard — cross-cutting gap shared with SCR-027, best closed by a shared `useUnsavedChangesBlocker`. MEDIUM #2: `text-warning` on `bg-warning-bg` ≈3.1:1 on the Modified badge / unsaved pill / simulated-save heading — below AA 4.5:1, same shared-token issue as SCR-021/027. LOW #3: `updateOrganizationSettings` funnels real 5xx/malformed responses into the `mocked:true` "no backend" branch (accurate while MOD-004 is PLANNED; needs a distinct real-failure path before the backend lands). LOW #4: `handleConfirmSave` optimistically clears dirty state regardless of result (intentional while there is no live endpoint; needs a failure branch once #3 is addressed). LOW #5: `primaryBrandColor` free-text accepted any string with no validation and still counted toward the Branding checklist. LOW #6: Validation Status reads 6/6 on load vs. the design's 4/6 — a direct, correct consequence of not seeding the form mid-edit (a valid saved config legitimately reads all-complete). | Fixed LOW #5 — added a `#RRGGBB` `HEX_COLOR` format check to `validate()` feeding the existing inline-error mechanism (wired `error`/`onBlur`/`aria-invalid`/`aria-describedby` onto the brand-color text input) and gated the Branding checklist item on `!errors.primaryBrandColor` so an invalid hex no longer counts as complete. Deferred (documented, non-blocking): MEDIUM #1 router-guard gap is cross-cutting with SCR-027 — best fixed once via a shared blocker hook across both screens, not one alone (consistency, agent-rules.md §9); MEDIUM #2 amber contrast is a shared cross-screen token issue for a project-wide token pass (same deferral as SCR-021/027); LOW #3/#4 real-vs-absent error path is acceptable while MOD-004 is PLANNED and mirrors the sibling write modules (revisit when the backend lands); LOW #6 6/6-on-valid-config is the correct outcome of the documented no-mid-edit-seed decision. | PASS after fix — rerun `npx oxlint` (scoped, exit 0) + `npm run build` (exit 0, 285 modules): both clean; `graphify update .` run. |


- SCR-001: brand heading size (19px) vs. Figma-observed ~16px is unverified pending a Figma MCP re-check of node `13:3982`; tracked, not blocking.
- SCR-002: "Continue with SSO" is implemented as a disabled control since no SSO provider/backend exists in any module spec yet; revisit once MOD-002 defines an SSO integration.
- SCR-002: the two duplicate top-level `App` instances under Figma node `14:4055` (`14:...` and `16:4949...`) were treated as a redundant Figma layer rather than a second screen state — flag for the design owner to confirm/clean up, not blocking.
- SCR-003/SCR-004: confirmed Figma content/naming swap between these two nodes (see screen-inventory.md SCR-003/SCR-004 build notes) — resolved; SCR-004 was built using node `16:4495`'s token-based reset-password content, not a duplicate forgot-password form.
- SCR-003: no in-place "try another email" / resend affordance from the "Check your email" success state — only path back is "Back to Sign In", which leaves the screen entirely. Not blocking; flagged for a future pass if product wants tighter recovery UX.
- SCR-003: browser-runtime verification (actual DOM/console-error check in a real browser) is blocked pending a Playwright/Puppeteer/Cypress dependency in `frontend/` — lint/build/routing were verified, in-browser behavior was not.
- SCR-004: mock-mode token validation is a length heuristic (≥16 chars = "valid"), not real validation — must be replaced once MOD-002's backend implements real token verification.
- SCR-004: browser-runtime verification is blocked for the same reason as SCR-003 (no browser-automation dependency installed).
- SCR-005: mock-mode token validation reuses SCR-004's length heuristic (≥16 chars = "valid"), not real validation — must be replaced once MOD-002's backend implements real token verification.
- SCR-005: the `?email=` query-param contract for delivering the masked address to this screen is an extrapolation (no Figma variant or module spec defines how the screen learns the address) — confirm the real contract once the inviting flow (post-login unverified-account gate) is designed.
- SCR-005: browser-runtime verification is blocked for the same reason as SCR-003/SCR-004 (no browser-automation dependency installed).
- SCR-006: "Use another method" (and LoginScreen's "Continue with SSO") use `disabled`+`title` to explain an unavailable control, but `disabled` elements are unfocusable and `title` is mouse-hover-only — keyboard/screen-reader users get no explanation at all. Flagged as a shared, cross-screen accessibility gap; revisit both screens together if/when an `aria-disabled` + visible-text-reason pattern is adopted project-wide.
- SCR-006: mock-mode code validation accepts any well-formed 6-digit numeric string (no real backend to check against) — must be replaced once MOD-002's backend implements real TOTP verification.
- SCR-006: the challenge/session context this screen submits against is assumed to live server-side (a short-lived post-login challenge token) — no Figma variant or module contract confirms this; verify the real contract once MOD-002's 2FA endpoints are designed.
- SCR-006: browser-runtime verification is blocked for the same reason as SCR-003/SCR-004/SCR-005 (no browser-automation dependency installed).
- SCR-008: browser-runtime interaction verification (confirm/cancel click behavior, focus management) is blocked for the same reason as prior screens (no browser-automation dependency installed) — static wiring and an HTTP-level route smoke check were substituted.
- SCR-008: the background-shell bleed-through in Figma node `20:5458` (the `App` instance rendering the Change Password screen behind the dialog) was confirmed via `get_design_context` to be incidental Figma-canvas composition, not a second real state — not reproduced, per the same MOD-001-not-yet-built precedent as SCR-007.
- SCR-009: built against a temporary `AppShellScaffold.jsx` scaffold since MOD-001's real Sidebar/Header (SCR-010/SCR-011) don't exist yet — MOD-001 cannot reach full `DONE` until those replace it; same open pattern as SCR-007/SCR-008's `SettingsShellScaffold.jsx`.
- SCR-009: 100% of dashboard traffic is currently mocked (no MOD-008/MOD-009 backend deployed) — a "Sample data" badge discloses this in the UI; must be removed/conditional once a real backend exists.
- SCR-009: the 7-day execution trend chart region is a labeled placeholder, not a real chart — no charting library or time-series API contract exists yet; revisit once MOD-009 defines the analytics endpoint shape.
- MOD-001 AppShell: `role`/`currentUser` in `sessionSlice.js` are hardcoded (default role `data_etl_engineer`), switchable only via the `roleChanged` Redux action — no MOD-002/MOD-003 backend exists yet to source a real authenticated session's role. Must be replaced once those modules ship.
- MOD-001 AppShell: `SettingsShellScaffold.jsx` (SCR-007) was NOT migrated to the real `AppShell` in this pass — only `DashboardScreen.jsx` (SCR-009) was, since it was a direct scaffold swap. `SettingsShellScaffold.jsx` has custom settings-specific side-nav/breadcrumb logic beyond a simple swap; migrating it is scoped as separate follow-up work, not done here to respect agent-rules.md §9 (no unrelated scope expansion).
- MOD-001 AppShell: Header/Footer's org-switcher label, notification unread-dot, and Footer's live stats (active pipelines, workers, queue depth, throughput) are static Figma-literal values — no MOD-004 (Organization Management)/MOD-008/MOD-009/MOD-010 backend exists yet to source them live.
- MOD-001 AppShell: the sidebar's "Errors" badge count (3) is a static Figma-literal value — no MOD-008/MOD-009 backend exists yet to source a real count.
- MOD-001 AppShell redesign: `shadow-sm` (used on 4 small avatar/logo chips in Header.jsx/Sidebar.jsx) is Tailwind's default box-shadow utility rather than a project design token — `tailwind.config.js` has no `boxShadow` extension since the design system never defined a shadow token. Not fixed (usage is sparse and doesn't read as visual noise); revisit if a shadow token gets formalized project-wide.
- MOD-001 AppShell redesign: the new `SidebarToggleIcon.jsx` collapse/expand icon is a hand-authored design (double-chevron, direction-flipping), not sourced from any Figma frame — there is no Figma source of truth for this icon to stay in sync with going forward.
- SCR-012: notification data/mark-all-read are 100% mocked (MOD-010 Notifications is `PLANNED`, no backend deployed) — `getNotifications`/`markAllNotificationsRead` attempt a real request first per the project's mock-fallback convention; a "Sample data" badge discloses this in the panel. Must be replaced once MOD-010 ships.
- SCR-012: the settings gear and "View all notifications" link are disabled with an explanatory `title` since no notification-settings screen or full-history route exists anywhere in the 71-screen Figma inventory beyond this panel — same disabled+title precedent as LoginScreen's SSO button, and shares that pattern's known limitation (unfocusable + mouse-hover-only explanation, not fixed here, tracked at SCR-006's entry above).
- SCR-014: no charting library is a project dependency, and Figma's own chart regions (SLA performance, execution volume, platform adoption) are hand-drawn static vector art with no live-render contract — this screen renders the real mock/API data shape as lightweight inline-SVG line/bar primitives instead. Revisit if/when a charting library (recharts/visx/etc.) becomes an explicit project requirement.
- SCR-014: Figma's "Adoption by Business Unit" table (node `45:1661`) has 4 duplicate placeholder rows in the source file (all "Sales & Revenue Ops" / "68 users", only pipeline count/% differ) — a Figma data-entry bug, not a real second state. The mock data uses 8 distinct business units instead, consistent with the screen's own "Across 8 business units" KPI figure. Flag for the design file owner to fix the source; not reproduced here.
- SCR-014: **found and fixed** — `dashboard.api.js`'s (SCR-009) identical `catch (err) { if (err instanceof X) throw err; ... }` mock-fallback pattern has the same "200 OK but non-JSON body" bug just fixed in `executiveDashboard.api.js` (see the SCR-014 tester entry above): with no dev API proxy configured, `/dashboard` also gets stuck on its error state instead of showing mock data in this dev environment. Deliberately left unfixed in `dashboard.api.js` itself per agent-rules.md §9 (scope control — this unit is SCR-014, not a SCR-009 revision); recorded here as a real, reproducible defect for a future pass to fix across both files (and any other feature `.api.js` sharing the same fallback shape).
- SCR-014: 100% of executive-dashboard data is currently mocked (no MOD-009 backend deployed) — a "Sample data" badge discloses this in the UI, same convention as SCR-009/SCR-012; must be removed/conditional once MOD-009 ships. The date-range toggle (7d/30d/90d/1yr) is real interactive state wired into the React Query key, but the mock API has only one 30-day dataset and ignores the range param — documented as a known mock limitation in `executiveDashboard.api.js`.
- SCR-012: the item-row unread indicator dot's read-state visual treatment (filled vs. fully transparent) was not independently re-verified against a fresh Figma pixel diff this session — flagged by the ui-reviewer as a `not-verified` fidelity note, not a confirmed defect.
- SCR-013: "Account Settings", "Preferences", and "Security" all route to `/account/change-password` — the only real account-settings screen built so far. No separate settings/security/preferences screen exists anywhere in the 71-screen Figma inventory; revisit once a dedicated settings/security screen is planned.
- SCR-013: "Switch Organization"/"Manage Organization"/"Billing" are disabled with an explanatory `title` (no MOD-004 backend); "Documentation"/"Help Center"/"Contact Support"/"Release Notes" are disabled the same way (no real destination exists anywhere in the project) — same disabled+title precedent and known limitation as SCR-006/SCR-012's entries above.
- SCR-013: the "Alt+S" keyboard-shortcut hint next to "Security" is decorative/static only (Figma shows the hint, but no keyboard-shortcut system exists in this codebase to wire it to) — not implemented as a real key handler.
- SCR-013: "Sign Out" navigates to `/logout` (SCR-008's real confirm screen) rather than calling `useLogout().confirm()` directly from the dropdown, so the existing confirm-before-logout UX step is always shown.
- SCR-015/SCR-016: the owner-initials avatar in `PipelineOverviewScreen.jsx`'s `AllPipelinesTable` and `DataQualityScreen.jsx`'s `FailedValidationsTable` both use `h-5.5 w-5.5`, which is not a valid Tailwind utility (no `5.5` step in the default or project spacing scale) — the class has no effect, so the avatar circle is unsized beyond its padding/flex content. Found first at SCR-016's review (inherited from SCR-015, not new); not fixed in either screen individually per agent-rules.md §9 (fixing one alone would create inconsistency) — fix both together in a future pass, either with `h-[22px] w-[22px]` or by adding a `5.5` spacing step to `tailwind.config.js`.
- SCR-016: 100% of data-quality dashboard data is currently mocked (no MOD-009 backend deployed) — a "Sample data" badge discloses this in the UI, same convention as SCR-014/SCR-015. The date-range toggle (Today/7d/30d) is real interactive state wired into the React Query key, but the mock API has only one fixed snapshot and ignores the range param — documented as a known mock limitation in `dataQuality.api.js`.
- SCR-016: Create Rule/Run Validation/Export buttons are disabled with an explanatory `title` since MOD-009 has no data-quality mutation endpoints yet — same disabled+title precedent and known accessibility limitation as SCR-006/SCR-012/SCR-013's entries above.
- SCR-016: no charting library is a project dependency, and Figma's own chart regions (quality score trend, validation results) are hand-drawn static vector art with no live-render contract — this screen renders the real mock data shape as lightweight inline-SVG line/bar primitives instead, same precedent as SCR-014/SCR-015. Revisit if/when a charting library becomes an explicit project requirement.
- SCR-017: `TREND_TONE`'s keys (`up`/`down`/`flat`) encode arrow-glyph direction rather than sentiment — the Storage Used KPI pairs an `↑` arrow with `trendTone: 'down'` (semantically correct, rising storage usage is bad news, renders red) but the key name is misleading for future editors. Recommend renaming to `good`/`bad`/`flat` in a future pass; not fixed here since it's a naming clarity issue, not a visible bug.
- SCR-017: no charting library is a project dependency, and Figma's own CPU/memory trend chart regions are hand-drawn static vector art with no live-render contract — this screen renders the real mock data shape as lightweight inline-SVG line primitives instead, same precedent as SCR-014/015/016. Revisit if/when a charting library becomes an explicit project requirement.
- SCR-019: Figma fidelity to node `65:17501` is `not-verified` — no Figma MCP tool was available in the build session, so the screen was built against the screen-inventory.md description + the SCR-018 sibling pattern, not a pixel inspection (agent-rules.md §2). A future Figma-enabled pass should re-diff node `65:17501` and reconcile any layout/section differences. Same class of gap as SCR-005–009's "Figma MCP call did not resolve" notes.
- SCR-019: icons reuse existing glyph-matched assets from sibling screens (no Figma MCP to export node `65:17501`'s own icons this session); `KPI_ICON.target` maps to `executive-dashboard/icon-database.svg` as the closest available destination/warehouse glyph. Revisit for node-accurate icons once Figma MCP is available.
- SCR-019: the destination connector taxonomy is unenumerated in the current design/spec (module-plan.md MOD-007 "Global Build Risks") — the mock's category set (Warehouse/Database/Storage/Streaming/BI/Webhook) is sample data, not a confirmed list. The All Destinations `categoryFilters` expose Warehouse/Database/Storage/Streaming chips only, so the sample `BI`/`Webhook` rows aren't reachable via category filtering. Must be reconciled once MOD-007's connector taxonomy lands.
- SCR-019: 100% of destination-health data is currently mocked (no MOD-007/MOD-009 backend deployed) — a "Sample data" badge discloses this in the UI, same convention as SCR-014–018. The Live/1h/24h/7d range toggle is real interactive state wired into the React Query key, but the mock API has only one fixed snapshot and ignores the range param — documented as a known mock limitation in `destinationHealth.api.js`. Add Destination/Test All/Export and both bulk-action buttons are disabled with explanatory `title` (no MOD-007 backend) — same disabled+title precedent and known accessibility limitation as SCR-006/SCR-012/SCR-013's entries above.
- SCR-018/SCR-019: `AllSourcesTable`/`AllDestinationsTable`'s `updateSearch` does not clear row selection (only category-filter change does), so narrowing the search after selecting rows can leave the bulk-action bar counting now-hidden rows. Identical behavior in both sibling screens — fix both together in a future pass if tightened selection UX is wanted, not one alone (consistency).
- SCR-019: no charting library is a project dependency, and the delivery-success/sync-performance chart regions render the real mock series as lightweight inline-SVG line primitives, same precedent as SCR-014–018. Revisit if/when a charting library becomes an explicit project requirement.
- SCR-020: Figma content fidelity to node `84:5098` is `verified` — a live `get_metadata`+`get_screenshot` pass ran during the build, so every KPI value, chart series, ranked list, schedule stat, insight, and the 10 execution rows are transcribed from the actual frame's text nodes (unlike SCR-018/019, whose build sessions had no Figma MCP).
- SCR-020: the Figma source frame itself quotes two conflicting success rates — `98.3%` in the headline chip vs. `98.9%` in the summary strip / KPI card. Reconciled to `98.9%` in the mock (38,037 successful / 38,452 total = 98.92%, the arithmetically correct figure); the `98.3%` is a Figma typo, documented as a FIGMA-SOURCE CONFLICT block in `executionStatistics.api.js` per agent-rules.md §3 rather than silently reproduced.
- SCR-020: the `purple` outcome-breakdown and insight tone keys render blue via the shell-accent token — no `purple` color token exists anywhere in `tokens.css`/`tailwind.config.js`. Left as-is (a screen-scoped fix would hardcode a raw hex or invent a one-off token); revisit if a purple token is formalized project-wide, same class of deferral as SCR-001's shadow-token note.
- SCR-020: `volumeTrend` carries 14 daily data points against 7 axis-tick labels — the extra points render as a denser polyline than the tick grid implies (cosmetic; both series plot on the real shared 0–axisMax scale after the Medium #1 fix). A real MOD-009 endpoint would return matched point/label counts per range.
- SCR-020: 100% of execution-statistics data is currently mocked — MOD-006/MOD-008 (Executions telemetry) and MOD-009 (Analytics) are all `PLANNED` with no backend deployed. A "Sample data" badge discloses this in the header + footer, same convention as SCR-014–019. The 7d/30d/90d/1y range toggle is real interactive state wired into the React Query key, but the mock has only one fixed snapshot and ignores the range param — documented as a known mock limitation in `executionStatistics.api.js`. Generate Report/Export are disabled with an explanatory `title` naming the blocking module — same disabled+title precedent and known accessibility limitation as SCR-006/SCR-012/SCR-013's entries above.
- SCR-021: Figma content fidelity to node `69:37077` is `verified` — a live `get_metadata`+`get_screenshot` pass ran during the build, so all 12 KPI values, both trend series, the severity/root-cause/category breakdowns, reliability stats, most-affected/active-critical lists, and the 8 error rows are transcribed from the actual frame's text nodes. Data integrity confirmed: severity, root-cause, and category counts each sum to exactly 248 (= Total Errors = the "of 248" table total), and Critical=12/High=19 reconcile with the headline banner.
- SCR-021: 100% of error-analytics data is currently mocked — MOD-008 (Operations/Incidents) and MOD-009 (Analytics) are `PLANNED` with no backend deployed. A "Sample data" badge discloses this in the header, same convention as SCR-014–020. The 7d/30d/90d/1y range toggle is real interactive state wired into the React Query key, but the mock has only one fixed snapshot and ignores the range param — documented as a known mock limitation in `errorAnalytics.api.js`, same as the SCR-020 sibling.
- SCR-021: the red "Create Incident" primary button is permanently disabled with an explanatory `title` naming the blocking module (MOD-008 Incidents has no mutation endpoints yet) — same disabled+title precedent and known accessibility limitation (unfocusable + mouse-hover-only explanation) as SCR-006/SCR-012/SCR-013/SCR-020's entries above.
- SCR-021: `text-token-xs` (~9px) is used on real table metadata (error IDs, timestamps) and `text-warning` small text appears on Open/Mitigating status badges + footer below the AA 4.5:1 contrast ratio — both are inherited by design from the approved SCR-020 pattern-of-record for MOD-009 dashboards. Best addressed as a project-wide token/contrast pass rather than a screen-scoped patch that would break sibling parity; not fixed here per agent-rules.md §9.
- SCR-021: `BAR_TONE`'s `warning-strong` key maps to `danger-strong` because no distinct strong-warning color token exists in `tokens.css`/`tailwind.config.js` — same class of missing-token deferral as SCR-020's `purple` note. Revisit if a strong-warning token is formalized project-wide.
- SCR-021: browser-runtime interaction verification (search/filter/pagination clicks, live-region announcements, focus management in a real DOM) is unverified — no browser-automation dependency (Playwright/Puppeteer/Cypress) is installed in `frontend/`; lint/build/data-contract/icon-resolution/routing were verified, in-browser behavior was flagged rather than asserted. Same blocked gap as prior screens.
- SCR-022: Figma content fidelity to node `70:43606` is `verified` — a live `get_metadata`+`get_screenshot` pass ran during the build, so every KPI value, chart series, resource-utilization row, runtime/throughput stat, ranked pipeline, regression, worker row, capacity forecast, recommendation, and the 8 component rows are transcribed from the actual frame's text nodes (the frame's Figma name carries a typo, "Performance Analytics Dashboard Screeb").
- SCR-022: **found and fixed** — `ComponentsTable`'s Environment filter (`row.environment !== envFilter`) matched against a field that didn't exist on any of the 8 mocked `components.rows`, so selecting Production/Staging/Development emptied the table (the "All" default masked the latent bug). Fixed by adding an `environment` value to all 8 rows in `performanceAnalytics.api.js`; `npm run lint` re-run clean.
- SCR-022: 100% of performance-analytics data is currently mocked — MOD-008 (Operations — worker/queue/runtime telemetry) and MOD-009 (Analytics) are `PLANNED` with no backend deployed. A "Sample data" badge discloses this in the UI, same convention as SCR-014–021. The 7d/30d/90d/1y range toggle is real interactive state wired into the React Query key, but the mock has only one fixed snapshot and ignores the range param — documented as a known mock limitation in `performanceAnalytics.api.js`, same as the SCR-020/021 siblings.
- SCR-022: `text-token-xs` (~9px) dense metadata (component IDs, per-row stats) and small `text-warning` status badges fall below the AA 4.5:1 contrast ratio — inherited by design from the approved SCR-020/021 pattern-of-record for MOD-009 dashboards. Best addressed as a project-wide token/contrast pass rather than a screen-scoped patch that would break sibling parity; not fixed here per agent-rules.md §9.
- SCR-022: the "Export Analytics" and "Generate Report" primary buttons are permanently disabled with an explanatory `title` naming the blocking module (MOD-008/MOD-009 have no analytics-export/report mutation endpoints yet) — same disabled+title precedent and known accessibility limitation (unfocusable + mouse-hover-only explanation) as SCR-006/SCR-012/SCR-013/SCR-020/SCR-021's entries above.
- SCR-022: `ScoreTrendCard` renders 8 axis-tick labels beneath a 14-point score/SLA series — the extra points render as a denser polyline than the tick grid implies (cosmetic; both series plot on the real shared 0–100 scale). Same class as SCR-020's polyline note. A real MOD-009 endpoint would return matched point/label counts per range.
- SCR-022: no charting library is a project dependency, and Figma's own chart regions (performance-score/SLA trend, hourly throughput) are hand-drawn static vector art with no live-render contract — this screen renders the real mock data shape as lightweight inline-SVG line/bar primitives instead, same precedent as SCR-014–021. Revisit if/when a charting library becomes an explicit project requirement.
- SCR-022: browser-runtime interaction verification (search/Category-SLA-Environment filter clicks, pagination, live-region announcements, focus management in a real DOM) is unverified — no browser-automation dependency installed in `frontend/`; lint/build/data-contract/icon-resolution/routing were verified, in-browser behavior was flagged rather than asserted. Same blocked gap as prior screens.
- SCR-023: Figma content fidelity to node `72:45894` is `verified` — a live `get_metadata`+`get_screenshot` pass ran during the build, so every KPI value, status-banner metric, live-activity row, active-pipeline row, worker/queue stat, infrastructure bar, throughput series, connectivity row, event, alert, and the 10 monitoring-table rows are transcribed from the actual frame's text nodes.
- SCR-023: **found and fixed** — the `connectivity` card's summary counters (`connected:9, reconnecting:1, failed:3`) contradicted its own enumerated remote+local rows (8/1/1). Corrected the counters to `connected:8, reconnecting:1, failed:1` in `realTimeMonitoring.api.js` so the headline matches the rows.
- SCR-023: **found and fixed** — the monitoring-table search placeholder overpromised ("…execution ID, worker…") while the filter only matched `name`/`owner`; narrowed to "Search by component or owner…". Separately, `liveActivity.statusFilters` lacked a `Completed` chip despite `Completed` rows existing; added it so those rows are reachable.
- SCR-023: this is a real-time monitoring screen but there is **no live transport** — MOD-006/007/008/009 are all `PLANNED` with no backend and no Socket.IO stream deployed (a production build would consume a Socket.IO live stream per agent.md §5.2). The "live" feel is simulated client-side by a 15s React Query `refetchInterval` plus a Pause/Live toggle that halts polling; the mock returns the same fixed snapshot every poll, so the "Updated" clock advances but the numbers do not actually stream. A "Sample data" badge discloses the mock, same convention as SCR-014–022; the not-real-streaming limitation is documented in `realTimeMonitoring.api.js`.
- SCR-023: the 30s/7d/30d range toggle is real interactive state wired into the React Query key, but the single fixed mock snapshot ignores the `range` param — same documented mock limitation as the SCR-020/021/022 MOD-009 siblings. A real MOD-009 endpoint would aggregate per range and push deltas over the websocket.
- SCR-023: the "Export Snapshot" primary button is permanently disabled with an explanatory `title` naming the blocking PLANNED module — same disabled+title precedent and known accessibility limitation (unfocusable + mouse-hover-only explanation) as SCR-006/SCR-012/SCR-013/SCR-020/SCR-021/SCR-022's entries above.
- SCR-023: no charting library is a project dependency — the throughput chart is rendered as a lightweight inline-SVG sparkline primitive over the real mock series instead, same precedent as SCR-014–022.
- SCR-023: `text-token-xs` (~9px) dense metadata and small status badges fall below the AA 4.5:1 contrast ratio — inherited by design from the approved SCR-020/021/022 pattern-of-record for MOD-009 dashboards. Best addressed as a project-wide token/contrast pass rather than a screen-scoped patch that would break sibling parity; not fixed here per agent-rules.md §9.
- SCR-023: browser-runtime interaction verification (Pause/Live toggle, range switch, monitoring-table search/type-status-environment filter clicks, pagination, live-region announcements, focus management in a real DOM) is unverified — no browser-automation dependency installed in `frontend/`; lint/build/data-contract/icon-resolution/routing were verified, in-browser behavior was flagged rather than asserted. Same blocked gap as prior screens.
- SCR-024: Figma content fidelity to node `90:8293` is `verified` — a live `get_metadata`+`get_screenshot` pass ran during the build, so every KPI value (Total 1,284 / Active 1,108 / Trial 124 / Suspended 38 / Expiring 14), the 12 organization rows (name, ID, primary admin, plan, status, users, pipelines, last activity, created, region), the toolbar filters, the bulk-action set, and the "Showing 1–12 of 1,284" footer are transcribed from the actual frame's text nodes.
- SCR-024: **found and fixed** — the footer's "(filtered from …)" clause was gated on `filtered.length !== data.total`, which is always true because the mock ships 12 rows against a declared `total:1284`; the default unfiltered view therefore rendered `Showing 1–12 of 12 (filtered from 1,284)` instead of Figma's `Showing 1–12 of 1,284`. Fixed by gating the total/suffix on `hasActiveFilters` rather than the count mismatch.
- SCR-024: **found and fixed** — the screen was unreachable from the sidebar (no `/organizations` nav entry) despite its `Administration › Organizations` breadcrumb. Added an `organization:manage` permission to `permissions.js` (following the `resource:action` convention; Super/Org Admin already hold `ALL_PERMISSIONS`), an Administration › Organizations leaf to `navConfig.js` gated on it, and a `nav-organizations.svg` asset wired through `navIcons.js`.
- SCR-024: **found and fixed** — load-bearing metadata (org ID, admin email, plan badge, status pill text) was rendered at `text-token-xs` (9px), which `tokens.css` reserves for decorative micro-labels (9px-on-content was flagged as a defect in the SCR-001 review). Raised to `text-token-meta` (11px). Also removed dead `KPI_TONE[*].iconWrap` keys (the KPI cards render no icon), and gave the select-all checkbox a proper `indeterminate` state for partial selections (per-page id-set membership rather than a raw size comparison, so it stays correct once MOD-004's real multi-page endpoint lands).
- SCR-024: 100% of organization data is currently mocked — MOD-004 (Organization Management) is `PLANNED` with no backend deployed (no `Organization` entity, no org CRUD/list/activate/suspend/assign-plan endpoints); MOD-002 (Auth) and MOD-003 (RBAC) that it depends on are also PLANNED. `getOrganizationList` attempts a real `/organizations` request first and falls back to the mock only on failure/non-JSON (same real-fetch-first + content-type-guard + `mocked` shape as the MOD-009 dashboard modules); a "Sample data" badge discloses the boundary in the UI.
- SCR-024: the mock is a single fixed page of 12 rows against a declared total of 1,284 — server-side search/filter/sort/pagination across the full set is not simulated; the screen applies its search + status/plan/region filters client-side over the 12 mocked rows only, and the pagination control stays single-page. A real MOD-004 endpoint would accept query params (search/status/plan/region/page/pageSize) and return the matching page plus a real total. Documented as a KNOWN LIMITATION in `organizationList.api.js`.
- SCR-024: all backend-dependent actions — Create Organization / Import / Export, the bulk Activate/Suspend/Assign Plan/Export/Delete bar, per-row View, and Columns — are permanently disabled with an explanatory `title` naming the blocking PLANNED module (MOD-004). Same disabled+title precedent and known accessibility limitation (unfocusable so the `title` is mouse-hover-only) as SCR-006/012/013/020/021/022/023; best addressed as a project-wide a11y pass (aria-disabled + handler-suppression, or visible helper text) rather than a screen-scoped patch that would break sibling parity, per agent-rules.md §9.
- SCR-024: the org sidebar/banner chrome in the Figma frame `90:8293` is a distinct platform-super-admin shell, not the built ConnectIQ AppShell — per the established precedent (SCR-010 mislabeled frame, SCR-011 ConnectIQ brand rename) the screen is built inside the existing shared AppShell with the frame's content transcribed; the alternate shell chrome is intentionally not rebuilt.
- SCR-024: browser-runtime interaction verification (search/Status-Plan-Region filter changes, active-filter chip removal, Clear all, bulk-selection + select-all indeterminate state, pagination, live-region announcements, focus management in a real DOM) is unverified — no browser-automation dependency installed in `frontend/`; lint/build/data-shape-contract/tone-map/routing were verified, in-browser behavior was flagged rather than asserted. Same blocked gap as prior screens.
- SCR-028: Figma content fidelity to node `93:3192` is `verified` — the seven setting cards (General, Security & Authentication, Notification, Branding, Integration, Operational Defaults, Compliance & Audit), the right-rail cards (Organization Summary, Validation Status 4/6, Security Health, Recent Configuration Activity, Quick Actions), the API-key table, and every field value were transcribed from the actual frame's text nodes during an earlier-session Figma MCP inspection (`get_screenshot` + `get_metadata`), documented in `organizationSettings.api.js`'s header. This session's ui-reviewer could not re-call the Figma MCP, so pixel/layout re-verification and the specific 4/6 checklist composition are the only fidelity items not independently re-confirmed this pass.
- SCR-028: DESIGN-INTENT DECISION (mirrors SCR-027) — the Figma frame shows the settings mid-edit ("2 Unsaved Changes": Display Name + SSO badged "Modified"). The screen deliberately loads the SAVED baseline (not the mid-edit snapshot); the Modified badges, unsaved-change pill, Change Summary diff, and Validation Status are computed live from the user's edits. A consequence is Validation Status reading 6/6 on a valid saved config vs. the design's 4/6 — the correct outcome of the decision, not a defect. Documented in `organizationSettings.api.js`.
- SCR-028: **found and fixed** — the `primaryBrandColor` free-text field accepted any string (only the color-swatch input guarded hex, silently falling back to `#0F5699` for display), so a value like `blue` persisted into the change diff and still counted toward the "Branding complete" checklist item. Added a `#RRGGBB` `HEX_COLOR` check to `validate()` feeding the existing inline-error mechanism, wired `error`/`onBlur`/`aria-invalid`/`aria-describedby` onto the text input, and gated the Branding checklist on `!errors.primaryBrandColor`.
- SCR-028: the unsaved-changes guard covers `beforeunload` (tab close/refresh) and `guardedNavigate` (Cancel, Back, Quick Actions, Recent Activity) but NOT the AppShell sidebar links, breadcrumb crumbs, or browser Back button — those route away silently discarding edits. Cross-cutting gap shared with SCR-027 (same button-only pattern); best closed once via a shared `useUnsavedChangesBlocker` (React Router `useBlocker`) adopted in both screens, not one alone, per agent-rules.md §9. Left as a documented open issue.
- SCR-028: `text-warning` (#d97706) on `bg-warning-bg` (#fffbeb) ≈3.1:1 on the Modified badge, the "N unsaved changes" pill, and the "Simulated save" banner heading (`token-xs`) — below the AA 4.5:1 threshold for small text. Shared design-token issue also present on SCR-021/027; best addressed as a project-wide token pass (darken `--color-warning` for text, or pair with a `warning-strong` token) rather than a screen-scoped patch that would break sibling parity. Not fixed here per agent-rules.md §9.
- SCR-028: `updateOrganizationSettings` funnels every failure — including a genuine 5xx / malformed-JSON response from a future deployed MOD-004 endpoint — into the `mocked:true` branch, so the UI discloses "no backend, nothing persisted" and `handleConfirmSave` optimistically clears dirty state regardless of result. Correct while MOD-004 is PLANNED (mirrors the sibling write modules `editOrganization.api.js`/`createOrganization.api.js`); needs a distinct real-failure path (that keeps the form dirty) before the backend lands. Documented open issue.
- SCR-028: 100% of settings data is currently mocked — MOD-004 (Organization Management) is `PLANNED` with no backend deployed (no organization-settings GET/PATCH endpoint, no key-management endpoint); MOD-002 (Auth) and MOD-003 (RBAC) it depends on are also PLANNED. `getOrganizationSettings`/`updateOrganizationSettings` attempt a real request first and fall back to the design baseline / simulated success only on failure/non-JSON (same real-fetch-first + content-type-guard + `mocked` shape as the sibling MOD-004 write modules); a "Sample data" badge + post-save "Simulated save (no backend)" banner disclose the boundary in the UI. Select option sets are design-sourced enumerations a real MOD-004 config endpoint would serve.
- SCR-028: all backend-dependent actions — logo upload, and the API-key Rotate/Revoke/Generate controls — are disabled with an explanatory `title` naming the blocking PLANNED module (MOD-002/004/005). Same disabled+title precedent and known accessibility limitation (unfocusable so the `title` is mouse-hover-only) as SCR-006/012/013/020/021/022/023/024/025/027; best addressed as a project-wide a11y pass rather than a screen-scoped patch that would break sibling parity, per agent-rules.md §9.
- SCR-028: browser-runtime interaction verification (per-field edits/toggles, dirty-state + Modified badges, Change Summary diff, Save confirm-dialog focus trap, navigation guard, live-region announcements in a real DOM) is unverified — no browser-automation dependency installed in `frontend/`; lint/build/route-serve (200)/data-contract/mock-boundary were verified, in-browser behavior was flagged rather than asserted. Same blocked gap as prior screens.
- SCR-029: **found and fixed** — the "Last Activity" sort option ordered rows by the `attention` boolean (`b.attention - a.attention`) rather than by activity recency, silently mis-sorting a shipped control. `lastActivity` is a relative-string display value ("2 min ago" / "Never" / "3 mo ago") and not `Date.parse`-able, so the fix added an explicit sortable `activityMinutes` field to every mock row (`Never` → `Number.MAX_SAFE_INTEGER`, i.e. least-recent) and changed the comparator to `(a.activityMinutes ?? Infinity) - (b.activityMinutes ?? Infinity)` (ascending → most-recent first). A real MOD-004 endpoint would return a true timestamp per row.
- SCR-029: the Figma table renders each department by code-initials + avatar only (no spelled-out name column) and only the Data Engineering (DE) drawer exposes a member/pipeline breakdown. Full department names, business units, and descriptions for the other 11 rows are derived sample values consistent with each row's Figma-verified stats; the DE member/pipeline previews are Figma-verified. Non-DE drawers show their real operational counts plus a labeled note that the detailed breakdown loads from the MOD-004 department service. All clearly labeled as sample data. A real endpoint would accept the org id + query params (search, status, manager, businessUnit, sort, page) and return matching rows plus per-department members/pipelines.
- SCR-029: 100% of department data is mocked — MOD-004 (Organization Management) is `PLANNED` with no backend (no `Department` entity, no department list/create/archive/activate/assign-manager endpoints). `getDepartmentManagement(orgId, filters)` attempts a real request first (scoped to the route org, `encodeURIComponent`) and falls back to the mock snapshot only on failure/non-JSON — same real-fetch-first + content-type-guard + `mocked:true` shape as `organizationList.api.js` (incl. the SCR-014 HTML-fallback content-type defense). A "Sample data" badge (header + footer) + a drawer sample-data note disclose the boundary.
- SCR-029: all backend-dependent actions — Import/Export/Create Department in the header, the bulk Activate/Archive/Assign Manager/Export/Delete bar, the Columns control, and the drawer View Details/Edit/Manage Members/Pipelines — are disabled with an explanatory `title` naming the blocking PLANNED module (MOD-004). Same disabled+title precedent and known a11y limitation (unfocusable → `title` is mouse-hover-only) as SCR-006/012/…/024/025/027/028; best addressed as a project-wide a11y pass rather than a screen-scoped patch that would break sibling parity, per agent-rules.md §9.
- SCR-029: `refetchInterval: 30_000` re-attempts the (absent) real endpoint every 30s while MOD-004 is PLANNED, flipping the footer to "Refreshing…" on a loop before re-returning the mock. Harmless and matches the SCR-024 cadence by design; revisit once the backend lands. Row click-to-open is a mouse affordance (`cursor-pointer`) with keyboard parity provided by the focusable per-row "View" button.
- SCR-029: browser-runtime interaction verification (search/filter/sort/pagination over the live DOM, row-open + drawer focus trap, bulk select-all indeterminate state, live-region announcements) is unverified — no browser-automation dependency installed in `frontend/`; lint/build/route-ordering/data-contract/mock-boundary were verified, in-browser behavior was flagged rather than asserted. Same blocked gap as prior screens.
- SCR-030 (2026-08-25, unit=screen, review=ui-reviewer+tester): Figma content fidelity to node `95:6572` (+ drawer `95:8111`) is `verified` by the builder — a live `get_metadata`+`get_screenshot` pass ran during the build, so the 6 KPI values (Total 41 / Active 37 / Leads 34 / Members 247 / Pipelines 183 / Attention 5), the 14 visible team rows (code, department+code, team lead / Unassigned, members, pipelines, connectors, status, last activity, created, favorite star on Pipeline Core, "Requires attention" flag), the toolbar filters (Department/Team Lead/Status/Pipeline Ownership/Created Date/Sort), the bulk-action set (Activate/Archive/Assign Department/Assign Team Lead/Export/Delete), the "Showing 14 of 41 teams" footer, and the full Pipeline Core drawer (operational summary 8/24/7 + 342 executions / 98.7% success, team information, description, ownership) are transcribed from the actual frame's text nodes. NOTE: this session's ui-reviewer could not re-call the Figma MCP (not in its tool set), so it explicitly marked pixel-level fidelity `not-verified` on its side and relied on the builder's transcription + the SCR-029 pattern of record.
- SCR-030: tester PASS — `oxlint` clean on all four new files (two warnings are pre-existing, in `AppShell.jsx`/`SplashScreen.jsx`), `vite build` clean (291 modules), and the full data contract is consistent: every field the mock rows expose is consumed and every field the screen reads (incl. all filters and all five sort options) exists on all 14 rows. No SCR-022-class (filter on absent field) or SCR-029-class (mis-wired sort) bug — `Last Activity` correctly sorts on `activityMinutes` (nulls → `Number.MAX_SAFE_INTEGER`), not on `attention`. Default `status='Active'` renders 10 populated rows, so the empty-state is not falsely triggered on first load.
- SCR-030: **found and fixed** (ui-reviewer Low #4) — the primary "Create Team" button used `text-white` instead of the `text-on-primary` token; swapped to keep the button on the token layer. (The identical SCR-029 spot is left as-is to preserve sibling parity; best fixed in a shared pass.)
- SCR-030: **found and fixed** (ui-reviewer Medium #2/#3, #7) — reconciled the code's own Figma-verification notes with the implementation: added the missing **Created** column to the teams table (the Figma frame lists a "Created" column header + per-row date, e.g. Pipeline Core `Jan 14, 2023`, confirmed from the frame's text nodes), added the favorite star to the `FIGMA VERIFICATION` enumeration, and corrected the `KNOWN LIMITATION` note (the Figma "Team" cell shows the code, not a spelled-out name; the spelled-out name column is a deliberate SCR-029-parity readability addition, clearly labeled sample data). Code-shown-twice (badge + Code column) matches the Figma frame's own `PC / PC` duplication.
- SCR-030: not-fixed, documented (ui-reviewer Low #5/#6) — whole-row click-to-open is a mouse affordance with keyboard parity via the focusable per-row "View" button (same accepted SCR-029 pattern); the drawer "View all N →" spans are inert `aria-hidden` decorations (correctly hidden from AT) that a real MOD-004 team service would wire to navigation. Left as-is to preserve SCR-029 parity per agent-rules.md §9.
- SCR-030: 100% of team data is mocked — MOD-004 (Organization Management) is `PLANNED` with no backend (no `Team` entity, no team list/create/archive/activate/assign-department/assign-lead endpoints). `getTeamManagement(orgId, filters)` attempts a real request first (scoped to the route org, `encodeURIComponent`) and falls back to the mock snapshot only on failure/non-JSON — same real-fetch-first + content-type-guard + `mocked:true` shape as `departmentManagement.api.js`/`organizationList.api.js` (incl. the SCR-014 HTML-fallback content-type defense). A "Sample data" badge (header + footer) + a drawer sample-data note disclose the boundary. All backend-dependent actions (Import/Export/Create Team, the bulk bar, drawer View Details/Edit/Manage Members/Pipelines/Connectors) are disabled with explanatory `title`s naming the blocking module — same disabled+title precedent and known a11y limitation as SCR-024/029.
- SCR-030: routes `/organizations/:id/teams` (registered before the `/organizations/:id` catch-all) and `/organizations/current/teams` (the Administration→Teams sidebar nav item, gated on `team:manage`) both render `TeamManagementScreen`; `orgId` falls back to `'current'` when the route param is absent so the org-scoped query stays enabled. `refetchInterval: 30_000` matches the SCR-024/029 cadence. The mock is a single fixed set of 14 rows against a declared `total:41` — server-side pagination across the full set is not simulated (client filters/sorts over the 14 rows, single-page control); a real MOD-004 endpoint would accept query params and return the matching page plus a real total. Documented as a KNOWN LIMITATION in `teamManagement.api.js`.
- SCR-030: browser-runtime interaction verification (search/Department-Team Lead-Status filter changes, filter-chip removal, Clear filters, bulk select-all indeterminate state, pagination, row-open + drawer focus trap, live-region announcements in a real DOM) is unverified — no browser-automation dependency installed in `frontend/`; lint/build/route-ordering/data-contract/mock-boundary were verified, in-browser behavior was flagged rather than asserted. Same blocked gap as prior screens.
